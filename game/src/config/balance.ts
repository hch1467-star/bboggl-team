import { applyTweaks } from './tweak'
/**
 * 게임의 모든 튜닝 수치를 여기 한 곳에 모읍니다.
 *
 * 설계 근거: 밸런싱은 "코드 고치기"가 아니라 "숫자 고치기"여야 합니다.
 * 숫자가 로직 안에 흩어져 있으면 밸런스 한 번 바꿀 때마다 버그가 생깁니다.
 *
 * UNITY 이식 노트: 이 파일의 각 블록이 그대로 ScriptableObject 하나가 됩니다.
 * (PlayerConfig.asset, EnemyConfig.asset, CameraConfig.asset ...)
 * 그래서 지금 JSON처럼 순수 데이터로만 유지하고, 함수는 넣지 않습니다.
 */

/** 쿼터뷰 카메라 리그 */
export const CAMERA = {
  /** 수평 회전각(도). 45° = 고전적인 쿼터뷰/아이소메트릭 각도 */
  yawDeg: 45,
  /**
   * 수직 내려보는 각(도). 30°면 아이소메트릭에 가깝고 벽/지형이 많이 보입니다.
   * 55°에 가까울수록 탑다운이 되어 바닥 장판(AoE)이 잘 보입니다.
   * 로스트아크류 ARPG는 장판 회피가 중요해서 이 정도로 눕혀 잡습니다.
   */
  pitchDeg: 52,
  /** 직교 카메라가 세로로 담는 월드 높이(m). 작을수록 확대. */
  viewSize: 22,
  /**
   * 달릴 때 시야를 이만큼 넓힙니다(22m → 26m).
   *
   * 속도 배율(1.55)보다 **훨씬 작습니다.** 속도만큼 넓히면 캐릭터가 작아져
   * 4색 예고와 등 뒤 표시를 못 읽습니다 — 이 게임이 읽히는 이유를
   * 시야 때문에 깨는 셈입니다. 잃은 반응 시간의 40%쯤만 되돌립니다.
   */
  sprintViewScale: 1.18,
  /**
   * 대상에서 카메라까지의 거리(m).
   *
   * 직교 투영이라 이 값은 **화면 크기에 아무 영향이 없습니다**(viewSize가 결정).
   * 그런데 안개(fog)는 카메라로부터의 거리로 계산되기 때문에, 이 값을 무시하고
   * 안개를 잡으면 씬 전체가 안개에 먹혀 화면이 새까맣게 나옵니다.
   * (자동 검증 스크린샷에서 실제로 잡은 버그입니다. 그래서 설정으로 뺐습니다.)
   */
  distance: 48,
  /**
   * 카메라가 플레이어를 따라가는 부드러움. 클수록 빠르게 따라붙음.
   *
   * ⚡ 9 → **12.** *"부드럽고 빠릿빠릿"* 의 나머지 절반입니다. 지수 추적이라
   * 값을 올려도 **끊기지 않고** 따라붙는 성질은 그대로이고, 화면이 몸을
   * 뒤늦게 쫓아오는 느낌만 줄어듭니다. 더 올리면(20+) 카메라가 몸에 붙어
   * 흔들림이 그대로 화면에 옮겨져 **멀미가 납니다** — 부드러움을 사는 값이
   * 남아 있어야 합니다.
   */
  followLerp: 12,
  /** 커서 방향으로 시야를 살짝 밀어주는 양(0이면 끔). 조준감이 좋아집니다. */
  aimLeadFactor: 0.18,
  aimLeadMax: 3.2,
  /** 화면 흔들림 */
  shake: {
    /** trauma가 1일 때 최대 위치 흔들림(m) */
    maxOffset: 0.55,
    /** trauma가 1일 때 최대 회전 흔들림(도) */
    maxRollDeg: 1.6,
    /** 초당 trauma 감쇠량. 클수록 빨리 잦아듦. */
    decay: 2.2,
    /** 흔들림 노이즈 주파수(Hz) */
    frequency: 22,
  },
} as const

/** 플레이어 */
/**
 * ── 기둥 1 의 **리듬 손잡이** ──────────────────────────────────────
 *
 * 모든 스킬 쿨다운에 곱합니다. 스킬마다 적어 둔 값(6~14초)은 **서로의
 * 관계**를 말하는 설계이고, 이 하나는 **두 리듬의 비율**을 말합니다.
 * 둘을 섞어 두면 "이 스킬이 저 스킬보다 무거운가"와 "스킬 구간이
 * 얼마나 자주 오는가"를 따로 못 만집니다.
 *
 * ── 왜 1.5 인가 (처음으로 잰 숫자) ────────────────────────────────
 * 기둥 1 은 *"쿨다운 도는 중에는 기본공격·회피로 버티고, 찬 순간에
 * 터뜨린다"* 고 적어 두었습니다. 이 게임의 **핵심 차별점**입니다.
 * 그런데 한 번도 재 본 적이 없었습니다. 처음 재 보니:
 *
 *   · 쓸 스킬이 하나도 없던 시간   **21%**   ← 버티는 구간
 *   · 셋 이상 준비되어 있던 시간   **34%**   ← 무기고가 가득
 *   · 스킬 53회 vs 기본 공격 29회  (스킬 비중 65%)
 *
 * **뒤집혀 있었습니다.** 터뜨림이 기본 상태이고 버티기가 예외였습니다.
 * 구조를 보면 당연합니다 — 슬롯 5개에 평균 9초면 1.8초마다 하나씩
 * 차오르는데, 스킬 한 번 쓰는 데 1초 안팎입니다. **무기고가 소비보다
 * 빨리 회복**됩니다.
 *
 * 스태미나 쪽은 멀쩡했습니다. 무기 프로브로 쉬지 않고 기본 공격만 시켜
 * 보니 회피(25)를 못 낼 만큼 낮았던 시간이 **73~88%** 였습니다. 자원은
 * 빡빡한데 **그 구간에 갈 일이 없었던** 것입니다.
 *
 * 그래서 스태미나가 아니라 쿨다운을 만집니다. 스킬에 스태미나를 물리는
 * 길은 설계가 이미 닫아 두었습니다 — *"스킬 쓸 때마다 회피 자원이
 * 사라져 답답해집니다"*(기둥 1). 그 판단은 지금도 유효합니다.
 *
 * 참고: 오공의 술법은 20~40초에 네 개뿐이고, 기본 상태는 봉술(스태미나)
 * 입니다. 로스트아크는 반대로 기본 공격이 없어 온통 쿨다운입니다.
 * 우리는 그 사이를 노렸는데, 재 보니 로스트아크 쪽에 붙어 있었습니다.
 */
export const SKILL_COOLDOWN_SCALE = 1.5

export const PLAYER = {
  maxHp: 100,
  radius: 0.45,
  height: 1.75,

  /** 이동 */
  moveSpeed: 5.4,
  /** 목표 속도까지 도달하는 가속(m/s^2). 무한대면 미끄러짐 없이 즉각 반응. */
  acceleration: 60,
  /** 초당 회전 속도(도) — 마우스 조준 방향으로 몸을 돌리는 속도 */
  turnSpeedDeg: 900,
  /**
   * ── ❤️ **"이제 한 대만 더 맞으면 끝"이라고 말하기 시작하는 체력 비율** ──
   *
   * 이 값이 `hud.ts` 안에 리터럴 `0.4` 로 박혀 있었습니다. 그런데 같은
   * 경고를 **귀에도** 내보내기로 하면서, 그 자리에 0.4 를 또 적으면 규칙의
   * 두 번째 사본이 생깁니다 — 눈과 귀가 **다른 순간에** 경고하는 날이
   * 옵니다. 이 저장소가 문턱을 베껴 적어 데인 것이 이번 세션에만 네 번입니다.
   *
   * 눈과 귀는 **같은 문턱**을 써야 합니다. 두 채널이 같은 것을 말할 때만
   * "위험"이 하나의 사실이 됩니다.
   *
   * 0.4 로 두는 이유: 잡몹 한 대가 대략 체력의 12~18% 라, 40% 는 *"두세 대
   * 남았다"* 입니다. 경고는 **마지막 한 대**가 아니라 *"이제부터 조심"* 에서
   * 시작해야 합니다 — 한 대 남았을 때 켜지면 이미 늦습니다.
   */
  lowHpWarn: 0.4,

  /**
   * ── ⚡ 스태미나 — **상한을 올리고 다시 도는 시간을 줄였습니다** ────────
   *
   * 요청: *"스태미너 상한 좀 더 늘리고 반응속도 더 빠르게. 조작감이 더
   * 부드럽고 빠릿빠릿해야 한다."*
   *
   * 마침 벤치가 같은 곳을 가리키고 있었습니다 — 롱소드의 **달성률 60%**
   * (이론상 낼 수 있는 타격의 60%만 실제로 나감). 이유는 기력이었습니다:
   * 한 바퀴 38 을 1.24초에 태우는데 회복은 34/초에 **0.55초나 쉬었다가**
   * 시작합니다. 즉 콤보 한 바퀴가 끝날 때마다 손이 **멈춰 섰습니다.**
   *
   * · maxStamina 100 → **130**
   *   롱소드 한 바퀴(38) 기준 2.6바퀴 → **3.4바퀴**. 구르기(18)까지 섞으면
   *   *"콤보 두 번 + 구르기 두 번"* 이 한 호흡에 들어옵니다. 이게 "빠릿"의
   *   대부분입니다 — 손이 멈추는 횟수가 줄어드는 것.
   *
   * · staminaRegenDelay 0.55 → **0.35**
   *   ⚠️ 여기가 **체감의 핵심**입니다. 상한만 올리면 "오래 싸운다"가 되고,
   *   지연을 줄여야 "끊기지 않는다"가 됩니다. 0 으로 두지 않은 이유는
   *   소울류가 이 지연을 두는 이유 그대로입니다 — 지연이 없으면 바닥에서
   *   한 대 치고 바로 구르는 것이 늘 가능해져, 기력이 **자원이 아니라
   *   장식**이 됩니다. 0.35 는 잡몹 예고(0.6~2초) 안에서 확실히 한 번은
   *   도는 길이입니다.
   *
   * ⚠️ 회복 **속도**(34/초)는 안 건드렸습니다. 한 번에 두 곳을 움직이면
   *    어느 쪽이 효과인지 못 가립니다 — 이 저장소가 여러 번 치른 값입니다.
   *
   * ── ⚡ 34 → **44** (회복 속도) ─────────────────────────────────────
   *
   * 요청: *"스태미너 회복속도를 좀 더 올려줘. 빠르게 치고 빠지는 컨트롤의
   * 재미를 느낄 수 있게."*
   *
   * 위에서 미뤄 둔 바로 그 값입니다. 지난번에는 상한과 지연만 움직여
   * **"손이 멈추는 횟수"** 를 줄였고, 이번에 움직이는 것은 **"한 번
   * 빠졌다가 다시 붙는 사이"** 입니다. 둘은 다른 것을 고칩니다:
   *
   *   · 상한 — 한 호흡에 **몇 번** 할 수 있나
   *   · 지연 — 멈춘 뒤 **다시 돌기까지**
   *   · 속도 — 쓴 만큼 **되찾는 데 걸리는 시간** ← 「치고 빠지기」는 여기
   *
   * 44 를 고른 근거는 **구르기 한 번을 구르는 동안 되찾는 속도**입니다:
   *
   *     구르기 비용 18 ÷ 구르기 시간 0.42초 = 42.9/초 → **44**
   *
   * 이렇게 두면 *"때리고 → 구르고 → 다시 때리고"* 가 한 박자로 이어집니다.
   * 롱소드 한 바퀴(38)를 되찾는 시간도 1.12초 → **0.86초**로, 한 바퀴를
   * 도는 시간(1.24초)보다 짧아집니다 — 즉 **콤보를 이어 가는 동안 이미
   * 다음 몫이 차 있습니다.**
   *
   * ⚠️ **왜 더 안 올리는가.** 여기를 더 올리면 기력이 자원이 아니라
   *    장식이 됩니다. 이 게임에서 기력이 자원인 이유는 *"구를까 때릴까"*
   *    를 고르게 만들기 때문이고, 무한히 구를 수 있으면 🔴·🔵 의 답이
   *    전부 "일단 구른다"로 수렴합니다. 지연(0.35초)을 그대로 둔 것도
   *    같은 이유입니다 — **되찾는 속도는 빨라지되, 시작은 여전히 늦습니다.**
   *
   * ⚠️ 이번에도 **한 곳만** 움직입니다. 상한과 지연은 그대로입니다.
   */
  maxStamina: 130,
  staminaRegen: 44,
  /** 스태미나를 쓴 뒤 회복이 시작되기까지의 딜레이(초) */
  staminaRegenDelay: 0.35,


  /** 회피 구르기 */
  dodge: {
    /**
     * ⌨️ 이 동작의 키. **여기 한 곳에만** 적습니다 — 가드 키를 옮긴 날
     * 프로브가 옛 키를 계속 누르고 있었고(V→Z), 그때 배운 것이 *"키는
     * 규칙이지 리터럴이 아니다"* 였습니다. `guardInfo().key` 와 같은 규약.
     */
    key: 'Space',
    duration: 0.42,
    distance: 4.2,
    /**
     * ── 🛡 **방어가 공격보다 싸야 합니다** ───────────────────────────
     *
     * 25 였습니다. 롱소드 기본 콤보는 10 · 11 · 17 이므로 **한 번 구르는
     * 값이 가벼운 공격 두 대(21)보다 비쌌습니다.** 소울류에서 이 부등호가
     * 뒤집힌 게임은 없습니다 — 블러드본의 퀵스텝은 공격보다 싸고, 엘든
     * 링의 구르기는 강공격보다 싸며, 세키로는 회피에서 자원을 아예 뺐습니다.
     * 방어가 더 비싸면 **공격한 사람은 방어할 수 없습니다.**
     *
     * 3판 벤치가 그 결과를 그대로 찍었습니다:
     *   · 맞은 이유의 60% 가 `못 피함`, 그중 손이 묶인 이유는 **전부 stamina**
     *   · 교전 중 **33%** 의 시간이 구르기 값(25)에 못 미쳤습니다
     *
     * ⚠️ **문턱을 없애는 쪽은 이미 해 보고 되돌렸습니다.** *"0보다 크면
     *    나간다"* (소울류의 실제 규칙)로 바꿨더니 봇이 판당 42회를 빚내며
     *    영구 파산 상태가 됐고, 받은 피해 162 → 280, 클리어 195 → 248초로
     *    **더 나빠졌습니다.** 자세한 기록은 DESIGN.md 참고. 고칠 곳은
     *    **문턱이 아니라 값**이었습니다.
     *
     * 18 로 잡은 근거: 롱소드 가벼운 공격 두 대(21)보다 싸고, 콤보 한
     * 바퀴(38)의 절반 아래입니다. 다시 채우는 데 0.53초(34/초) —
     * 잡몹 예고 0.6~2초 안에 **한 번은 회복됩니다.**
     */
    /**
     * ── 🔓 **0 — 구르기를 기력에서 떼어냅니다** ────────────────────────
     *
     * 요청: *"우선은 구르기를 무한히 쓸 수 있도록 해보자. 너무 밸런스가
     * 붕괴되면 다른 방식으로 난이도를 높이는 거로 하고, 추후에 쿨타임을
     * 추가해도 되니까 일단은 없애고 진행해봐."*
     *
     * 이전 값은 **18** 이었습니다(그 전엔 25). 위 문단들이 그 근거를
     * 적어 두었으니 되돌릴 때 그대로 쓰십시오 — **지우지 않고 남깁니다.**
     *
     * ── 이 한 줄이 실제로 바꾸는 것 두 가지 ─────────────────────────
     * ① 구르기가 기력에 막히지 않습니다(`dodgeBlockReason` 의 두 문턱이
     *    모두 `>= 0` 이 되어 늘 통과합니다).
     * ② **공격도 덜 막힙니다.** `canAffordAttack` 이 «구르기 한 번 분»을
     *    남겨 두려고 공격을 세우고 있었는데(`reserveMult 1`), 그 예약분이
     *    0이 됩니다. *"빠릿하지 않다"* 의 상당 부분이 여기였을 수 있습니다 —
     *    기력이 남았는데도 **구르기 몫을 아끼느라** 손이 멈추던 자리입니다.
     *
     * ⚠️ **알고 들어가는 위험** — 방금 벤치가 잰 값이 이렇게 말합니다:
     *    보스의 예고는 **붙어 있을수록 죽습니다**(판정까지 간 예고는
     *    평균보다 1.4~3.1m 멀리서 걸린 것들입니다). 구르기가 공짜가 되면
     *    플레이어가 더 오래 붙어 있게 되고, 그러면 보스가 자기 패턴을
     *    **더 못 끝냅니다.** 난이도가 아니라 **보스가 말을 못 하게** 되는
     *    쪽이 먼저 무너질 수 있습니다. 그건 재서 확인합니다.
     *
     * ⚠️ 예전에 문턱만 *"0보다 큼"* 으로 바꿔 본 적이 있고, 봇이 판당
     *    42회를 빚내며 받은 피해가 162 → 280 으로 늘어 되돌렸습니다.
     *    **이번은 다릅니다** — 그때는 «빚을 허용»했고 지금은 «값을 0으로»
     *    합니다. 빚이 없으니 파산도 없습니다. 그래도 같은 자리이니
     *    `npm run bench` 의 받은 피해를 반드시 나란히 보십시오.
     */
    staminaCost: 0,
    /**
     * ── 🛡 **공격이 남겨 두어야 할 구르기 몫** (구르기 값의 배수) ────────
     *
     * 1 이면 *"어떤 공격을 내도 구르기 한 번 분은 남는다"* 가 약속이 됩니다.
     * 0 이면 예전과 같습니다(공격이 0까지 다 씀).
     *
     * 근거와 앞선 두 번의 실패는 playerControl.ts `canAffordAttack` 주석에
     * 한 번만 적어 뒀습니다 — **판단하는 자리에** 붙여 둡니다.
     *
     * ⚠️ 1 로 잡은 이유: *"한 번은 구를 수 있다"* 는 세어 볼 수 있는
     *    약속이지만 *"1.5번 분"* 은 아무 뜻이 없습니다. 검사할 수 없는
     *    값은 다음 사람이 이유 없이 만지게 됩니다.
     */
    reserveMult: 1,
    /** 무적 프레임 구간(초). 시작 직후가 아니라 살짝 뒤부터 — 이게 소울라이크식입니다. */
    iFrameStart: 0.06,
    iFrameEnd: 0.3,
    /**
     * 구르기 끝나고 다시 구를 수 있을 때까지.
     *
     * ⚡ 0.12 → **0.06.** 연속으로 굴러 거리를 벌 때 손이 한 박자 걸리는
     * 자리였습니다. 무적 구간(0.06~0.3초)은 그대로이므로 *"굴러서 다 넘긴다"*
     * 가 되지는 않습니다 — 줄어드는 것은 **아무 일도 안 일어나는 시간**뿐입니다.
     * (기력 18 이 여전히 연타를 막습니다.)
     */
    cooldown: 0.06,
    /**
     * ── 공격을 **구르기로 끊을 때** 더 내는 스태미나 ─────────────────
     *
     * 지금까지 구르기는 공격의 **후딜에서만** 빠져나올 수 있었습니다.
     * 예고나 판정 중에 누르면 눌러 두기만 하고(선입력), 후딜이 와야
     * 나갔습니다. 그게 "공격은 커밋이다"라는 소울류 규칙이었고, 저는
     * 그걸 지키는 쪽으로 만들었습니다.
     *
     * 그런데 이 게임이 참고하는 셋 중 둘(오공·NRFTW)은 반대로 갑니다 —
     * **끊을 수 있되 값을 치릅니다.** 그쪽이 만드는 것은 "실수를 되돌릴
     * 기회"가 아니라 **판단**입니다: 지금 이 한 대를 포기하고 살 것인가.
     * 못 끊으면 그건 판단이 아니라 그냥 죽음이고, 공짜로 끊으면 판단이
     * 아니라 그냥 버튼입니다.
     *
     * 그래서 **낼 수는 있게, 대신 비싸게** 만듭니다.
     * 기본 25 + 이 값 20 = **45** — 스태미나 최대치의 거의 절반입니다.
     * 두 번 연속으로는 못 끊습니다. 그게 "아껴 두는 탈출구"가 되는 값입니다.
     *
     * ⚠️ **후딜에서 빠져나오는 것은 그대로 25입니다.** 그건 원래 열려 있던
     * 길이고, 값을 매기면 이미 만들어 둔 이어짐이 도로 막힙니다.
     */
    /**
     * 🔓 **0 — 위 `staminaCost` 와 함께 뗍니다.**
     *
     * 이 값만 남기면 *"공격 중에는 여전히 기력이 있어야 구른다"* 가 되어
     * 구르기가 **무한이 아니게** 됩니다. 요청은 «무한히 쓸 수 있도록»
     * 이므로 공격 취소 쪽도 같이 엽니다.
     *
     * 이전 값 **20**. 이 값이 하던 일은 *"지금 이 한 대를 포기하고 살
     * 것인가"* 라는 판단을 만드는 것이었습니다(위 문단). 그 판단이
     * 사라지므로, 되살릴 때 여기부터 보십시오 — 난이도를 다시 올릴
     * 자리로 **구르기 쿨다운보다 이쪽이 먼저**일 수 있습니다.
     */
    cancelExtraCost: 0,
  },

  // 기본 공격 콤보 · 콤보 윈도우 · 공격 중 이동 배율은 **무기마다 다르므로**
  // config/arsenal.ts 의 WEAPONS 로 옮겼습니다. (대검은 2타, 쌍단검은 4타 등)

  /**
   * ── 🏃 달리기 — **걷기와 구분되는 두 번째 이동** ──────────────────
   *
   * 지금까지 이동은 5.4m/s 하나뿐이었습니다. 그래서 존을 걷는 시간과
   * 싸우는 시간이 **같은 속도**로 흘렀습니다. 자동 플레이가 잰 값이
   * 그 대가를 그대로 보여 줍니다 — 교전 사이 빈 시간 평균 4초, 최장 12초.
   * 그 시간에 플레이어가 하는 판단은 없습니다. 그냥 걷습니다.
   *
   * ── 왜 스태미나를 안 쓰는가 ──────────────────────────────────
   * 소울류는 달리기에 스태미나를 씁니다. 우리는 **안 씁니다.**
   * 기둥 1이 *"스태미나 = 기본기·회피 전용"* 이라고 못박았기 때문입니다.
   * 달리기가 스태미나를 먹으면 "달릴까 구를까"가 판단의 중심이 되고,
   * 애써 만든 두 리듬(스킬 쿨 · 스태미나)이 셋으로 흐려집니다.
   *
   * 대신 **전투가 값을 냅니다**: 무기를 휘두르면 즉시 걷기로 돌아옵니다.
   * 달리기는 "싸우지 않는 동안의 이동"이지 전투 기술이 아닙니다.
   * 그래서 도망이 공짜가 되지도 않습니다 — 등을 보이면 때릴 수 없습니다.
   *
   * ── 수치의 근거 ──────────────────────────────────────────────
   * · 1.55배(8.4m/s) — 빈 시간 12초짜리 구간이 7.7초가 됩니다. 2배로
   *   올리면 쿼터뷰에서 화면 밖으로 튀어나가 길을 읽을 수가 없습니다
   *   (카메라가 세로로 담는 것이 22m 입니다).
   * · 붙는 데 0.3초 — 즉시 최고속이면 걷기와 달리기가 **같은 것**이 되고,
   *   길면 조작이 굼떠집니다. 소울류의 가속 구간과 같은 이유입니다.
   * · 끊길 때는 **즉시** — 공격하려는 순간 미끄러지면 조준이 어긋납니다.
   */
  sprint: {
    speedScale: 1.55,
    /** 최고 속도까지 붙는 시간(초). 끊길 때는 즉시. */
    rampUp: 0.3,
  },

  /**
   * ── ⚔️ **상황이 모션을 바꿉니다** — 같은 버튼, 다른 공격 ────────────
   *
   * 지금 무기 하나가 가진 것은 콤보 2~4타 + 강타 + 처형입니다. 그런데
   * **상황이 결과를 안 바꿉니다** — 서 있든 달리든 막 굴렀든, 좌클릭은
   * 늘 같은 1타입니다. 손이 하나뿐인 게임이 됩니다.
   *
   * ── 다른 게임의 깊이는 대부분 여기서 나옵니다 ──────────────────
   * · **다크 소울 / 엘든 링** — 무기 하나의 모션 표가 이렇게 갈립니다:
   *   서서 · **달리며** · **구른 직후** · 백스텝 뒤 · 점프. 버튼은 하나이고
   *   **상태가 기술을 고릅니다.** 특히 *구르기 공격*은 소울류의 교과서적인
   *   반격 수단입니다 — 읽고 굴러 넘긴 사람에게 **갚을 손**을 줍니다.
   * · **몬스터헌터** — 회피 직후 파생기가 무기마다 따로 있습니다.
   * · **세키로** — 대시 중 공격이 거리를 좁히는 유일한 수단입니다.
   * · **검은 신화: 오공** — 달리며 치는 것과 서서 치는 것이 아예 다른 기술입니다.
   *
   * 이 게임의 자동 플레이는 시간의 **24%를 접근**, **20%를 구르기**에
   * 씁니다. 그 44%는 지금 **공격 선택지가 없는 시간**입니다. 여기에
   * 모션을 붙이면 새 키 없이 무기마다 기술이 둘씩 늘고, 무엇보다
   * *"이동과 회피가 곧 공격의 일부"* 가 됩니다.
   *
   * ⚠️ 값은 **무기마다 적지 않습니다.** 강타·처형과 같은 방식으로 그 무기의
   *    1타에서 파생시킵니다(arsenal.ts `runningStep` · `rollingStep`).
   *    셋을 따로 적으면 대검만 고치고 단검을 빠뜨리는 날이 옵니다.
   */
  contextAttack: {
    /**
     * 🏃 **달리기 공격** — 거리를 좁히며 한 방. 커밋이 큽니다.
     *
     * 파고들기를 크게(×2.6) 준 것이 이 기술의 전부입니다 — 접근 시간을
     * 공격으로 바꾸는 것이 목적이니까요. 대신 후딜이 길어(×1.3) *"일단
     * 달려들고 본다"* 가 답이 되지 않습니다. 소울류의 달리기 공격이
     * 정확히 이 계약입니다: **거리는 벌지만 빗나가면 크게 문다.**
     */
    running: {
      damageMult: 1.25,
      rangeMult: 1.15,
      lungeMult: 2.6,
      windupMult: 1.15,
      recoveryMult: 1.3,
      staminaMult: 1.6,
      arcAdd: -10,
    },
    /**
     * 🤸 **구르기 공격** — 굴러 넘긴 **직후**에만 나가는 빠른 한 방.
     *
     * 예고를 짧게(×0.7) 준 이유: 이건 갚는 손입니다. `punish.ts` 가 재는
     * *"정답대로 답한 사람에게 돌아오는 길"* 이 지금은 **1타의 예고만큼
     * 늦습니다.** 구르기 공격은 그 길을 짧게 만듭니다.
     *
     * 대신 피해가 낮습니다(×0.9) — 갚는 한 방이지 주력이 아닙니다.
     * 소울류에서도 구르기 공격은 딜이 아니라 **자리**를 버는 기술입니다.
     */
    rolling: {
      damageMult: 0.9,
      rangeMult: 1.05,
      lungeMult: 1.4,
      windupMult: 0.7,
      recoveryMult: 1.0,
      staminaMult: 1.2,
      arcAdd: 0,
    },
    /**
     * 구르기가 끝난 뒤 **이만큼 안에** 누르면 구르기 공격이 됩니다.
     *
     * 0.35초 근거: 선입력 창(0.55초)보다 **짧게** 잡습니다. 같거나 길면
     * 구르기 중에 눌러 둔 평범한 공격이 전부 구르기 공격으로 바뀌어,
     * *"굴러 넘기고 갚는다"* 라는 선택이 사라지고 그냥 기본기가 됩니다.
     * 짧게 두면 **굴러 넘긴 직후를 노려 누르는 것**이 됩니다.
     */
    rollWindow: 0.35,

    /**
     * 🪂 **낙하 공격** — 높이를 무기로 바꾸되, **값을 치릅니다.**
     *
     * ── 왜 이 게임에 없었나 ────────────────────────────────────────
     * 이 존은 높이로 지어져 있습니다 — 절벽, 단차, 사다리, `MAX_CLIMB`,
     * VOID. 자동 플레이는 판마다 `적을 떨어뜨림 4회 · 내가 떨어짐 1~2회`
     * 를 찍습니다. 그런데 **떨어지는 것이 손해이기만** 했습니다. 지형이
     * 전투의 일부가 되는 길이 하나도 없었습니다.
     *
     * ── 참고 게임 ──────────────────────────────────────────────────
     * · **다크 소울 · 엘든 링** — 낙하 공격은 높이를 피해로 바꾸는 유일한
     *   수단이고, 강인도를 크게 깎아 처형으로 잇습니다. **너무 높으면
     *   내가 다칩니다** — 그래서 "얼마나 높은 데서 뛸까"가 판단이 됩니다.
     * · **세키로** — 공중에서 인살로 잇습니다.
     * · **검은 신화: 오공** — 도약 내려찍기가 별도 기술입니다.
     *
     * ── 우리 구조에 맞춘 형태 ──────────────────────────────────────
     * 이 게임에는 **공중이라는 상태가 없습니다**(physics.ts 설계 노트 —
     * 쿼터뷰에서 체공을 넣으면 조작감만 나빠집니다). 그 판단을 뒤집지
     * 않고, 이미 있는 **상황 모션** 체계에 얹습니다:
     *
     *     떨어진 직후 짧은 창 안에 치면 → 낙하 공격
     *
     * 소울류가 파는 것도 체공이 아니라 **"높이를 값과 맞바꾼다"** 입니다.
     * 낙하는 이미 체력을 깎으므로(`FALL.damagePerStep`), 뛰어내리는 것은
     * 그 자체로 대가가 있는 선택입니다.
     *
     * ⚠️ 창을 구르기(0.35초)보다 **짧게** 잡습니다. 떨어진 뒤 한참 있다
     *    치는 것까지 낙하 공격이 되면 *"뛰어내리고 곧바로"* 라는 판단이
     *    사라지고 그냥 보너스가 됩니다.
     */
    plunge: {
      /** 기본 배율 — 여기에 낙하 단수만큼 더 붙습니다(아래 perStep). */
      damageMult: 1.4,
      rangeMult: 1.1,
      lungeMult: 0.4,
      windupMult: 0.9,
      recoveryMult: 1.25,
      staminaMult: 1.3,
      arcAdd: 30,
      /**
       * **한 단 더 떨어질 때마다** 피해·강인도가 이만큼 더 붙습니다.
       *
       * 0.35 근거: 무료 낙하가 2단이므로 3단에서 ×1.4, 5단이면 ×2.1 입니다.
       * 그 5단은 체력의 36%를 깎습니다(`damagePerStep` 0.12 × 3단).
       * **큰 것을 얻으려면 크게 다칩니다** — 그게 이 기술의 전부입니다.
       */
      perStep: 0.35,
      /** 강인도는 더 크게 붙습니다 — 낙하 공격의 값은 무너뜨림입니다. */
      poiseMult: 2.2,
    },
    /** 떨어진 뒤 이 시간 안에 쳐야 낙하 공격이 됩니다(구르기 창보다 짧게). */
    plungeWindow: 0.28,
  },

  /**
   * ── ⏱️ 템포 — 공격의 앞뒤를 함께 줄입니다 ────────────────────────
   *
   * 콤보 한 바퀴가 롱소드 1.54초 · 대검 1.80초였습니다. 판정(active)은
   * 그대로 두고 **예고(windup)와 후딜(recovery)만** 줄입니다:
   *   · active 를 건드리면 **맞던 것이 안 맞습니다.** 판정 창은 손맛이
   *     아니라 규칙이고, 적 예고 길이와 맞물려 있습니다.
   *   · 체감 속도의 대부분은 후딜입니다 — 휘두른 뒤 못 움직이는 시간.
   *
   * 0.82배: 롱소드 1.54 → 1.29초, 대검 1.80 → 1.51초.
   * 무기 사이의 **비율은 그대로**이므로 "대검은 느리고 무겁다"는 정체성이
   * 유지됩니다(`npm run weapons` 가 그 비율을 검사합니다).
   *
   * ⚠️ 초당 피해가 같이 오릅니다. 그건 의도한 것입니다 — 템포를 올린다는
   * 것은 곧 시간당 판정을 늘린다는 뜻입니다. 적이 너무 쉬워지면 고칠 곳은
   * 이 값이 아니라 **적의 체력·수**입니다. 여기서 되돌리면 템포도 같이
   * 돌아옵니다.
   */
  tempo: {
    attackScale: 0.82,
    /**
     * 후딜의 이 비율만 남으면 다음 콤보로 이어집니다(예전 0.45).
     * 올릴수록 콤보가 촘촘해집니다. 0.6 이면 후딜의 40%를 건너뜁니다.
     */
    comboCancel: 0.6,
    /**
     * ── ⚡ **적중 캔슬(평캔)** — 맞혔으면 곧바로 스킬로 끊는다 ───────────
     *
     * 규칙 한 줄: **맞히면 남은 판정·후딜을 기다리지 않고 스킬을 낼 수 있다.
     * 헛치면 그대로 다 치른다.**
     *
     * 로스트아크의 「평캔」이 정확히 이것입니다 — 평타로 평타를 끊는 것이
     * 아니라 **평타 모션을 스킬로 끊는 것**입니다. 몬헌의 히트 캔슬,
     * 오공의 연격도 같은 물건이고, 셋 다 **적중을 조건으로** 겁니다:
     *
     *   · 조건 없이 열면 그냥 **후딜을 줄인 것**입니다. 그건 값 하나로 될
     *     일이고, 실제로 위 `attackScale` 이 이미 하고 있습니다.
     *   · 적중을 조건으로 걸면 **잘 맞힌 사람만 손이 빨라집니다.** 허공을
     *     가른 사람은 소울류의 그 무게를 그대로 치릅니다. 잘하는 쪽에 상을
     *     주고 못하는 쪽에 벌을 주는, 같은 장치 하나로.
     *
     * 1 = **후딜 전 구간**(0.6 이던 comboCancel 이 "후딜의 40%를 건너뛴다"
     * 였으니, 1 은 "통째로 건너뛴다"). 값으로 두는 이유는 이 저장소의
     * 규칙입니다 — **문턱은 규칙이지 리터럴이 아닙니다.**
     *
     * ── ⚠️ **스킬을 내는 데만 열립니다** — 이건 재 보고 좁힌 것입니다 ────
     * 처음엔 "다음 공세"라면 무엇이든(평타 포함) 열었습니다. `npm run weapons`
     * 를 **켜고 끄고 두 번** 돌린 결과입니다(초당 피해, 처형 포함):
     *
     *     끈 채(기준선)        롱소드 53   · 대검 63.25 · 쌍단검 75.05  → 1.416배
     *     평타까지 열었을 때    롱소드 55.3 · 대검 61.95 · 쌍단검 83.6   → 1.51배 ❌
     *     스킬만 열었을 때      롱소드 53   · 대검 63.2  · 쌍단검 75.05  → 1.416배 ✅
     *
     * 평타까지 열자 **쌍단검만 11% 튀어서** 「꼴찌 무기가 없게」 검사가
     * 터졌습니다(1.5배 상한). 평타는 쿨다운이 없어 문지기가 스태미나뿐인데,
     * 쌍단검은 한 대가 기력 5짜리라 그 문지기가 사실상 없습니다. 즉 이
     * 규칙은 **가장 가벼운 무기에게만 몰아 주는** 규칙이었습니다.
     *
     * 스킬만 열면 **쿨다운이 문지기**라 그런 일이 없습니다 — 실제로 초당
     * 피해가 기준선에서 **한 자리도 안 움직였습니다.** 그게 이 변경이
     * 원한 그림입니다: **조작감만 사고 밸런스는 안 건드린다.** 그리고
     * 그게 애초에 「평캔」이라는 말의 뜻이기도 합니다 — 평타로 평타를
     * 끊는 것이 아니라 평타 모션을 스킬로 끊는 것.
     *
     * ⚠️ 처음엔 이 자리에 *"36 → 55, 55% 폭등"* 이라고 적었습니다. **틀린
     *    비교였습니다** — 36 은 스태미나·템포를 손보기 전 옛 코드의 값이라
     *    지금 코드의 기준선이 아니었습니다. 켜고 끈 A/B 를 실제로 돌리기
     *    전까지 그 숫자를 근거로 설계를 정할 뻔했습니다. **비교 앞에
     *    게이트를 세운다** 는 이 저장소의 규칙이 여기서도 값을 했습니다.
     *
     * ── ⚠️ **빠져나가는 길에도 안 겁니다** ─────────────────────────────
     * 구르기·성수병·가드는 지금 규칙 그대로입니다. 구르기 취소(기력 45)가
     * 적중으로 싸지면 **한 대 치고 무적으로 빠지는 것이 모든 상황의 정답**이
     * 되고, 그러면 4색 예고를 읽을 이유가 사라집니다.
     */
    hitCancel: 1,
    /**
     * 스킬 후딜의 남은 비율이 이 아래면 다음 동작으로 빠져나갈 수 있습니다.
     *
     * 평타의 `comboCancel`(0.6)보다 **좁습니다** — 스킬은 한 방이 크고
     * 쿨다운을 태우므로 커밋도 그만큼 무거워야 합니다. 값 자체는 원래
     * 이 파일이 아니라 playerControl.ts 에 `0.5` 라고 **네 군데 흩어져**
     * 있었습니다. 그중 둘만 적중 캔슬로 열게 되면서, 나머지 둘이 조용히
     * 다른 값이 될 길이 생겼습니다 — 그래서 이름을 붙여 한 곳에 모읍니다.
     * **문턱은 규칙이지 리터럴이 아닙니다.**
     */
    skillChain: 0.5,
    /**
     * ── 선입력(버퍼) 창 — **템포가 "이어지는" 이유** ────────────────
     *
     * 지금까지 이 게임의 입력은 **누른 그 프레임에만** 유효했습니다.
     * 공격 선행동작 중에 구르기를 누르면 그냥 사라졌고, 구르는 도중에
     * 공격을 누르면 역시 사라졌습니다. 그래서 손이 배우는 것이
     * *"언제 눌러야 하는가"* 가 아니라 *"언제까지 참았다가 눌러야 하는가"*
     * 였습니다. 동작 하나하나는 빨라도 사이가 끊기는 것이 이것 때문입니다.
     *
     * 공격 템포 · 회피 템포 · 반격(🟢) 세 가지가 전부 같은 뿌리였습니다.
     * 반격은 특히 그렇습니다 — 초록 예고는 후딜 중에 뜨는데, 그때 누른
     * 공격이 버려지면 **읽었는데도 답할 수 없습니다.**
     *
     * ── 왜 0.55초인가 ──────────────────────────────────────────────
     * 게임 안에서 **가장 오래 기다려야 하는 이어짐**을 덮어야 합니다:
     *
     *   구르기 지속 0.42 + 구르기 쿨다운 0.12 = **0.54초**
     *
     * 즉 구르기가 시작되자마자 다음 구르기를 눌러도 그 입력이 살아서
     * 연속 구르기가 됩니다. 이보다 짧으면 가장 급한 순간(연속 회피)에
     * 정확히 버퍼가 없는 것과 같아집니다.
     *
     * 더 길게 잡지 않는 이유: 0.6초를 넘어가면 **취소한 줄 알았던 입력이
     * 뒤늦게 튀어나옵니다.** 버퍼의 값어치는 "눌러 둔 것이 나온다"이지
     * "안 누른 것이 나온다"가 아닙니다.
     *
     * ⚠️ **버퍼는 취소가 아닙니다.** 눌러 두는 것을 허용할 뿐, 선행동작이나
     * 판정 중에 빠져나가게 해 주지는 않습니다. 커밋은 그대로입니다 —
     * 소울류에서 공격이 무거운 이유를 버퍼로 지우면 안 됩니다.
     */
    inputBuffer: 0.55,
  },

  /** 피격 시 경직 시간(초) */
  hurtStagger: 0.28,
  /** 피격 후 무적(연속 피격 방지) */
  invulnAfterHit: 0.45,
} as const

/**
 * ⏱ **사람이 반응하는 데 걸리는 시간** — 설계 예산입니다.
 *
 * ── 왜 이 값이 필요해졌는가 ────────────────────────────────────────
 * `npm run rules` 는 4색의 정답이 성립하는지를 재고 있었습니다. 그런데
 * 그 계산에는 **사람이 들어 있지 않았습니다.** 예를 들어 🟡 의 정답
 * ("걸어서 이탈")을 이렇게 쟀습니다:
 *
 *   갈 수 있는 거리 = 걷는 속도 × **예고 길이 전체**
 *
 * 이건 *"예고가 뜬 첫 프레임에 이미 반대 방향으로 걷기 시작한 플레이어"*
 * 를 가정한 것입니다. 그런 사람은 없습니다. 색을 **보고**, 그게 무슨
 * 색인지 **알아보고**, 어느 쪽으로 갈지 **정하고** 나서야 손이 움직입니다.
 * 그 시간을 빼지 않으면 모든 검사가 실제보다 후하게 통과합니다.
 *
 * ── 고정값을 쓰려다 **바로 데었습니다** ───────────────────────────
 * 처음엔 `choice: 0.45` 하나를 박아 뒀습니다. "4색이니까 4지선다"라는
 * 뜻이었습니다. 그런데 프로브를 돌리자마자 이렇게 나왔습니다:
 *
 *   ❌ 색이 넷을 넘지 않는다 — **5색** · 🔴 🟡 🔵 🟣 ?4
 *
 * 🟢 반격이 다섯째 색으로 들어와 있었습니다(enemyAttacks.ts). 색을 늘린
 * 것은 좋은 결정이었지만, **반응 예산은 아무도 안 올렸습니다.** 예산을
 * 숫자로 박아 두면 정확히 이런 식으로 조용히 낡습니다.
 *
 * ── 그래서 **세지 않고 계산합니다** (Hick의 법칙) ──────────────────
 * 반응 시간은 선택지 수에 따라 늘어납니다. "뭔가 왔다, 피해"는 빠르고,
 * "이건 다섯 중 어느 색이지, 그럼 답이 뭐지"는 느립니다. 인간공학에서
 * 오래 알려진 관계입니다:
 *
 *   반응 시간 = base + perChoice × log2(선택지 + 1)
 *
 * 선택지 수는 **게임이 자기 데이터에서 셉니다**(실제로 쓰이는 색 가짓수).
 * 그래서 색을 여섯째로 늘리면 예산이 저절로 올라가고, 예고가 모자라면
 * `npm run react` 가 그날 바로 막습니다. 아무도 이 상수를 기억하지
 * 않아도 됩니다.
 *
 * ── 두 계수를 고른 근거 ───────────────────────────────────────────
 * 지어낸 값이 아니라, **널리 알려진 두 지점을 지나가도록** 맞췄습니다:
 *
 *   선택지 1개 → 0.26초  (고전적인 단순 반응 0.22~0.28초)
 *   선택지 4개 → 0.45초  (고전적인 4지선다 반응 0.40~0.50초)
 *
 * ⚠️ **이건 우리 플레이어를 잰 값이 아닙니다.** 측정치가 아니라 **예산**
 *    입니다. 그래서 프로브에 숨겨 넣지 않고 여기 밖으로 꺼내 둡니다 —
 *    어떤 검사가 이 값 때문에 실패하면, 게임을 고칠지 예산을 고칠지
 *    **눈으로 보고 정할 수 있어야** 합니다.
 *
 * ── 이 값을 낮추는 정당한 방법도 있습니다 ─────────────────────────
 * 세키로가 위험 공격에 색이 아니라 **큰 아이콘 + 소리**를 쓰는 이유가
 * 이것입니다 — 판별을 선택 반응에서 단순 반응 쪽으로 끌어내리면 예고를
 * 짧게 유지할 수 있습니다. 예고를 늘리는 것만이 답은 아닙니다.
 */
export const REACTION = {
  /** 보고 손이 움직이기까지의 고정분 (선택이 없어도 드는 시간) */
  base: 0.12,
  /** 선택지가 늘 때 붙는 기울기 — Hick의 법칙의 b */
  perChoice: 0.14,
  /**
   * 답이 **하나뿐인** 것을 알 때의 반응. 색을 가릴 필요가 없는 경우에 씁니다.
   * 위 두 계수에서 나온 값과 같아야 하므로 여기 적지 않고 계산합니다.
   */
} as const

/**
 * ── 🖥 **손끝 차이를 세는 기준 프레임** ────────────────────────────
 *
 * 「한 프레임 이상 더 멎는가」로 묻는 검사가 여럿입니다(히트스톱 등급,
 * 콤보 마무리). 그런데 *"한 프레임"* 이 게임 어디에도 없었습니다 —
 * `npm run feel` 이 `1 / 60` 을 **손으로 적어** 쓰고 있었습니다.
 *
 * ⚠️ `core/time.ts` 의 `MAX_FRAME_DT`(1/20) 를 쓰면 안 됩니다. 그것은
 *    **탭 전환 복귀용 안전 상한**이지 목표 프레임이 아닙니다. 이 저장소의
 *    검증 기계는 GPU 가 없어 **매 프레임 그 상한에 붙습니다**(0.05초).
 *    그 값으로 「한 프레임」을 세면 *"느린 기계에서는 손끝 차이가 없다"*
 *    가 되어, 게임이 아니라 **기계**를 검사하게 됩니다.
 *
 * ⚠️ 그리고 이건 `REACTION` 과 같은 종류입니다 — **측정치가 아니라
 *    예산**입니다. 우리 플레이어의 화면이 60fps 라는 가정이고, 가정이니
 *    프로브 안에 숨기지 않고 여기 꺼내 둡니다. 어떤 검사가 이 값 때문에
 *    갈리면 **게임을 고칠지 가정을 고칠지 눈으로 보고** 정해야 합니다.
 */
export const FEEL = {
  /** 손끝 차이를 셀 때의 한 프레임(초) — 60fps 가정. */
  frame: 1 / 60,
} as const

/** 선택지가 n개일 때의 반응 시간(초) — Hick의 법칙. */
export function reactionTime(choices: number): number {
  return REACTION.base + REACTION.perChoice * Math.log2(Math.max(1, choices) + 1)
}

/**
 * ── 💥 **폭발통** — 플레이어가 만드는 🟡 ────────────────────────────
 *
 * DESIGN.md 「아직 안 한 것」에 유일하게 *"이건 정말 없습니다"* 로 남아
 * 있던 항목입니다. 옆 항목(밀어 떨어뜨리기)은 열어 보니 **이미 있었고**
 * 없던 것은 눈금이었습니다. 그래서 이번에도 순서를 지켰습니다 —
 * 코드에 폭발이 있는지 먼저 찾고(없음), **놓을 자리가 있는지 먼저 재고**
 * (반경 4m 기준 동선 6m 안에 적 2마리 이상 담는 칸 22개), 그 다음에
 * 만들었습니다.
 *
 * ── 무엇이 새로운가 ────────────────────────────────────────────────
 * 이 게임의 4색은 전부 **적이 던지고 내가 답하는** 것이었습니다.
 * 폭발통은 **내가 던지는 🟡** 입니다 — 때리면 도화선이 붙고, 그 동안
 * 반경 안의 모두가(나 포함) *"걸어서 이탈"* 이라는 **같은 답**을 해야
 * 합니다. 새 동사를 안 만들고 **이미 가르친 동사의 주어를 바꾸는** 것이
 * 요점입니다. 소울류의 폭발통, 젤다의 폭탄통, 오공의 화염 항아리가
 * 전부 같은 자리에 있습니다.
 *
 * ── ⚠️ **피해를 안 줍니다** (오사와 같은 이유) ──────────────────────
 * `applyPoise` 위 오사 주석의 결론을 그대로 따릅니다:
 * *"유인이 싸움을 대신하면 안 됩니다."* 통이 적을 죽일 수 있으면
 * "통 있는 자리로 끌고 가기"가 전투의 정답이 되고, 애써 만든 무기·색·
 * 강인도가 통 앞에서 전부 무의미해집니다.
 *
 * 대신 **자세를 무너뜨립니다.** 무너진 적에게는 이미 처형이 열려 있으니
 * (POISE 주석), 보상은 충분하고 **싸움은 여전히 손으로** 해야 합니다.
 *
 * ── ⚠️ 그럼 나는 무엇을 잃는가 ─────────────────────────────────────
 * 대가가 없으면 통은 "항상 쓰는 공짜 버튼"이 됩니다. 그래서 휘말린
 * 플레이어는 **스태미나를 잃습니다**(구르기 2회분 = `staminaCost` ×2).
 * 피해가 아니라 스태미나인 이유: 기둥 1이 *"스태미나 = 기본기·회피 전용"*
 * 이라 이 자원이 곧 **다음 몇 초의 선택지**입니다. 적 셋을 무너뜨려 놓고
 * 정작 처형하러 갈 스태미나가 없으면, 그 판단은 진짜 판단이 됩니다.
 *
 * 즉 폭발은 **모두의 자세를 무너뜨립니다.** 적은 강인도로, 나는
 * 스태미나로 — 같은 사건이 각자의 자원에 걸립니다.
 */
/**
 * 🏺 **항아리** — 부술 수 있는 잡동사니. 그리고 그중 하나가 보물입니다.
 *
 * ── ⚠️ 여기서 한 번 방향을 잘못 잡았고, 그 기록을 남깁니다 ────────
 * 처음엔 *"부수는 데 대가를 붙이자"* 로 설계했습니다. 깨면 소리가 나고
 * 근처 적이 깨어나게 해서, 항아리를 **거래**로 만드는 안이었습니다.
 * 규칙으로는 말이 되는데 **재미의 방향이 반대**였습니다:
 *
 *   대가를 붙이면 플레이어는 항아리 앞에서 **망설입니다.**
 *   그런데 이 물건이 존재하는 이유는 *"시원하게 부수고 다니는 것"* 이고,
 *   부수는 김에 뭔가 나오면 *"역시 내 감이 맞았다"* 가 되는 것입니다.
 *   망설이게 만들면 그 감각이 통째로 사라집니다.
 *
 * *"인터랙션 세금"* 을 피하려다 **재미 쪽에 세금을 매긴** 셈이었습니다.
 *
 * ── 그래서 기준을 바꿉니다: **플레이어가 똑똑해진 기분** ──────────
 * 검은 신화 오공·젤다가 탐험으로 파는 것은 보상 자체가 아니라
 * *"저 폭포 뒤에 뭔가 있을 것 같은데"* → **정말 있었다** 의 감각입니다.
 * 게이머가 이미 갖고 있는 **기대**를 확인해 주는 것이고, 그때 남는 말은
 * "운 좋았다"가 아니라 **"역시 나는 게임을 안다"** 입니다.
 *
 * 그러니 항아리의 값어치는 **막는 것**(대가·확률)이 아니라
 * **확인해 주는 것**에 있습니다:
 *   · 부수는 것은 **공짜**입니다. 망설일 이유를 안 만듭니다.
 *   · 대신 **어디를 부술지**가 감입니다 — 구석, 막다른 길, 유독 정돈된
 *     한 무더기. 감이 맞으면 나오고, 그게 전부입니다.
 *   · 그래서 진짜를 정하는 것은 **확률이 아니라 배치**입니다(`Urn.holds`).
 *     굴리면 *"전부 부수는 것이 최적"* 이 되어 감이 낄 자리가 없습니다.
 */
export const URN = {
  /** 몸 굵기·키 — 통보다 작습니다. 잡동사니는 잡동사니처럼 보여야 합니다. */
  radius: 0.42,
  height: 0.8,
  /**
   * 부서질 때의 **화면 흔들림**. 폭발통(0.7)보다 훨씬 작습니다 —
   * 항아리가 통만큼 요란하면 플레이어는 둘을 같은 물건으로 배웁니다.
   *
   * ⚠️ **여기에 `hitstop` 이 있었는데 아무도 안 읽고 있었습니다.**
   *    항아리를 칠 때의 히트스톱은 **무기 쪽에서** 이미 나옵니다
   *    (`applyHit` 이 `landed = true` 를 돌려주면 부르는 쪽이 무기의
   *    값을 씁니다). 즉 이 칸은 *"항아리는 0.03초 멈춘다"* 라고
   *    적어 두고 게임은 다른 값으로 멈추고 있었습니다 — 「규칙은 한
   *    곳에만」이 깨진 자리라 **지웠습니다.**
   *    금 간 벽에도 같은 이유로 `hitstop` 을 안 만들었습니다.
   */
  trauma: 0.18,
} as const

/**
 * 🧱 **금 간 벽** — 「폭포 뒤에 뭔가 있을 것 같다」를 게임이 인정해 주는 자리.
 *
 * ── 무엇을 노리는가 ──────────────────────────────────────────────
 * 오공·젤다·엘든 링이 공통으로 쓰는 장치입니다. 값은 방 안의 보물이
 * 아니라 **알아본 것 자체**입니다: *"역시 나는 게임을 안다."*
 * 그래서 설계의 무게가 **보상이 아니라 단서**에 실려 있습니다.
 *
 * ── 왜 한 대면 부서지는가 (산나비의 규칙) ─────────────────────────
 * 폭탄이나 특정 무기를 요구하면, 이 물건이 묻는 질문이 *"저기 뭔가
 * 있나?"* 에서 **"열쇠가 어디 있지?"** 로 바뀝니다. 그건 알아보는
 * 즐거움이 아니라 심부름입니다.
 *
 * 산나비의 웹스윙이 **줄 길이를 자동으로 맞춰 주고** 플레이어에게는
 * *"쏘고 놓는 타이밍"* 만 남기듯이, 여기서도 **어려운 부분은 기계가
 * 가져갑니다.** 플레이어 몫으로 남는 것은 하나뿐입니다 — **알아보는 것.**
 * 알아본 사람은 반드시 열립니다. 못 알아본 사람만 지나칩니다.
 *
 * ── 그래서 값이 거의 없습니다 ────────────────────────────────────
 * 체력도 도화선도 없습니다. 여기 있는 것은 **부서질 때의 손맛**뿐이고,
 * 그 크기의 근거는 아래 주석에 있습니다. 「부수면 열린다」는 규칙 자체는
 * 숫자가 아니라 지형에 있습니다(terrain.ts `Shortcut.kind`).
 */
export const CRACKED_WALL = {
  /**
   * 벽의 폭·두께·높이(m). 한 칸이 2m 이므로 **칸을 꽉 채웁니다** —
   * 옆이 비면 "부술 필요가 없는데?"가 되고, 그러면 단서가 아니라
   * 장식이 됩니다.
   */
  width: 2,
  thickness: 0.5,
  /**
   * 키 1.9m — **플레이어(1.75m)보다 큽니다.** 넘겨다볼 수 없어야
   * 안이 안 보이고, 안이 안 보여야 *"뭐가 있지"* 가 성립합니다.
   */
  height: 1.9,
  /**
   * 부술 때의 **화면 흔들림**. **항아리보다 크고 폭발통보다 작습니다**
   * (0.18 → **0.34** → 0.7). 순서에 뜻이 있습니다: 잡동사니 <
   * **길이 열리는 일** < 폭발. 항아리와 같은 값을 주면 *"방금 뭔가
   * 큰 일이 일어났다"* 가 몸으로 안 옵니다.
   *
   * ⚠️ 히트스톱은 **여기 없습니다** — 위 `URN` 주석의 이유와 같습니다.
   */
  trauma: 0.34,
} as const

export const BARREL = {
  /** 몸 굵기·키 — 판정에 들어가려면 `Body` 가 필요합니다. */
  radius: 0.5,
  height: 1.15,
  /**
   * 터지는 반경(m). 🟡 광역들과 같은 자릿수로 둡니다 — 색이 같으면
   * 크기도 비슷해야 *"노랑은 넓다"* 가 배운 대로 맞습니다.
   */
  blast: 4,
  /**
   * ── 🔥 **불이 옮겨 붙는 거리** — 피해 반경과 **다른 숫자**입니다 ──────
   *
   * ── 왜 나눴는가 (한 칸이 두 일을 하고 있었습니다) ─────────────────
   * 예전에는 `blast` 하나가 둘을 다 맡았습니다 — *"이만큼 아프다"* 와
   * *"이만큼까지 불이 번진다"*. 이 저장소가 이번 세션에 가장 여러 번
   * 데인 모양 그대로입니다: **처방이 다른 둘이 한 칸에 담기면 정확히
   * 거꾸로 읽힙니다.** 불을 넓히려면 폭발이 세지고, 폭발을 줄이려면
   * 불이 안 번집니다. 둘 중 어느 쪽도 원한 적이 없습니다.
   *
   * ── 무엇이 막혀 있었는가 ─────────────────────────────────────────
   * 「연쇄로만 터뜨릴 수 있는 통」을 놓으려다 못 놓았습니다. 그런 자리가
   * 있으려면 통 A 가 **모든 걸어갈 수 있는 칸에서 무기가 안 닿는 거리**에
   * 있으면서 통 B 의 불은 닿아야 합니다. 재 봤습니다(`weaponTable()`):
   *
   *     불이 옮겨 붙는 최대 거리   4 + 몸 굵기 0.5 = **4.5m**
   *     근접 무기 최대 도달       **5.7m** (대검 2타 — 사거리 3.5 + 파고들기 2.2)
   *
   * 불보다 칼이 **더 멀었습니다.** 그러면 「불로만 닿는 자리」는 존재하지
   * 않습니다. 지형을 아무리 잘 파도 안 됩니다 — 숫자가 막고 있었습니다.
   * (이걸 모르고 낭떠러지 폭을 이리저리 재 보다가, 창이 0.3m 밖에
   *  안 열린다는 걸 알고서야 자를 의심했습니다.)
   *
   * ── 왜 8m 인가 ───────────────────────────────────────────────────
   * · **5.7m 보다 커야** 「불로만 닿는 자리」가 생깁니다. 창이 5.7~8.5m 로
   *   **2.8m** 열리고, 격자가 2m 라 **칸 하나가 온전히** 들어갑니다.
   *   6.9m 같은 값으로 겨우 맞추면 「한 칸 차이의 초록은 운이다」입니다.
   * · 피해 반경의 **정확히 두 배**라 외우기 쉽고, 젤다·오공이 쓰는 감각
   *   그대로입니다 — **불은 폭발보다 멀리 번집니다.** 폭발은 순간이고
   *   불은 옮겨 다니는 것이니까요.
   *
   * ── ⚠️ **이 값으로도 「연쇄로만 열리는 벽」은 못 만듭니다** ─────────
   * 처음에 이 주석에 *"이제 벽 퍼즐을 만들 수 있다"* 고 적었다가 지웠습니다.
   * 종이에 그려 보니 **구조적으로 불가능**합니다:
   *
   *     벽을 여는 통은 벽에서 `blast`(4m) 안에 있어야 합니다.
   *     플레이어는 벽 바로 앞(약 1m)까지 걸어갈 수 있습니다 — 그게 문입니다.
   *     ⇒ 플레이어에서 그 통까지는 **아무리 멀어도 5m**
   *     그런데 대검은 **5.7m** 를 칩니다.
   *
   * 즉 **벽을 열 수 있는 통은 언제나 칼도 닿습니다.** 막는 것은 연쇄
   * 반경이 아니라 **`blast` 와 「문 앞까지 걸어갈 수 있다」** 입니다.
   * 벽 퍼즐을 강제하려면 그 둘 중 하나를 건드려야 하고, 둘 다 전투가
   * 쓰는 값이라 **퍼즐이 전투의 숫자를 정하게 됩니다.** 안 합니다.
   *
   * 그래서 이 값이 여는 것은 벽이 아니라 **「손이 안 닿는 자리」**입니다 —
   * 낭떠러지 건너 단상 위의 통처럼, 벽과 무관하게 5.7~8.5m 에 놓인 통.
   * 거기에 무엇을 놓을지(예: 쏘는 자가 서 있는 단상)는 지도의 몫이고,
   * 그 배치는 아직 안 했습니다. **숫자만 먼저 열어 둡니다.**
   *
   * ── ⚠️ 지금 이 존의 전투는 **하나도 안 바뀝니다** (재 봤습니다) ────
   * 이 지도의 통 넷은 서로 **12m 넘게** 떨어져 있습니다. 4m → 8m 로
   * 넓혀도 저절로 이어지는 통은 **한 쌍도 없습니다.** 즉 이 변경은
   * 지금 존에서는 *"새 배치를 놓을 수 있게 됐다"* 는 뜻일 뿐이고,
   * 기존 전투의 세기는 그대로입니다. 통을 8m 안에 두 개 놓는 날부터
   * 연쇄가 전투에도 등장합니다 — 그건 그때의 결정입니다.
   *
   * ⚠️ **원거리 룬(사거리 8.5m)은 여전히 통 A 를 직접 맞힙니다.**
   *    막지 않습니다. 젤다의 폭탄 벽도 화살로 열리는 것이 있고, 자기
   *    빌드로 지름길을 찾아낸 사람에게 *"그건 반칙"* 이라고 말하는 것은
   *    이 게임이 지키려는 *"역시 나는 게임을 안다"* 와 정반대입니다.
   *    기본 무기 셋으로는 **연쇄가 유일한 답**이고, 그거면 충분합니다.
   */
  chain: 8,
  /** 화면 흔들림·히트스톱 — 폭발이 타격보다 작게 느껴지면 안 됩니다. */
  hitstop: 0.09,
  trauma: 0.7,
  /** 휘말린 플레이어가 밀려나는 세기(m/s). */
  knockback: 6.5,
} as const

/**
 * 💥 **도화선 길이(초)** — 지어낸 값이 아니라 **답에 걸리는 시간**입니다.
 *
 * 답이 하나뿐이므로(걸어서 나간다) 선택 반응이 아니라 **단순 반응**을
 * 씁니다. 거기에 **반경 전체를 걸어 나가는 시간**을 더합니다 — 한가운데
 * 서 있던 사람도 답할 수 있어야 하니까요.
 *
 * ⚠️ 이 식은 `react` 프로브가 🟡 에 요구하는 것과 **같은 모양**입니다
 *    (*"반응하고도 걸어서 빠져나올 시간이 남는다"*). 통만 따로 후한
 *    값을 쓰면, 같은 노랑인데 통 앞에서만 규칙이 달라집니다.
 */
export function barrelFuse(): number {
  return reactionTime(1) + BARREL.blast / PLAYER.moveSpeed
}

/** 💥 폭발에 휘말린 플레이어가 잃는 스태미나 — 구르기 2회분. */
export function barrelStaminaLoss(): number {
  return PLAYER.dodge.staminaCost * 2
}

/**
 * 👁 **적이 나를 알아채는 방식** — 거리만이 아니라 **방향**도 봅니다.
 *
 * ── 왜 필요해졌는가 ────────────────────────────────────────────────
 * 기둥 3은 *"포지셔닝이 보상받는다"* 인데, 지금까지 손본 것은 전부 **싸움이
 * 시작된 뒤**의 포지셔닝이었습니다(등 뒤 배수·강인도 배수·봇의 돌기).
 * 소울류·세키로가 포지셔닝을 가르치는 **첫 자리**는 **접근**입니다 —
 * 못 본 적의 등에 먼저 꽂는 것. 우리에겐 그 순간이 아예 없었습니다:
 * 적은 **방향과 무관하게 거리만으로** 깨어났고, `combat.ts` 는 상대가
 * 나를 봤는지 **한 번도 묻지 않았습니다.**
 *
 * ── 시야와 청각을 나눕니다 ────────────────────────────────────────
 * 그냥 시야만 좁히면 `npm run bypass` 가 이미 겪은 *"싸우지 않고 존을
 * 종주"* 가 커집니다. 그래서 둘로 나눕니다:
 *   · 앞쪽 부채꼴 안이면 원래 거리에서 **봅니다.**
 *   · 그 밖이면 아주 가까이서만 **듣습니다.**
 */
export const AWARE = {
  /** 이 부채꼴 안이면 "보인다" — 사람 시야(약 120°)보다 조금 넉넉하게. */
  frontArcDeg: 150,
  /**
   * 🔊 **등 뒤에서 알아채는 거리는 내가 얼마나 시끄러운가로 정해집니다.**
   *
   * ── 왜 고정값이면 안 되는가 ────────────────────────────────────────
   * 처음엔 시야의 비율(0.5)로 뒀다가 프로브가 잡았습니다. 존의 시야는
   * 14m 이므로 듣는 거리가 7m 가 되는데, **때리는 거리는 2.3m** 입니다.
   * 7m 에서 이미 깨어나니 못 본 적을 칠 방법이 **원리적으로 없습니다.**
   *
   * 그래서 1.8m 고정으로 낮췄더니 이번엔 반대쪽이 뚫렸습니다: **등만
   * 보이면 아무리 뛰어도 안 들킵니다.** 그건 `npm run bypass` 가 이미
   * 재고 있는 *"싸우지 않고 존을 종주"* 를 그대로 키우는 값입니다.
   *
   * ── 그래서 **속도로 잇습니다** ────────────────────────────────────
   * 메탈기어·고스트 오브 쓰시마·세키로가 전부 쓰는 규칙이고, 이 게임에
   * 이미 있는 것(Shift 질주)에 뜻을 하나 더 얹습니다:
   *
   *   · 서 있거나 살금살금 → `hearQuiet` 안에서만 들린다
   *   · 전력 질주          → `hearLoud` 밖까지 들린다
   *
   * 그래서 **접근이 선택이 됩니다.** 빨리 붙으면 정면승부, 걸어서 붙으면
   * 기습. 기둥 3(포지셔닝이 보상받는다)이 싸움 **시작 전**으로 넓어지는
   * 자리가 여기입니다 — 지금까지 우리 포지셔닝은 전부 싸움이 붙은
   * 뒤에만 있었습니다.
   *
   * ⚠️ `hearQuiet` 은 **가장 짧은 근접 사거리보다 안쪽**이어야 합니다.
   *    이게 사거리보다 크면 걸어가도 때리기 전에 깨어나서, 기습은 다시
   *    코드에만 있고 게임에는 없는 기능이 됩니다.
   * ⚠️ `hearLoud` 는 **시야(존 14m)보다 작게** 둡니다. 이보다 크면 등이
   *    앞보다 예민해지는 셈이라 방향을 나눈 의미가 사라집니다.
   */
  hearQuiet: 1.8,
  hearLoud: 9,
  /**
   * 깨어난 뒤에도 **이만큼은** 기습으로 쳐 줍니다(초).
   *
   * ⚠️ 이게 없으면 기습은 한 번도 성립하지 않습니다. 무기가 휘두르며
   *    앞으로 나가기 때문에(롱소드 1타 돌진 1.5m), 예고 도중에 청각
   *    거리 안으로 들어가 **판정 전에 적이 깨어납니다.** 내 칼이 내
   *    기습을 깨우는 셈입니다.
   *
   * 0.45초는 가장 긴 근접 예고(강타 0.42초)보다 조금 깁니다 — 휘두르기
   * 시작할 때 못 보고 있었으면 그 한 대는 기습이라는 뜻입니다.
   */
  ambushGrace: 0.45,
  /**
   * 📣 방금 깨어난 적의 **고함이 닿는 거리**(m).
   *
   * 방향으로 인지를 나누자 무리가 흩어졌습니다 — 근거와 설계는
   * `enemyAI` 의 고함 문단에 적어 두었습니다. 여기서 정할 것은 크기뿐입니다.
   *
   * ⚠️ 위아래 양쪽에 벽이 있습니다:
   *   · 너무 작으면 무리가 1대1의 줄이 됩니다(`npm run encounter`).
   *   · 너무 크면 한 번 들킬 때 존이 통째로 일어나서, 걸어서 조용히
   *     가는 선택이 사라집니다(`npm run bypass`).
   * 두 프로브가 이 값을 양쪽에서 붙잡고 있습니다.
   */
  alertRadius: 7,
  /**
   * ── 👀 여기부터는 **보여 주기** 값입니다 ──────────────────────────
   *
   * 지난 라운드에 인지 규칙을 셋이나 실어 놓고, **화면에는 한 글자도
   * 올리지 않았습니다.** `src/render` 와 `src/ui` 를 통틀어 `aggro` 라는
   * 낱말이 **한 번도** 안 나옵니다. 즉 플레이어가 알 수 있는 것이
   * 하나도 없습니다 —
   *   · 저 적이 나를 봤는지 못 봤는지
   *   · 방금 왜 갑자기 다 달려드는지
   *   · 뛰는 것이 걷는 것보다 시끄럽다는 사실 자체
   *
   * 이 저장소가 이미 한 번 적어 둔 문장이 그대로 다시 맞습니다:
   * *"게임이 자기 규칙을 한 번도 설명하지 않고 있었습니다."* 규칙은
   * 플레이어가 **읽을 수 있을 때만** 규칙이고, 그 전까지는 그냥 운입니다.
   *
   * ⚠️ **새 색을 만들지 않습니다.** 이 게임에서 색은 *"어떻게 답하라"* 는
   *    뜻이고(4색 표), 다섯 번째 색을 들이면 그 표가 무너집니다.
   *    인지 신호는 전부 **무채색 + 모양**으로만 말합니다.
   */
  /** 못 본 적 표시를 켜 두는 거리(m) — 이 밖은 화면만 지저분해집니다. */
  markRange: 12,
  /**
   * 들킨 순간에 터지는 파문의 크기(m)와 시간(초).
   *
   * 세키로의 `!`, 쓰시마의 경계 아이콘이 하는 일입니다 — **원인과 결과를
   * 붙여 주는 것.** 이게 없으면 무리가 우르르 오는 것이 "버그처럼"
   * 보입니다. 파문은 고함 거리(`alertRadius`)와 **같은 크기**로 그립니다:
   * 눈에 보이는 것이 곧 규칙의 크기여야, 보고 배운 것이 맞는 것이 됩니다.
   */
  spotFlash: 0.45,
  /**
   * 발소리 파문 — 플레이어 발밑에 지금의 청각 거리를 그립니다.
   *
   * 메탈기어의 소리 원, 쓰시마의 감지 링이 하는 일입니다. 이 게임의
   * 새 선택(걷기 ↔ 질주)은 **눈에 보이지 않으면 존재하지 않습니다** —
   * 아무도 "뛰면 더 멀리 들린다"를 스스로 알아내지 못합니다.
   *
   * ⚠️ 항상 그리지는 않습니다. 근처에 **아직 나를 못 본 적이 있을 때만**
   *    켭니다. 싸움이 붙은 뒤에도 계속 깔리면 그건 정보가 아니라 방해고,
   *    이 게임이 예고 부채꼴에 쓰는 바닥을 잡아먹습니다.
   */
  noiseRingRange: 16,
} as const

/**
 * 지금 이 속도면 등 뒤 몇 m 에서 들리는가.
 *
 * ⚠️ **규칙은 여기 한 곳에만 둡니다.** `enemyAI` 도 프로브도 이 함수를
 *    부릅니다 — 프로브가 같은 식을 베껴 쓰면, 값을 고쳤을 때 게임은
 *    바뀌고 검사는 옛 규칙을 통과시켜 버립니다(이 저장소에서 가장
 *    비쌌던 버그가 전부 그 모양이었습니다).
 */
export function hearDistance(speed: number): number {
  // 기준은 **이 게임이 낼 수 있는 가장 빠른 속도** — 질주 최고속.
  const top = PLAYER.moveSpeed * PLAYER.sprint.speedScale
  const t = top > 0 ? Math.min(1, Math.max(0, speed / top)) : 0
  return AWARE.hearQuiet + (AWARE.hearLoud - AWARE.hearQuiet) * t
}

/** 잡몹 — 소울라이크식으로 "읽고 피할 수 있는" 적 */
export const GRUNT = {
  maxHp: 58,
  radius: 0.45,
  height: 1.7,
  moveSpeed: 3.0,
  /**
   * 몸을 돌리는 속도(도/초).
   *
   * 420이었을 때의 문제: 등 뒤로 돌아가도 **0.4초면 다시 정면을 봐서** 백어택이
   * 사실상 불가능했습니다(플레이 테스트 피드백). 이제 180° 도는 데 1.2초 걸립니다.
   * 느린 게 아니라, 등 뒤가 **약점으로 성립하려면** 돌아서는 데 시간이 들어야 합니다.
   */
  turnSpeedDeg: 150,
  /**
   * 등 뒤를 잡혔을 때, 알아채고 돌아서기 시작하기까지의 지연(초).
   *
   * 회전 속도만 낮추면 "느리게 계속 따라 도는" 적이 됩니다. 지연을 넣어야
   * **확실한 반격 창**이 생깁니다. 이 값 + 회전 시간이 곧 백어택 기회의 길이입니다.
   */
  backReactionDelay: 0.5,

  /**
   * 이 거리 안에 들어오면 추격 시작.
   *
   * 아레나 모드에서는 반드시 아레나 전체를 덮어야 합니다.
   * (자동 검증에서 잡은 실제 버그: 15로 두면 적이 반지름 21 지점에 스폰되는데
   *  어그로 범위 밖이라 영원히 제자리에 서 있었습니다. 웨이브가 진행되지 않습니다.)
   * 나중에 던전 콘텐츠를 만들 때는 방 단위로 작은 값을 쓰게 됩니다.
   */
  aggroRange: 55,
  /** 이 거리 안이면 공격 시도 */
  attackRange: 2.1,
  /** 공격 후 다음 공격까지 */
  attackCooldown: 1.1,

  /**
   * 텔레그래프(예고 동작).
   * 0.5초 이상 줘야 사람이 보고 반응해서 구를 수 있습니다.
   * 이 수치를 0.2로 낮추면 게임이 즉시 "불공정하다"고 느껴집니다.
   */
  windup: 0.55,
  active: 0.12,
  recovery: 0.7,

  damage: 14,
  attackArcDeg: 100,
  attackReach: 2.5,
  knockback: 2.6,

  /** 피격 경직 */
  hurtStagger: 0.22,
  /** 서로 밀어내는 힘 — 적들이 한 점에 겹쳐 쌓이는 것을 막습니다. */
  separationForce: 7.5,
} as const

/**
 * 보스 — 잡몹과 같은 상태 기계를 쓰되 수치만 다릅니다.
 *
 * 설계 근거: 보스를 "체력 많은 잡몹"으로 만들면 지루해집니다. 그래서
 * 체력보다 **예고 시간과 후딜을 더 길게** 잡았습니다. 느리고 무거운 대신
 * 한 대가 치명적이라, 플레이어가 "읽고 → 피하고 → 반격하는" 리듬을
 * 확실히 연습하게 됩니다. (기둥 2·3의 훈련장 역할)
 */
/**
 * 🎯 **예고 동안 적이 따라 돌 수 있는 각도(도).** — `enemyAI` 가 씁니다.
 *
 * ── ❌ 원래는 "평소 선회의 30%" 였습니다 ─────────────────────────────
 * `enemyAI.ts` 에 `cfg.turnSpeedDeg * 0.3` 이라고 박혀 있었습니다. 초당
 * 각도로 주면 **예고가 긴 적이 손해**를 봅니다 — 오래 준비할수록 그동안
 * 플레이어가 더 많이 돌아 들어가니까요. 실제로 그렇게 나왔습니다
 * (`npm run track`, 반경 2.04m, 예고 전체):
 *
 *     보스(예고 0.78초 · 100°/s × 0.3 = 30°/s) → **100°/s 까지만** 따라잡음
 *     잡몹(예고 0.35초 · 150°/s × 0.3 = 45°/s) → 150°/s 도 따라잡음
 *
 * 그런데 사람이 반경 2m 에서 **옆으로 걷기만 해도 152°/s** 입니다
 * (이동속도 5.4 ÷ 2.04). 즉 **보스는 걷는 사람을 못 맞힙니다.** 자동
 * 플레이의 보스전이 *"22.4초 · 받은 피해 38"* 이던 이유가 이것입니다.
 *
 * ── ✅ 그래서 **초당 각도가 아니라 예고당 각도**로 줍니다 ────────────
 * `ω = 이 각도 ÷ 이번 예고 길이` (평소 선회 속도를 넘지는 않습니다).
 * 그러면 예고가 길다고 공짜로 돌아 들어가지지 않습니다 — 소울류·오공의
 * 큰 보스가 긴 예고 동안 **끈질기게 따라 도는** 것과 같은 규칙입니다.
 *
 * ── 45°를 고른 근거 (짐작이 아니라 계산) ─────────────────────────────
 * 판정에서 벗어나려면 `(사람 각속도 − 적 각속도) × 예고 > 허용 반각`.
 * 보스의 허용 반각은 실측 77°(부채꼴 130° 절반 + 굵기 보정)이므로,
 * 걷는 사람(152°/s)을 잡으려면 `152 − 77/0.78 = 53°/s` 가 필요하고,
 * 0.78초 동안 53°/s 는 **41°** 입니다. 45°는 거기에 약간의 여유입니다.
 *
 * ⚠️ **백어택(기둥 3)을 죽이지 않는지 반드시 같이 봅니다.** 구르기는
 *    0.24초에 4m 라 반경 2m 에서 **470°/s** — 이 규칙으로는 절대 못
 *    따라잡습니다. 즉 *"걸어서 도는 것"* 은 잡고 *"굴러서 도는 것"* 은
 *    놓칩니다. 그게 이 값이 지키려는 경계선입니다.
 */
/**
 * 🌀 **마무리 타를 맞히면 그 타의 기력이 일부 돌아옵니다** (0~1, 비율).
 *
 * ── 왜 (벤치가 병목을 가리켰습니다) ────────────────────────────────
 * 롱소드의 마무리를 표준 반격 창(0.70초) 안으로 밀어 넣었더니 그 창의
 * 1등이 됐는데, **총 초당 피해는 제자리**였습니다. 달성률(이론상 타격 수
 * 대비)이 **70% → 60%** 로 떨어졌기 때문입니다:
 *
 *     한 바퀴 38 기력 ÷ 1.24초 = 초당 30.6 소비 vs 회복 34/초(지연 0.55초)
 *
 * 즉 **병목이 시간이 아니라 기력**이었습니다. 시간을 줄이면 기력을 더 빨리
 * 태울 뿐입니다.
 *
 * ── 왜 비용을 그냥 낮추지 않는가 ───────────────────────────────────
 * 롱소드의 기력합을 38 → 30 으로 내리면 효율이 1.39 → 1.77 이 되어
 * **쌍단검(1.68)의 축을 빼앗습니다.** 이 게임은 무기마다 이기는 축이
 * 하나씩이라야 하고(폭발 롱소드 · 효율 쌍단검 · 강인도 대검), 평평하게
 * 만드는 것이 목적이 아닙니다.
 *
 * ── 참고: 니오의 기(氣) 펄스 ───────────────────────────────────────
 * 니오는 지속력 문제를 **숫자를 낮춰서** 풀지 않습니다. *"잘 맞추면 기가
 * 돌아온다"* 로 풉니다 — 지속력이 **조건**이 되고, 그래서 배울 것이
 * 생깁니다. 여기서는 새 입력을 만들지 않고 **이미 있는 조건**을 씁니다:
 * **콤보의 마지막 타를 실제로 맞히는 것.**
 *
 * ⚠️ **빗나가면 안 줍니다.** 허공에 휘두른 마무리는 아무것도 안 갚습니다 —
 *    그래야 "이어 붙이면 이득"이 아니라 "닿게 이어 붙이면 이득"이 됩니다.
 * ⚠️ 한 번의 휘두름에 **한 번만** 갚습니다(여럿을 때려도 한 번).
 *
 * ── 왜 이 규칙이 롱소드에게 특히 이로운가 (예외를 안 만든 이유) ────
 * 규칙은 세 무기에 똑같이 적용됩니다. 그런데 표준 반격 창(0.70초) 안에서
 * **마무리까지 낼 수 있는 무기는 롱소드뿐**입니다(0.65초 · 대검 0.90초 ·
 * 쌍단검 4타 0.90초). 그러니 무기별 예외를 하나도 안 만들고도, 갚음은
 * 이 무기가 방금 얻은 자리로 저절로 흘러갑니다. **예외 대신 조건.**
 *
 * 0.5: 마무리 비용의 절반. 전부 갚으면 마무리가 공짜가 되어 *"무조건 3타"*
 * 가 정답이 되고, 그러면 판단이 사라집니다.
 */
export const COMBO_FINISH_REFUND = 0.5

export const WINDUP_TURN_BUDGET_DEG = 90

export const BOSS = {
  /**
   * **420 → 620.** 페이즈 시간을 재고 나서 올렸습니다.
   *
   * 자동 플레이로 보스전을 페이즈별로 갈라 보니 이랬습니다:
   *
   *     보스전 32.7초 — 1단계 **26.5초** · 2단계 4.3초 · 3단계 **1.9초**
   *
   * 배너 셋, 연계 세 벌, 가중치 세 벌을 만들어 놓고 뒤의 둘이 **6초**입니다.
   * 3단계는 두 번 숨 쉴 시간이라 연계(🔵→🟡, 🟣→🔴)가 나올 수가 없습니다.
   * 실제로 **연계는 한 번도 관측되지 않았습니다.**
   *
   * 경계를 옮겨(65/30 → 75/40) 뒤쪽에 체력을 더 줬지만 부족했습니다.
   * 이유는 플레이어의 화력이 전투 중에 **올라가기** 때문입니다 —
   * 집중이 차고, 무너뜨리고, 처형이 나갑니다. 같은 체력 구간이라도
   * 뒤로 갈수록 빨리 녹습니다.
   *
   * 그래서 총량을 올립니다. 32초는 3페이즈 보스로 짧기도 합니다 —
   * 소울류의 지역 보스는 보통 1~3분입니다. 620이면 구간이
   * 155 / 217 / 248 로 나뉘어, 뒤로 갈수록 길어집니다.
   *
   * ⚠️ 체력을 올리는 것은 **무딘 수단**입니다. 여기서 고르는 이유는
   * 페이즈의 길이 자체가 문제이기 때문입니다 — 공격을 세게 하거나
   * 예고를 줄이면 "읽고 피한다"는 이 게임의 규칙이 깨집니다.
   */
  maxHp: 620,
  radius: 0.95,
  height: 2.9,
  moveSpeed: 2.4,
  /** 보스는 더 크고 굼뜹니다 — 180° 도는 데 1.8초. */
  turnSpeedDeg: 100,
  backReactionDelay: 0.7,
  aggroRange: 55,
  attackRange: 3.4,
  /**
   * 1.5 → 1.05.
   *
   * 시간 예산을 재 보니 보스전 31초 중 **쿨다운이 8.7초(28%)** 였습니다.
   * 공격 동작(예고+휘두름+후딜)은 12.3초뿐이고, 나머지는 전부 "못 하고
   * 있던" 시간입니다. 5번 휘두르고 끝났습니다 — 6.2초에 한 번.
   *
   * 쿨다운은 **플레이어가 때릴 시간**이기도 해서 0으로 만들 수는 없습니다.
   * 다만 후딜(0.8~1.2초)이 이미 그 역할을 합니다. 후딜 뒤에 1.5초를 더
   * 쉬면 한 박자에 2.5초가 비고, 그 사이 플레이어는 3~4대를 넣습니다.
   * 그게 "보스가 620을 31초 만에 잃는" 이유이기도 합니다.
   *
   * 1.05로 줄이면 3단계(×0.55)에서 0.58초까지 내려가, 3단계가 실제로
   * "쉴 틈이 없다"가 됩니다 — 페이즈가 올라가는 값어치를 여기서 냅니다.
   */
  attackCooldown: 1.05,
  /** 잡몹보다 확실히 긴 예고 — 크고 느린 만큼 확실히 읽힙니다. */
  windup: 0.78,
  active: 0.16,
  recovery: 1.05,
  damage: 30,
  attackArcDeg: 130,
  attackReach: 4.2,
  knockback: 6.5,
  hurtStagger: 0.1,
} as const

/**
 * ── 성수병 (회복) ────────────────────────────────────────────────
 *
 * 지금까지 이 게임에는 **회복 수단이 아예 없었습니다.** 176m짜리 존을 도는
 * 동안 체력은 내려가기만 했고, 그래서 죽음은 "실력이 모자랐다"가 아니라
 * **"오래 걸었다"** 의 결과였습니다. 초반에 한 번 실수하면 남은 존 전체를
 * 반피로 걸어야 하는데, 그건 어려운 게 아니라 그냥 답답한 것입니다.
 *
 * ── 왜 물약을 쌓게 하지 않는가 ───────────────────────────────────
 * 개수를 쌓을 수 있게 하면 결국 **"물약을 99개 들고 가면 안 죽는다"** 가
 * 최적 전략이 되고, 그 순간 전투 설계 전체가 무의미해집니다.
 * 충전식(3회, 화톳불에서 보충)이면 "지금 쓸까 아낄까"가 매 순간 판단이 됩니다.
 *
 * ── 왜 즉시 회복이 아닌가 (이게 가장 중요합니다) ──────────────────
 * 즉발 회복은 **아무 대가가 없습니다.** 언제 눌러도 손해가 없으면 판단이
 * 아니라 습관이 됩니다. 그래서 마시는 데 0.85초가 걸리고, 그동안
 *   · 무적 프레임이 **없고**
 *   · 이동 속도가 35%로 떨어지고
 *   · **맞으면 병이 사라집니다** (회복도 없이)
 * 우리 설계의 "적도 커밋한다"와 정확히 같은 원리를 플레이어에게도 적용한
 * 것입니다. 회복은 안전한 행동이 아니라 **틈을 봐서 하는 도박**이어야 합니다.
 */
/**
 * 🛡 **저스트 가드** — 타이밍으로만 성립하는 막기.
 *
 * ── 이 문서가 「막기를 안 넣는다」고 적어 둔 것과의 관계 ──────────────
 * enemyAttacks.ts 에 이렇게 적혀 있습니다:
 *
 *   > *"막기를 넣으면 스태미나를 또 나눠 써야 하고, '막을까 구를까'가
 *   > 아니라 **'스태미나가 있나'** 가 판단의 중심이 됩니다."*
 *
 * 그 문단이 반대한 것은 **지속 가드**입니다 — 누르고 있으면 막히는 것.
 * 저스트 가드는 반대 계약입니다: **성공하면 값이 0이고, 실패하면 비쌉니다.**
 * 그러면 판단의 중심이 자원이 아니라 **타이밍**에 남습니다. 기둥 1 의
 * "스태미나 = 기본기·회피 전용"도 지켜집니다 — 성공한 가드는 기력을
 * 쓰지 않고, 오히려 조금 돌려줍니다.
 *
 * ── 참고한 게임들의 계약 ────────────────────────────────────────────
 * | 게임 | 창 | 성공 | 실패 |
 * |---|---|---|---|
 * | 세키로 튕기기 | 아주 짧음 | 무피해 + **체간** 피해 | 가드로 흘러 체간 손실 |
 * | Lies of P 퍼펙트 가드 | ~0.2초 | 무피해 + 적 경직 | 체력·내구 손실 |
 * | God Hand 저스트 가드 | 매우 짧음 | 무피해 + 반격 기회 | 그냥 맞음 |
 * | Wo Long 화해 | 넉넉함 | 기력 회복 + 붕괴 | 기력 손실 |
 *
 * 공통 문법 셋: **① 창이 짧다 ② 성공은 자원을 안 쓰고 오히려 번다
 * ③ 실패는 평소보다 나쁘다.** 셋을 다 옮깁니다. 하나라도 빼면
 * "일단 눌러 보는 버튼"이 됩니다.
 *
 * ── 완벽 회피와 **다른 것을 벌어야** 합니다 ─────────────────────────
 * 둘이 같은 보상을 주면 하나는 장식입니다. 세키로가 튕기기를 **체간**에,
 * 회피를 **위치**에 붙여 나눈 것을 그대로 가져옵니다:
 *
 * | | 완벽 회피(이미 있음) | **저스트 가드** |
 * |---|---|---|
 * | 자리 | **뜹니다** | **지킵니다** |
 * | 보상 | 다음 한 대 **확정 치명타** | 적 **강인도**를 깎음 |
 * | 여는 문 | 공격 | 무너뜨림 |
 *
 * ⚠️ **🔴 직격에만 통합니다.** 아무 색에나 통하면 "전부 가드로 푼다"가
 *    만능 정답이 되어 색 다섯이 통째로 무의미해집니다. 🟡 은 자리를 뜨는
 *    것이, 🔵 은 무적 프레임이, 🟣 은 거리가, 🟢 은 반격이 정답이고
 *    그 정답들은 **다른 답이 생기는 순간 값을 잃습니다.**
 *    (세키로에서 위험(危) 공격이 튕기기로 안 풀리는 것과 같은 자리입니다.)
 */
/**
 * 🍶 **회복을 노리는 적** — 적이 플레이어의 상태를 읽는 첫 자리.
 *
 * ── 무엇이 없었는가 ──────────────────────────────────────────────
 * `enemyAI.ts` 를 통틀어 **플레이어의 상태(ActorState)를 읽는 줄이 하나도
 * 없었습니다.** 적의 판단은 거리 · 가중치 · 토큰 · 페이즈뿐입니다. 즉
 * 플레이어가 무엇을 하든 적에게는 똑같습니다 — 공격 중이든, 구르든,
 * **성수병을 마시든**.
 *
 * 그런데 이 게임은 마시기에 **무적 프레임을 일부러 안 넣었습니다**
 * (components.ts `ActorState.Drink` 주석: *"무적 프레임이 없는 것이
 * 핵심입니다"*). 위험하라고 만들어 놓고, 정작 그 위험을 만들 주체가
 * 아무도 없었습니다. 설계 노트만 있고 **실행이 없는 상태**였습니다.
 *
 * ── 참고 게임은 전부 여기를 씁니다 ────────────────────────────────
 * 소울류에서 *"언제 마실까"* 가 가장 어려운 판단인 이유는 정확히 이것
 * 입니다 — 에스투스를 드는 순간 적이 붙습니다. 세키로·오공도 회복 동작을
 * 끊어 옵니다. 회복이 안전하면 체력은 자원이 아니라 그냥 시간이 됩니다.
 *
 * ── 값을 정한 근거 ────────────────────────────────────────────────
 * · **예고는 하나도 안 줄입니다.** 줄이면 기둥 2("읽으면 답이 있다")가
 *   깨집니다. 줄어드는 것은 *"아무 일도 안 일어나는 시간"*(쿨다운)뿐이고,
 *   이건 이미 `impatient`(노려보다 커밋)에서 쓴 것과 같은 논리입니다.
 * · **토큰은 그대로 지킵니다.** 안 그러면 다섯이 동시에 덮치는 자리가
 *   되어 다대일 규칙이 무너집니다.
 * · `rangeMult 1.25` — 사거리 바로 밖의 적까지만. 존 반대편에서 달려오는
 *   것은 "노렸다"가 아니라 그냥 순간이동처럼 느껴집니다.
 */
export const PUNISH_HEAL = {
  /** 이 배수까지의 거리에 있는 적이 노립니다(적의 attackRange 기준) */
  rangeMult: 1.25,
  /**
   * 남은 쿨다운을 이 값까지 **당깁니다**(초). 0 이 아닌 이유: 마시는
   * 순간 칼이 튀어나오면 예고를 볼 시간이 마시기 동작 안에 통째로
   * 들어가 버립니다. 0.2초는 손이 반응하는 시간이 아니라 **적이
   * 자세를 잡는 시간**입니다.
   */
  cutTo: 0.2,
} as const

/**
 * 🧭 **곁길 알림** — "저쪽에 뭔가 있다"를 길 위에서 알려 주는 규칙.
 *
 * ── 왜 생겼는가 ──────────────────────────────────────────────────
 * 길안내의 목표는 보스가 살아 있는 한 **항상 보스**입니다. 보물이 목표가
 * 되는 것은 보스를 잡은 뒤인데, 보스를 잡으면 존이 끝납니다. 즉 정련석이
 * 나오는 유일한 곳을 게임이 **한 번도 가리킨 적이 없습니다.**
 *
 * 게다가 주 동선은 지도 북쪽으로 안 가고(z=1 이 한계), 카메라는 22m 까지만
 * 담습니다. 그래서 「북쪽 단상」의 지름길 사다리와 보물 둘은 길 위에서
 * **알 방법이 아예 없었습니다.**
 *
 * 엘든 링은 화살표 없이 세계가 보여 줍니다(아이템 광채·유적 실루엣).
 * 우리 시점에서는 그게 안 통합니다 — 시점이 같은 로스트아크·NRFTW 는
 * 화면 표시로 풉니다. 대신 **데려다 주지는 않습니다**: 알려만 주고
 * 갈지 말지는 플레이어가 정합니다. 그게 곁길의 값어치입니다.
 */
/**
 * 🔥 **화톳불 사이 이동 키.**
 *
 * 리터럴로 흩어 두지 않는 이유는 `npm run guard` 의 키 충돌 검사가
 * config 에 선언된 키를 읽어 **상시 키와 맥락 키가 겹치는지** 보기
 * 때문입니다. 여기 두면 키를 바꾸는 날 검사가 저절로 따라옵니다.
 *
 * `H` 인 근거: 처음에 `Z` 로 뒀다가 `npm run guard` 가 잡았습니다 —
 * **`Z` 는 이미 저스트 가드**입니다(`GUARD.key`). 상시 키와 맥락 키가
 * 겹치면 한쪽이 먼저 소비해서 다른 쪽이 조용히 안 먹습니다. 이 저장소가
 * 그 사고로 세 판을 날린 적이 있고(사다리 V ↔ 가드 V), 그래서 그 검사가
 * 있습니다. 이번엔 **검사가 먼저 잡았습니다.**
 *
 * 남은 자리 중 `H`(home·hub)를 고릅니다. 이동은 전투 동작이 아니라
 * WASD 에서 멀어도 되고, 오히려 **전투 중에 잘못 눌리지 않는** 편이
 * 낫습니다.
 */
export const TRAVEL_KEY = 'KeyH'

export const NAV = {
  /**
   * **원래 길보다 이만큼(m) 더 걷는 것으로 끝나는** 미획득 보물만 알려 줍니다.
   *
   * ── ⚠️ 원래는 *"걸어서 이 거리 안"* 이었습니다 — 자를 바꿨습니다 ──────
   * 편도 거리로 재면 이 존의 「남쪽 함몰지」를 읽을 수가 없습니다. 거긴
   * **2단 낙하로 내려가면 못 올라오고, 대신 복귀 램프가 계단으로 이어지는**
   * 곳이라(생성기가 일부러 그렇게 만들었습니다), 들어갔다 나오는 길이
   * 원래 가던 길과 거의 같습니다. 같은 다섯 보물을 두 자로 재면:
   *
   *                   편도            더 걷는 거리
   *     (13, 47)      46m ❌          **20m** ✅   ← 함몰지, 가장 싼 축
   *     (17,-57)      32m ✅          **56m** ❌   ← 단상, 유일한 예산 밖
   *
   * 편도 자로는 **잘 만들어진 곁길을 안 알려 주고, 비싼 곁길을 권했습니다.**
   * 엘든 링·오공이 비밀을 배치하는 방식(가는 길에 얹기)을 읽으려면
   * *"저기까지 얼마"* 가 아니라 *"원래 길보다 얼마나 더"* 를 물어야 합니다.
   *
   *     더 걷는 거리 = (나→보물) + (보물→목표) − (나→목표)
   *
   * ⚠️ **화면에 적는 숫자는 그대로 편도입니다.** 「남동 32m — 보물」은
   *    *발이 실제로 걸을 거리*라야 안내가 됩니다. 바뀐 것은 **알려 줄지
   *    말지의 판단**이지 표시가 아닙니다 — 두 질문에 두 자를 씁니다.
   *
   * 근거: 곁길 예산과 **같은 값**입니다(tools/policy.mjs `DETOUR_BUDGET`).
   * 갈 수 없는 것을 알려 주는 것은 안내가 아니라 놀리는 것입니다.
   *
   * ⚠️ **이 주석이 거짓말을 하고 있었습니다.** "같은 값"이라고 적혀 있는데
   *    실제로는 45 대 40 이었습니다. 언제 갈라졌는지는 아무도 모릅니다 —
   *    **갈라진 것을 알려 주는 것이 아무것도 없었기 때문입니다.**
   *
   *    그 사이에 게임은 40~45m 짜리 보물을 *"저쪽에 있다"* 고 알려 주고
   *    있었고, 봇은 규칙상 그걸 영영 안 갔습니다. 즉 **사람에게는 권하고
   *    계측기는 안 가는 자리**가 5m 폭으로 열려 있었습니다. 벤치에서
   *    `(27,-43)` 이 딱 그 경계(40m · 32~48m)에 걸려 판마다 갔다 말았다
   *    한 것이 이 틈입니다.
   *
   *    값을 맞추는 것만으로는 또 갈라집니다. 그래서 `npm run guard` 에
   *    **두 값이 같은지 보는 검사**를 넣었습니다. 이 저장소가 지도와
   *    생성기를 그렇게 묶어 둔 것과 같은 방법입니다 — 언어가 달라
   *    (TS ↔ mjs) 한쪽에서 import 할 수 없을 때, 남는 수단은 **갈라지면
   *    빨개지게** 만드는 것뿐입니다.
   */
  sideHintRange: 40,
  /**
   * 이보다 가까우면 **안 알려 줍니다** — 빛기둥이 이미 화면에 있습니다
   * (카메라가 담는 거리 22m). 아는 것을 또 말하면 화면만 시끄러워집니다.
   */
  sideHintNear: 20,
} as const

export const GUARD = {
  /**
   * 누른 뒤 이만큼 안에 맞으면 저스트 가드입니다.
   *
   * 완벽 회피의 무적 창(0.06~0.30 = **0.24초**)보다 **짧게** 잡습니다.
   * 가드는 자리를 안 뜨고 보상도 더 크므로, 값은 정확도로 냅니다.
   * Lies of P 의 퍼펙트 가드가 대략 이 크기입니다.
   */
  window: 0.18,
  /**
   * 헛쳤을 때 굳어 있는 시간. 이게 없으면 **연타가 곧 상시 가드**가 되어
   * 위 `window` 가 아무 뜻이 없습니다 — 헛친 딜레이와 같은 원리입니다.
   */
  whiffLock: 0.35,
  /**
   * 헛쳤을 때 내는 기력. 실패가 평소보다 나빠야 "일단 눌러"가 안 됩니다.
   *
   * ⚠️ **구르기 값(18)과 정확히 같습니다.** 즉 가드를 한 번 헛치면
   *    구르기 한 번이 통째로 사라집니다. 그 결과를 벤치가 찍었습니다 —
   *    아래 `whiffKeepsDodge` 주석 참고.
   */
  whiffStamina: 18,
  /**
   * ── 🛡 **헛친 벌이 다음 방어까지 가져가지는 않습니다** ────────────────
   *
   * ── 숫자가 먼저 있었습니다 ────────────────────────────────────────
   * `손이 묶임 — stamina` 를 "누가 그 기력을 썼나"로 갈라 보니:
   *
   *     구르기 10 · **헛친가드 5** · 공격 1   (16대 중)
   *
   * 🟢 를 잘못 읽고 가드를 헛치면, 그 벌(18)이 **다음 구르기(18)를
   * 통째로** 가져갑니다. 그래서 다음 예고를 보고도 못 굴러 맞습니다.
   * 실패 한 번이 **두 번의 실패**가 되는 구조입니다.
   *
   * ── 참고 게임이 여기서 하는 것 ──────────────────────────────────
   * 세키로는 쳐내기에 실패해도 **막기로 격하**될 뿐 다음 방어 수단을
   * 뺏지 않습니다. 로스트아크의 반격 실패는 **쿨다운**을 먹지 기동력을
   * 먹지 않습니다. 공통 원리: **읽기에 실패한 벌이 다음 읽기를 막으면
   * 안 됩니다.** 배울 기회 자체가 사라지기 때문입니다.
   *
   * 이 저장소가 공격 쪽에서 이미 같은 결론을 냈습니다 —
   * playerControl `canAffordAttack`(공격은 구르기 한 번 분을 남긴다).
   * 그때 가드와 구르기를 **일부러 뺐는데**, 그건 *"낼 수 있느냐"* 의
   * 이야기였습니다. 이건 다릅니다 — **벌이 얼마나 깊이 파고드느냐**입니다.
   *
   * 참이면 헛친 벌은 구르기 한 번 분 **위에서만** 깎습니다. 여유가
   * 있으면 18을 다 냅니다(벌은 그대로), 없으면 남은 것까지만 냅니다.
   */
  whiffKeepsDodge: true,
  /**
   * 성공했을 때 되찾는 기력. **0 이 아니라 조금 주는** 이유: 잘 막은 사람이
   * 이어서 답할 수 있어야 합니다. 크게 주면 가드가 기력 회복기가 됩니다.
   */
  refund: 12,
  /**
   * 성공했을 때 적 강인도에 주는 피해. 무기의 `poiseScale` 을 그대로 탑니다 —
   * 대검으로 막으면 더 무너뜨립니다. 값을 무기와 무관하게 두면 "무엇을 들든
   * 가드는 똑같다"가 되어 무기 선택이 한 겹 얇아집니다.
   *
   * ── ⚠️ **이 값은 «강인도»가 아니라 «trauma» 입니다** ──────────────────
   *
   * 예전 값은 `14` 였고, 바로 위 한 줄이 *"적 강인도에 주는 피해"* 라고
   * 적어 둬서 **강인도 14를 준다**로 읽혔습니다. 그런데 이 값이 들어가는
   * 자리는 `poiseDamage(trauma, …)` 의 **첫 인자**입니다:
   *
   *     14 × POISE.fromTrauma(40) × 1 = **560**
   *
   *     견줄 것 — 평타 0.34 · 강타 0.62 (**같은 trauma 자리**)
   *     그릇   — 잡몹 30 · 정예 85 · 보스 105
   *
   * 게임이 계산해서 낸 실측(`npm run poise`): **보스 5.3배 · 잡몹 18.7배.**
   * 「막으면 무너뜨릴 수 있다」가 아니라 **「막으면 무엇이든 한 번에
   * 부순다」** 였습니다. 게다가 이 붕괴는 장부에 **«평타»** 로 적히고
   * 있어서(`breakPoise` 의 없어진 기본값) 어느 화면에도 안 나왔습니다 —
   * **두 결함이 서로를 가려 주고 있었습니다.**
   *
   * ── 새 값을 1.5(=60)로 고른 근거 ────────────────────────────────────
   *
   *     강타     0.62 × 40 × 2.2 = 54.6   (집중을 태워서 내는 한 방)
   *     예고중   0.34 × 40 × 2.5 = 34     (예고를 읽고 끼워 넣는 평타)
   *     **가드   1.5  × 40 × 1   = 60**   ← 셋 중 가장 셉니다
   *
   * 가장 세게 두는 이유: 저스트 가드는 이 게임에서 **가장 어려운 입력**
   * 입니다 — 창이 0.18초뿐이고(완벽 회피 0.24초보다 짧습니다), 빗나가면
   * 잠김 0.35초 + 기력 18을 뭅니다. 위험이 가장 큰 것이 보상도 가장
   * 커야 «잘하면 잘한 티가 나는» 게임이 됩니다.
   *
   * 그러면서도 **보스는 한 번에 안 무너집니다**(105 ÷ 60 → 두 번).
   * 즉 갚아 주는 것은 «한 번의 요행»이 아니라 **«두 번 연속 읽어 낸 것»**
   * 입니다. 잡몹(30)은 한 번에 무너지는데, 그건 강타(54.6)도 마찬가지라
   * 설계와 어긋나지 않습니다.
   *
   * 무너뜨릴수록 단단해지는 규칙이 뒤를 받칩니다 — 한 번 무너뜨린 보스는
   * ×1.45 가 붙어 60이 41.4로 줄고, 세 번째부터는 다섯 번 막아야 합니다
   * (`POISE.breakResistStep`). 값 하나로 상한을 만들 필요가 없습니다.
   *
   * ⚠️ **아직 «고친 값»이지 «잰 값»이 아닙니다.** 위 근거는 전부 산수이고,
   *    실제 판에서 가드가 몇 %를 차지하는지는 다시 재야 합니다.
   *    검사는 `npm run poise` 의 🛡 두 줄이 지킵니다(위·아래 양쪽).
   */
  poise: 1.5,
  /**
   * 🛡 **휘두르는 중에 창을 여는 값**(기력).
   *
   * ── 왜 생겼는가 (잰 숫자) ──────────────────────────────────────
   * 처음엔 서 있을 때(Idle)와 **후딜**에서만 열었습니다. 근거는
   * *"예고·판정 중에도 열리면 구르기 취소(기력 45)를 내야 하는 자리를
   * 가드가 공짜로 빠져나간다"* 였고, 그 걱정 자체는 옳았습니다.
   *
   * 그런데 자동 플레이로 재 보니 이랬습니다:
   *
   *     붙잡은 🔴 18회 · **창을 본 것 18회(100%)** · 그중 못 낸 자리 9회
   *
   * 창을 못 본 적은 **한 번도 없습니다.** 손실의 절반이 통째로
   * *"낼 자리가 없어서"* 였습니다. 읽었는데 답할 수 없는 것은
   * 기둥 2 가 가장 싫어하는 모양입니다 — *"내가 못 봤네"* 도
   * *"내가 못 피했네"* 도 아닌, **"손쓸 방법이 없었네"** 입니다.
   *
   * ── 그래서 구르기와 **같은 계약**으로 엽니다 ────────────────────
   * `dodge.cancelExtraCost` 주석이 이미 이 논리를 적어 두었습니다:
   *
   *   > 못 끊으면 그건 판단이 아니라 그냥 죽음이고, 공짜로 끊으면 판단이
   *   > 아니라 그냥 버튼입니다. 그래서 **낼 수는 있게, 대신 비싸게**.
   *
   * 세키로·Wo Long 은 아예 공짜로 열어 줍니다(튕기기가 전투의 기본
   * 문법이라서). 우리는 커밋을 더 무겁게 치는 게임이므로 값을 붙입니다.
   *
   * **20 인 근거:** 구르기 취소는 25+20=**45** 입니다. 가드가 더 싼 이유는
   * 몸을 빼 주지 않고(자리를 지킵니다), 빗나가면 **또** 18 을 내고 0.35초
   * 굳기 때문입니다. 즉 가드는 값의 절반을 **정확도로** 냅니다.
   * 서서 내는 가드는 그대로 공짜입니다 — 계약의 "성공은 공짜"는
   * 그 자리에서 지켜집니다.
   */
  commitCost: 20,
  /**
   * ⌨️ **이 동작의 키.** 여기 한 곳에만 적습니다.
   *
   * ── 왜 상수로 빼는가 (같은 실수를 두 번 했습니다) ──────────────────
   * 처음엔 `V` 였는데, `V` 는 *"이 자리에서 할 수 있는 일"* 로 묶인 **맥락
   * 키**(사다리 · 화톳불 강화)였습니다. 가드가 그 입력을 먼저 소비해서 봇이
   * 사다리에서 90프레임 갇혔고 기력이 0이 됐습니다. 그래서 `Z` 로 옮겼는데,
   * 이번엔 **프로브가 여전히 `V` 를 누르고 있었습니다** — 게임은 안 듣는데
   * 프로브는 계속 눌렀고, 그 프로브는 조용히 다른 것을 재게 됩니다.
   *
   * 키를 코드 세 곳(게임·프로브·봇)에 적어 두면 옮길 때마다 셋 다 고쳐야
   * 하고, 하나를 빠뜨려도 **아무도 알려 주지 않습니다.** 그래서 게임이
   * `guardInfo().key` 로 알려 주고 나머지는 읽기만 합니다 — 이번 세션에
   * 창 길이·사거리·정면 판정에 전부 같은 처방을 썼습니다.
   */
  key: 'KeyZ',
} as const

export const VIAL = {
  /** 시작 충전 수. 화톳불에서 전부 채워집니다. */
  charges: 3,
  /** 한 번에 회복하는 양. 최대 체력이 100이므로 3병 = 체력 1.35개분. */
  heal: 45,
  /** 병을 들어 올리는 시간 — 이 구간에 맞으면 병만 날립니다. */
  windup: 0.4,
  /** 회복이 들어간 뒤 자세를 되찾는 시간 */
  recovery: 0.45,
  /** 마시는 동안의 이동 속도 배율. 0이면 너무 가혹하고, 1이면 대가가 없습니다. */
  moveScale: 0.35,
} as const

/**
 * ── 보스 조우 (전용 영역) ────────────────────────────────────────
 *
 * ── 지금 무엇이 문제인가 ────────────────────────────────────────
 * 보스의 어그로 범위가 **55m** 입니다. 잡몹과 싸우는 도중에 저 멀리 있던
 * 보스가 걸어 들어올 수 있고, 실제로 존을 가로지르면 그렇게 됩니다.
 * 3페이즈짜리 보스전을 잡몹 넷과 섞어서 하게 되면, 페이즈도 연계도
 * 읽을 수가 없습니다.
 *
 * 그리고 **준비할 순간이 없습니다.** 걷다 보면 어느새 시작돼 있습니다.
 *
 * ── 참고한 설계 ────────────────────────────────────────────────
 * 안개문은 원래 "여기부터 보스다"라는 **명시적 체크포인트**로 만들어진
 * 장치입니다. 들어가기 전에 장비를 정비하고 마음을 먹으라는 신호죠.
 * 그리고 한 번 들어가면 나갈 수 없어서, 전투가 **격리**됩니다.
 *
 * 우리는 문을 세우지 않고 **영역**으로 같은 일을 합니다. 쿼터뷰라 문이
 * 화면을 가리기도 하고, 지형 편집기로 문틀을 세우는 것보다 데이터 한 줄이
 * 명확합니다.
 *
 * ── 나갈 수 있게 둔 이유 ────────────────────────────────────────
 * 원본은 못 나가게 막지만 우리는 **나갈 수 있되 초기화**로 했습니다.
 * 못 나가게 하려면 보이지 않는 벽이 필요한데, 쿼터뷰에서 안 보이는 벽은
 * 버그로 읽힙니다. "나가면 보스가 자리로 돌아가고 체력이 찬다"면 도망은
 * 여전히 가능하지만 **아무것도 얻지 못하므로**, 결과적으로 같은 압박이 됩니다.
 */
export const BOSS_ARENA = {
  /**
   * 보스가 자기 자리에서 이 거리 안에 들어와야 싸움이 시작됩니다.
   * 잡몹 무리(보통 8~12m 간격)와 겹치지 않을 만큼은 좁아야 합니다.
   */
  radius: 17,
  /**
   * 이 거리를 넘어가면 보스가 귀환합니다.
   *
   * **자동 플레이로 고친 값입니다(24 → 30).** 봇을 돌려보니 보스를 58초에
   * 만나 240초를 싸우고도 못 끝냈습니다. 원인은 이랬습니다:
   * 체력이 낮아 물러나 회복하면 그 후퇴가 24m를 넘겨 **보스가 초기화**되고,
   * 돌아오면 처음부터 다시. 무한 반복이었습니다.
   */
  leashRadius: 30,
  /**
   * 밖에 **이만큼 머물러야** 보스가 포기합니다(초).
   *
   * 거리만으로 판정하면 두 가지가 구분되지 않습니다:
   *   · 물러나서 회복하고 돌아온다 — 소울라이크의 **정상적인 플레이**
   *   · 때리고 도망친다            — 우리가 막으려던 것
   * 시간을 붙이면 갈립니다. 잠깐 나갔다 오는 것은 허용하고,
   * 계속 멀리 있으면 그때 포기합니다.
   */
  leashGrace: 2.5,
  /** 귀환 중 이동 속도 배율 — 천천히 걸어 돌아가야 "포기했다"가 읽힙니다. */
  returnSpeedScale: 1.6,
  /** 조우 연출 시간(초). 이 동안 보스는 공격하지 않습니다. */
  introTime: 1.6,
  /**
   * ── 추격 걸음 ──────────────────────────────────────────────────
   * 멀어지면 보스가 **뛰기 시작하는** 거리(m)와 그때의 속도 배율.
   *
   * ── 왜 필요한가 (잰 숫자) ────────────────────────────────────
   * 보스전 31초의 **시간 예산**을 처음으로 나눠 재 봤습니다:
   *   예고 6초 · 휘두름 2초 · 후딜 4.3초  → 실제 공격 40%
   *   쿨다운 8.7초 · **대기·이동 7.5초** · 페이즈전환 2.5초
   * 그리고 거리 분포에서 **9m 이상이 31%** 였습니다.
   *
   * 보스 이동속도는 2.4m/s, 플레이어는 5.4m/s 입니다. 두 배 차이라
   * 플레이어가 빠지기로 마음먹으면 보스는 **영원히 못 따라옵니다.**
   * 그 결과가 "31초 동안 5번 휘두름"(6.2초에 한 번)입니다.
   * 참고한 게임들(엘든 링·세키로·오공)의 보스는 1.5~2.5초에 한 번
   * 움직입니다. 3페이즈·5패턴·연계까지 얹은 설계가 도달 불가였던
   * 진짜 이유는 밸런스가 아니라 **보스가 자리에 못 온다**였습니다.
   *
   * ── 왜 기본 속도를 올리지 않는가 ─────────────────────────────
   * 근접에서 2.4m/s 라서 옆으로 돌아 등을 잡을 수 있습니다(백어택).
   * 기본값을 올리면 그게 통째로 사라집니다. 참고한 게임들도 보스의
   * **걷는 걸음과 쫓는 걸음이 다릅니다** — 붙으면 느긋하고, 빠지면
   * 달려옵니다. 거리로 나누면 둘 다 지킬 수 있습니다.
   *
   * 1.7배 = 4.1m/s 로, 플레이어(5.4m/s)보다는 여전히 느립니다.
   * "도망은 되지만 공짜는 아니다"가 되어야지, 따라잡히면 안 됩니다.
   */
  chaseRange: 7,
  chaseSpeedScale: 1.7,
} as const

/**
 * ── 강인도(포이즈) ───────────────────────────────────────────────
 *
 * ── 측정이 먼저였습니다 ─────────────────────────────────────────
 * 지금까지는 **타격 한 번마다 무조건 경직**이었습니다. 14초 동안 재보니:
 *
 *     잡몹  실제 공격 5회 → 계속 때리면 2회
 *     보스  실제 공격 3회 → 계속 때리면 **0회**
 *
 * 보스가 한 번도 공격하지 못합니다. 3페이즈도, 4색 연계도, 공격 토큰도
 * **왼클릭 연타 하나에 전부 무의미**해집니다. 만들어 놓은 설계가 작동조차
 * 못 하고 있었던 겁니다.
 *
 * (첫 측정은 "예고를 몇 번 띄웠나"를 셌는데 오히려 늘어서 가설이 반증됐습니다.
 *  경직이 공격을 끊고 되돌리니 예고가 **다시 시작**되어 더 세어졌던 것입니다.
 *  "판정까지 도달한 공격"을 세도록 계측기를 고치고 나서야 진짜 그림이 보였습니다.)
 *
 * ── 그렇다고 경직을 없애면 안 됩니다 ────────────────────────────
 * 경직에는 원래 목적이 있었습니다 — **"플레이어가 선공으로 흐름을 끊는다."**
 * 그냥 없애면 이번엔 때릴 이유가 사라지고 전투가 답답해집니다.
 *
 * 그래서 **끊는 것을 없애는 대신 조건을 붙입니다:**
 *   · 평소에는 맞아도 안 밀립니다 (연타 봉쇄 불가)
 *   · **예고(Windup) 중에 맞으면 강인도가 배로 깎입니다** — 타이밍을 보고
 *     끊는 것은 여전히 되고, 오히려 더 정확해야 합니다
 *   · 강인도를 다 깎으면 **무너집니다** — 긴 무방비 = 공격의 진짜 보상
 *
 * 즉 "아무 때나 연타"는 막고 "예고를 읽고 끊기"는 살립니다.
 * 4색을 읽어야 하는 이유가 하나 더 생기는 셈입니다.
 */
export const POISE = {
  /**
   * 강인도 피해 = 그 공격의 `trauma` × 이 값.
   *
   * 별도 필드를 새로 만들지 않은 이유: trauma 는 이미 "이 타격이 얼마나
   * 묵직한가"를 나타내는 숫자이고, 소리(sfx.swing)도 같은 값을 씁니다.
   * 같은 뜻의 숫자를 두 개 두면 밸런스를 바꿀 때 한쪽만 고쳐서
   * **손맛·소리·강인도가 서로 어긋납니다.**
   */
  fromTrauma: 40,
  /** 예고 중에 맞으면 이 배로 깎입니다 — "보고 끊기"를 살리는 장치. */
  /**
   * 💢 **무너질 때마다 단단해집니다** — 무거운 적(보스·정예)만.
   *
   * ── 왜 (자동 플레이 한 판이 통째로 이걸 가리켰습니다) ──────────────
   *     보스전 15.4초 · **받은 피해 0** · 미처치
   *     보스의 시간 — 예고 4.9초인데 **휘두름 0.2초(한 번)**
   *     붕괴 3회 · 처형 3회 · 무너져서 끊긴 연계 7회
   *
   * 예고를 여섯 번 띄웠는데 판정까지 간 것은 **한 번**입니다. 나머지는
   * 전부 무너져서 끊겼습니다. 존의 마지막 시험이 15초 동안 **한 대도**
   * 못 때립니다.
   *
   * 이 저장소는 같은 병을 이미 한 번 앓았습니다 — *"맞으면 무조건 경직"*
   * 시절에 보스가 14초 동안 한 번도 공격하지 못했고, 그래서 평타의
   * 강인도 배수를 0.35 로 낮추고 *"예고를 읽고 끊기"* 에 값을 몰아줬습니다.
   * 그 처방은 옳았지만 **한 번의 붕괴가 얼마나 자주 와도 되는지**는 아무도
   * 정하지 않았습니다. 15초에 세 번이면 보상이 아니라 **기본기**입니다.
   *
   * ── 참고한 답 ────────────────────────────────────────────────────
   * 세키로는 체간이 **회복**되고, 오공의 보스는 무너진 뒤 **분노**하며,
   * 엘든 링은 후반 페이즈에서 포이즈가 오릅니다. 공통점 하나 — **첫
   * 붕괴는 크게 갚아 주고, 그 뒤로는 점점 어렵게** 만듭니다.
   *
   * 0.45: 한 번 무너뜨릴 때마다 강인도 저항이 45%씩 오릅니다(누적 곱이
   * 아니라 덧셈이라 예측 가능합니다 — 1 → 1.45 → 1.9 → 2.35).
   * 상한 3회는 *"네 번째부터는 사실상 안 무너진다"* 가 아니라
   * *"세 번이면 충분히 갚았다"* 는 뜻입니다.
   *
   * ⚠️ **잡몹에는 안 겁니다.** 잡몹을 연달아 무너뜨리는 것은 군중을 다루는
   *    재미 그 자체입니다(로스트아크가 파는 것이 그것입니다). 이 규칙은
   *    *"한 놈과 오래 싸우는"* 상대에게만 뜻이 있습니다.
   */
  breakResistStep: 0.45,
  breakResistMax: 3,
  windupMultiplier: 2.5,
  /**
   * **평범한 타격은 강인도를 잘 못 깎습니다.**
   *
   * ── 왜 (재고 나서 정했습니다) ────────────────────────────────────
   * 처형을 넣고도 한 판에 **붕괴 40회 · 처형 1회** 였습니다. 원인을 재 보니:
   *
   *     무너진 순간의 평균 체력 **36%**
   *     무너진 채로 그대로 죽은 적 **29마리 / 40회 (73%)**
   *
   * 붕괴가 보상이 아니라 **사망 연출**이었습니다. 숫자를 보면 당연했습니다 —
   * 잡몹 강인도 30, 롱소드 3타 콤보의 강인도 피해 합이 33.6.
   * **아무 콤보나 한 번 넣으면 무너집니다.** 그런데 그 콤보는 체력도 같이
   * 깎으므로, 강인도는 "체력보다 조금 뒤에서 따라오는 두 번째 체력바"였습니다.
   * 축이 하나면 축이 아닙니다.
   *
   * 세키로가 체간을 체력과 **따로** 둔 이유가 이것입니다. 체간은 체력이 많이
   * 남았을 때도 터져야 "무너뜨리고 마무리한다"가 하나의 전술이 됩니다.
   *
   * 그래서 평타의 강인도 피해를 크게 줄이고, **끊는 수단**에 몰아줍니다:
   *   · 🥋 강타(집중 소모)  — 한 방에 잡몹을 무너뜨립니다
   *   · 예고 중에 때리기     — "보고 끊기"
   *   · 🟢 반격             — 강인도와 무관하게 즉시
   * 이 셋이 실제 답이 되어야 4색과 집중이 장식이 아니게 됩니다.
   */
  basicMultiplier: 0.35,
  /**
   * ── 등 뒤에서 때리면 강인도가 훨씬 빨리 깎입니다 ──────────────────
   *
   * ── 왜 (잰 것) ─────────────────────────────────────────────────
   * 존에서 백어택 비율이 **6~7%** 입니다. 무기와 무관하게(롱소드 7% ·
   * 쌍단검 6%) 그렇습니다. 기둥 3은 *"포지셔닝이 보상받는다"* 인데
   * 보상받는 타격이 열에 하나가 안 됩니다.
   *
   * 기회가 없어서가 **아닙니다.** 산수로 확인했습니다:
   *     등 뒤 판정까지 110° · 사거리 2.2m 에서 호 4.2m → **0.78초**
   *     창 = 반응 0.5초 + 되돌아보기 110/150 = **1.23초**
   * 창이 도는 시간보다 깁니다. 돌 수 있습니다.
   *
   * 문제는 **쓸 이유**입니다. 돌아가는 0.78초에 앞에서 때리면 두 대가
   * 들어갑니다. 등 뒤 보상은 한 대에 ×1.55(+치명타)뿐이라, 손익이
   * 거의 같습니다. **거의 같으면 아무도 안 합니다.**
   *
   * ── 참고한 게임들은 이걸 **범주적**으로 만듭니다 ────────────────
   *   · 소울/엘든 링 — 백스탭은 "피해 +55%"가 아니라 **전용 치명 연출**
   *   · 세키로 — 등 뒤 일격은 잡몹 즉사, 그리고 **체간이 훨씬 빨리 참**
   *   · 오공 — 뒤에서 치면 별도 연출과 큰 피해
   * 공통점은 "조금 더 아프다"가 아니라 **다른 결과가 나온다**는 것입니다.
   *
   * 우리에겐 이미 범주적 보상이 있습니다 — **붕괴 → 처형**. 그래서 새
   * 장치를 만들지 않고, 등 뒤 타격이 **그 문으로 가는 지름길**이 되게
   * 합니다. 이제 돌아가는 이유가 "이 한 대가 세다"가 아니라
   * **"무너뜨리는 속도가 4.6배"** 가 됩니다.
   *
   * 값 1.6 의 자리: 평타 0.35 < **등 뒤 1.6** < 예고 중 2.5.
   * 예고 중에 등 뒤를 잡는 것이 여전히 최고 보상으로 남습니다.
   * (배수는 겹치지 않고 하나만 고릅니다 — combat.ts applyPoise 설계 노트)
   */
  backMultiplier: 1.6,
  /** 마지막으로 맞고 이 시간이 지나면 회복이 시작됩니다(초). */
  regenDelay: 2.2,
  /** 초당 회복량. 전투가 길어져도 결국 무너뜨릴 수 있어야 합니다. */
  regenPerSec: 18,
  /** 무너졌을 때의 무방비 시간(초). 큰 적일수록 깁니다 = 보상도 큽니다. */
  brokenTime: 1.0,
  brokenTimeBoss: 1.9,
} as const

/**
 * ── 처형 — 무방비에만 붙는 **전용 동사** ─────────────────────────────
 *
 * ── 왜 넣는가 (재고 나서 정했습니다) ────────────────────────────────
 * 강인도 붕괴는 원래 *"긴 무방비 자체가 보상"* 이라고 두고 별도 배수를 안
 * 붙였습니다. 그 판단이 맞으려면 **무방비 동안 실제로 때리고 있어야** 합니다.
 * 자동 플레이로 재 보니 한 판에 **붕괴 34회**(처치 45마리 — 적 하나당 0.75회)
 * 인데, 무방비인 적 곁에서 실제로 때린 시간은 **44%** 였습니다.
 * 즉 이 게임에서 가장 자주 오는 큰 보상의 **절반 이상이 그냥 흘러갑니다.**
 *
 * 세키로의 인살, 소울의 리포스트, 오공의 처형이 공통으로 하는 일은 하나입니다:
 * 무너진 상태에 **그 상태에서만 되는 동사**를 붙입니다. 우리 게임의 무방비에는
 * "평소처럼 때린다" 말고 할 수 있는 게 없었습니다. 동사가 없으면 상태는
 * 상태로만 남습니다.
 *
 * ── 세지는 게 아니라 **옮기는** 것입니다 ────────────────────────────
 * 봇은 이미 한 판도 안 죽습니다. 여기서 플레이어를 그냥 더 세게 만들면 안
 * 됩니다. 그래서 처형은 **무방비를 소모합니다** — 넣는 순간 적이 일어납니다.
 * 남은 창에서 두세 대 더 넣을 수도 있었던 것을 **한 방에 몰아 받는 거래**이지,
 * 공짜로 얹는 피해가 아닙니다.
 *
 * 그리고 **선행동작 동안 무적이 아닙니다.** 소울류의 리포스트가 위험한
 * 이유와 같습니다 — 이 존은 교전 중 45%가 둘 이상이라, 처형을 넣는 0.34초는
 * 옆의 적에게 등을 내주는 시간입니다. 그게 이 동사의 결정 지점입니다.
 */
export const FINISHER = {
  /** 무방비인 적이 이 안에 있으면 기본 공격이 처형으로 바뀝니다(m). */
  reach: 2.6,
  /** 정면 판정 각도 — 등 뒤에서도 됩니다. 무방비는 방향을 안 봅니다. */
  arcDeg: 150,
  /**
   * 콤보 마무리 타 피해의 몇 배인가.
   *
   * 2.6 인 이유: 무방비 1.0초(잡몹)에 평소처럼 때리면 두 대 남짓 들어갑니다.
   * 그보다 확실히 나으면서, 한 방에 전투가 끝나지는 않아야 합니다.
   * 무기별로 따로 적지 않는 이유는 **무기 정체성을 마무리 타가 이미 담고**
   * 있기 때문입니다 — 느린 무기는 마무리가 세고, 빠른 무기는 약합니다.
   */
  damageMultiplier: 2.6,
  windup: 0.34,
  active: 0.1,
  recovery: 0.42,
  staminaCost: 18,
  hitstop: 0.16,
  trauma: 0.62,
  /** 처형은 밀지 않습니다 — 시체가 날아가면 "끝냈다"가 아니라 "튕겼다"가 됩니다. */
  knockback: 0.4,
  lunge: 1.6,
} as const

/**
 * ── 불티 (처치 보상 + 죽음의 대가) ──────────────────────────────
 *
 * ── 왜 필요한가 ────────────────────────────────────────────────
 * 지금까지 적을 죽이면 **아무 일도 일어나지 않았습니다.** 그러면 최적 전략은
 * 공들여 만든 전투를 전부 **지나쳐 달리는 것**입니다. 4색 예고도, 트라이포드도,
 * 무기 3종도 쓸 이유가 없어집니다. 전투 시스템이 있는데 전투할 이유가 없는 게
 * 지금 이 게임의 가장 큰 모순이었습니다.
 *
 * 동시에 **죽음의 대가도 0**이었습니다. 화톳불 부활은 잃는 것이 없어서,
 * 위험한 선택을 말릴 것이 아무것도 없었습니다.
 *
 * 두 구멍은 사실 하나입니다. 처치 보상을 만들고 **그것을 죽을 때 떨어뜨리면**
 * 한 번에 메워집니다.
 *
 * ── 왜 "잃는다"가 아니라 "떨어뜨린다"인가 ───────────────────────
 * 그냥 사라지게 하면 그건 벌일 뿐입니다. **되찾으러 가는 길**이 있어야
 * 긴장이 생깁니다 — 죽은 자리로 돌아가는 그 한 번의 이동이, 이 시스템이
 * 만들어내는 유일하고도 가장 중요한 순간입니다.
 *
 * ── 왜 표식은 하나뿐인가 ────────────────────────────────────────
 * 여러 개가 쌓이면 "나중에 한꺼번에 줍지 뭐"가 되어 손실이 실감나지 않습니다.
 * 하나만 두면 **되찾으러 가다가 또 죽는 것이 진짜 손실**이 됩니다.
 */
export const EMBER = {
  /**
   * 성수병 강화 비용 (3개 → 4 → 5 → 6).
   *
   * 왜 하필 성수병인가: 방금 만든 시스템에 직접 이어지고, **생존력** 강화라
   * 죽을수록 다음 시도가 쉬워집니다. 좌절을 푸는 밸브 역할을 합니다.
   * (공격력에 붓게 하면 "약해서 죽었으니 더 죽어야 강해진다"가 되어 반대로 갑니다.)
   */
  vialUpgradeCosts: [60, 140, 260] as const,
  /** 성수병 최대 충전 상한. 무한히 늘리면 회복이 다시 공짜가 됩니다. */
  vialMax: 6,

  /**
   * ── 되살아난 적은 불티를 덜 줍니다 ──────────────────────────────
   *
   * ── 측정이 먼저였습니다 ─────────────────────────────────────
   * 자동 플레이가 존을 사망 0회로 끝냅니다. "쉽다"만으로는 무엇을 고칠지
   * 알 수 없어서 위험을 직접 재봤습니다:
   *
   *     총 받은 피해 205 (최대 체력의 2배) · 최저 체력 26.8
   *     성수병 3개 사용 · **휴식 4회**
   *
   * 봇은 **맞긴 맞습니다.** 안 죽는 이유는 회복이 피해보다 훨씬 넉넉하기
   * 때문입니다 — 휴식 4회는 **완전 회복 4번**이고, 그게 공짜입니다.
   *
   * ── 휴식의 대가가 대가가 아니었습니다 ───────────────────────
   * 소울라이크에서 쉬는 대가는 "적이 되살아난다"입니다. 그런데 우리는
   * 적을 잡으면 불티가 나오므로, 되살아나는 것이 **벌이 아니라 보상**입니다.
   * 그래서 쉬는 데 아무 대가가 없고, 무한 파밍까지 열립니다
   * (같은 판에서 처치가 21 → 59로 늘어난 것이 이것입니다).
   *
   * ── 왜 "휴식에 비용"이 아니라 "보상 체감"인가 ─────────────────
   * 쉬는 데 불티를 받으면 **회복이 유료**가 됩니다. 그건 잘하는 사람보다
   * 못하는 사람을 훨씬 세게 때립니다 — 우리 설계에서 성수병이 맡은
   * "죽을수록 쉬워지는 밸브"와 정면으로 부딪칩니다.
   *
   * 보상 체감은 반대로 **파밍하는 쪽만** 때립니다. 진도를 위해 쉬는 사람은
   * 잃는 것이 없고, 같은 자리를 반복해 도는 사람만 수확이 줄어듭니다.
   *
   * ── 죽음은 세대를 올리지 않습니다 ───────────────────────────
   * 죽어도 적이 되살아나지만 그건 **자발적이지 않습니다.** 죽을수록 벌이가
   * 줄면 못하는 사람이 영영 못 따라옵니다. 대가는 **스스로 고른 휴식**에만.
   */
  respawnDecay: 0.55,
  /** 아무리 반복해도 이 아래로는 안 떨어집니다 — 0이면 되살아난 적이 완전한 낭비가 됩니다. */
  respawnFloor: 0.2,
} as const

/**
 * ── 무기 강화 ────────────────────────────────────────────────────
 *
 * ── 측정이 먼저였습니다 ─────────────────────────────────────────
 * 자동 플레이가 존을 끝냈을 때 **불티 364를 들고 있었고 강화는 1회**였습니다.
 * 불티는 쓸 곳이 없어 그냥 쌓이는 숫자였습니다. 자원이 남아돈다는 것은
 * "죽으면 불티를 잃는다"는 규칙도 아프지 않다는 뜻입니다 —
 * **잃어도 아깝지 않은 것을 잃는 것은 대가가 아닙니다.**
 *
 * ── 왜 하필 무기인가 ────────────────────────────────────────────
 * 성수병(생존)은 이미 첫 번째 소비처입니다. 두 번째는 성격이 달라야 합니다.
 *   · 성수병 = **죽을수록 쉬워지는** 밸브. 좌절을 풉니다.
 *   · 무기   = **싸울수록 세지는** 보상. 전투를 지나치지 않을 이유가 됩니다.
 * 둘이 같은 방향이면 선택이 아니라 순서일 뿐입니다. 지금은 "지금 죽지 않는
 * 것이 급한가, 다음 구간을 빨리 넘기고 싶은가"가 됩니다.
 *
 * ── 수치의 근거 ────────────────────────────────────────────────
 * · maxLevel 5 · 단계당 +12% — 최대 +60%. 존 하나를 통째로 무의미하게
 *   만들지 않을 만큼입니다(적 체력을 2배로 만들면 그건 새 게임입니다).
 * · 비용 80/170/300/480/720 — 합계 1750. 자동 플레이 한 판이 364였으니
 *   **한 판으로는 두 단계도 못 올립니다.** 남아돌던 자원이 모자란 자원이 됩니다.
 * · 성수병 첫 강화(60)보다 첫 무기 강화(80)가 비쌉니다. 처음 손에 쥔 불티는
 *   **생존에 쓰는 것이 기본값**이어야 합니다 — 초보자가 죽어서 배우는 게임이니까요.
 * · 무기마다 따로 올립니다. 그래야 "무엇을 키웠는가"가 그 사람의 선택이 됩니다.
 */
/**
 * ── 🩸 **출혈 — 적에게 쌓이는 세 번째 축** ──────────────────────────
 *
 * 지금 적에게 쌓이는 것은 **체력과 강인도** 둘뿐입니다. 둘 다 *"한 방이
 * 얼마나 센가"* 를 묻습니다. 그래서 무기가 갈라질 축도 그 하나뿐이고,
 * 쌍단검(약한 다단히트)이 존재할 이유가 **백어택 하나**밖에 없습니다.
 *
 * ── 참고 게임은 전부 세 번째 축을 갖고 있습니다 ─────────────────
 * · **소울류** — 출혈·독이 맞을수록 차오르다 **터집니다**. 터질 때 피해가
 *   최대 체력의 비율이라, 체력 많은 적일수록 값이 큽니다. 곡검·단검처럼
 *   약하고 빠른 무기가 여기서 제 값을 합니다.
 * · **블러드본** — 신속 독. 같은 원리를 더 빠르게.
 * · **검은 신화: 오공** — 화상·빙결·감전이 쌓이다 한 번에 터집니다.
 * · **로스트아크** — 파괴·무력화 게이지. **이어진 압박**을 보상합니다.
 *
 * 공통점 하나 — **한 방이 아니라 이어짐이 보상받습니다.** 그리고 그
 * 보상은 강인도(무너뜨림)와 **다른 것**이어야 합니다. 같으면 축이 하나
 * 늘어난 게 아니라 이름이 둘인 같은 축입니다.
 *
 * ── 강인도와 무엇이 다른가 ────────────────────────────────────
 *   강인도 — **큰 한 방**이 잘 깎습니다(예고 중 ×2.5, 강타 ×2.2).
 *            보상은 **무방비 시간**(처형으로 잇는 창).
 *   출혈  — **잦은 여러 방**이 잘 쌓습니다(타수로 오릅니다).
 *            보상은 **최대 체력 비율 피해**(즉시, 창 없음).
 *
 * 그래서 대검은 무너뜨리고, 단검은 터뜨립니다. 같은 적을 **다른 방법으로**
 * 죽이는 것 — 그게 무기제를 고른 이유입니다(DESIGN.md 기둥 1).
 */
export const BLEED = {
  /** 가득 차면 터집니다. 100 으로 두어 화면 표시가 곧 퍼센트가 되게 합니다. */
  max: 100,
  /**
   * 한 타가 쌓는 기본량. 무기별 배율은 arsenal 의 `bleedScale` 이 곱합니다.
   *
   * 12 로 잡은 근거: 쌍단검 콤보 한 바퀴가 4타(배율 1.6)라 **76.8** 이
   * 쌓입니다. 즉 **두 바퀴가 채 안 되어** 터집니다(약 1.6바퀴). 롱소드는
   * 3타(배율 1.0)라 36 — **세 바퀴**가 필요합니다. 대검은 2타(배율 0.55)라
   * 13.2 로, 여덟 바퀴가 걸려 사실상 이 축을 안 씁니다. 그게 의도입니다.
   */
  perHit: 12,
  /**
   * 마지막 타격 뒤 이만큼 지나면 **줄기 시작**합니다.
   *
   * 2.5초 근거: 이 게임의 콤보 한 바퀴가 1.3~1.5초입니다. 한 바퀴 넣고
   * 한 번 구르고 다시 붙는 데 드는 시간이 대략 그 안쪽이라, **이어서
   * 압박하면 유지되고 물러나면 식습니다.** 그게 이 축이 파는 것입니다.
   */
  decayDelay: 2.5,
  /** 식는 속도(초당). 5초면 0으로 돌아갑니다 — 되돌릴 수 없을 만큼 느리지 않게. */
  decayPerSec: 20,
  /**
   * 터질 때 **최대 체력의 이 비율**만큼 피해.
   *
   * 비율인 이유: 잡몹(체력 60)에게 15%는 9, 보스(체력 620)에게는 93 입니다.
   * 고정값으로 두면 잡몹에게만 세거나 보스에게만 세집니다. 소울류의 출혈이
   * 비율인 이유가 그것이고, **체력이 많은 적일수록 이 축의 값이 큽니다** —
   * 즉 오래 버티는 상대에게 쓰는 답이 됩니다.
   */
  popDamagePct: 0.15,
  /**
   * 터질 때 피해의 **상한**.
   *
   * ── 왜 필요한가 (계측기가 먼저 죽었습니다) ────────────────────────
   * `npm run weapons` 는 허수아비 체력을 **1,000,000** 으로 세워 놓고 씁니다
   * (안 죽어야 16초를 잴 수 있으니까요). 거기에 *"최대 체력의 15%"* 를
   * 곱하니 한 번 터질 때 **15만**이 들어갔고, 초당 피해가 **18,764** 로
   * 찍혔습니다. 대검만 멀쩡했는데 — 대검은 이 축을 안 쓰기 때문입니다.
   *
   * 계측기 탓으로 넘길 일이 아닙니다. **비율 피해에 상한이 없다**는 것은
   * 체력이 큰 상대가 나오는 날 그 적이 **출혈 하나로 삭제된다**는 뜻입니다.
   * 소울류·엘든 링의 출혈도 *"고정값 + 비율"* 이고 위가 막혀 있습니다.
   *
   * 100 으로 잡은 근거: 지금 이 존에서 가장 큰 적이 보스(체력 620)이고
   * 15%가 **93** 입니다. 즉 **오늘의 밸런스는 한 점도 안 바뀝니다** —
   * 이 값은 미래의 큰 적과 실험대를 막는 난간입니다.
   */
  popDamageCap: 100,
  /**
   * 터질 때 함께 깎는 강인도. 작게 둡니다(무너뜨리기는 강인도의 몫).
   * 0.35 = 기본 타격 한 대 남짓 — *"터지면 잠깐 휘청"* 정도입니다.
   */
  popPoise: 0.35,
  /**
   * ── 🩸 **「한 번도 터진 적이 없다」는 틀린 문장이었습니다** ─────────────
   *
   * ⚠️ 아래 진단 전체가 **판이 잘린 상태에서** 나온 것입니다. 그때 자동
   *    플레이는 예산 420초 중 **67~120초에 막혀** 끝났고 보스를 만나지도
   *    못했습니다. 그 반쪽짜리 판에서 나온 「0회」를 이 주석이 *"이 축은
   *    죽어 있다"* 로 적었고, **세 라운드가 그 위에서 헛돌았습니다.**
   *
   * 봇의 막힘을 고쳐 존을 끝까지 돌게 한 뒤, 같은 것을 다시 쟀습니다:
   *
   *     롱소드  ★166.5초 클리어 · 🩸 터짐 **1회** · 보스 최고 109.5/100
   *     쌍단검  ★217.1초 클리어 · 🩸 터짐 **2회** · 보스 최고 109.9/100
   *                                  보스를 녹인 것 중 **출혈 158** (3위)
   *     대검    ★137.9초 클리어 · 🩸 터짐 **0회** — 문턱까지 16대이니
   *             **설계대로입니다**(`perHit` 주석: *"여덟 바퀴가 걸려 사실상
   *             이 축을 안 씁니다. 그게 의도입니다"*)
   *
   * ⚠️ 대검은 **첫 판에서 69초에 막혀** 「존을 못 끝냄」으로 찍혔습니다.
   *    다음 판은 137.9초에 클리어했습니다 — 판 변동입니다. 한 판으로
   *    *"이 무기로는 존을 못 깬다"* 를 적을 뻔했습니다. 세 무기 표는
   *    **판당 하나**라 아직 경향이지 결론이 아닙니다.
   *
   * **이 축은 살아 있고, 설계한 대로 무기마다 다르게 삽니다.** 단검이
   * 가장 많이 터뜨리고, 대검은 안 터뜨립니다 — 위 `perHit` 주석이 적어 둔
   * 그대로입니다.
   *
   * ── 그래서 이 저장소가 배운 것 ────────────────────────────────
   * ① **「0회」 옆에는 무기를 적어야 합니다.** 어떤 무기에게는 0회가
   *    정답인데, 장부가 무기를 안 적으면 그 0이 병으로 읽힙니다.
   *    이제 `playthrough` 가 *"← daggers 로 잰 판"* 까지 함께 씁니다.
   * ② **잘린 판의 숫자로 설계를 진단하면 안 됩니다.** 봇이 예산의 17%에서
   *    죽고 있다는 사실이 모든 장부를 오염시키고 있었습니다.
   *
   * 아래 진단은 **역사로 남깁니다** — 잡몹 쪽 이야기는 지금도 유효하고
   * (적이 먼저 죽습니다), 보스 쪽 처방(`decayFloorRatio`)도 그대로 씁니다.
   *
   * ── (이하 당시의 기록) ────────────────────────────────────────
   *
   * 위 `perHit` 주석이 *"쌍단검은 약 1.6바퀴면 터진다"* 고 적어 두었는데,
   * 그 계산에는 **적이 그때까지 살아 있는가**가 빠져 있었습니다.
   *
   * ── 병이 **둘**이고 처방이 다릅니다 ─────────────────────────────
   *
   *   잡몹 — 적이 **먼저 죽습니다.**
   *          죽기까지 맞은 횟수 **2.8대** × 한 대당 **11.4** = **31.9/100**
   *          그 판에서 **식어서 잃은 것은 2.4뿐**입니다. 즉 식는 게 문제가
   *          아니라 살아 있는 시간이 짧습니다.
   *
   *   보스 — **식어서 날아갑니다.**
   *          쌓은 총량 192 · **식어서 날아간 것 162.7 (85%)** · 최고 65.7
   *          타격 간격 평균 2.45초(유예 2.5초 안쪽!) · **최대 17.05초**
   *          평균은 유예 안인데 85%가 날아갑니다 — 세 번에 한 번 나는
   *          긴 공백이 그때까지 쌓은 것을 통째로 지웁니다.
   *
   * ── ⛔ **잡몹 쪽은 손대지 않습니다** (이미 한 번 데인 자리입니다) ──
   * 문턱을 적의 체력에 비례시키면 잡몹이 29 쯤 됩니다. 그런데 이 저장소는
   * **정확히 그 값을 이미 시도하고 되돌렸습니다** — `enemies.ts`
   * `bleedMaxOf` 의 설계 노트에 있습니다. 잡몹 문턱 30 이면 쌍단검이
   * **두 대 만에** 터뜨리고, 터짐이 최대 체력의 15% 라 짧은 창의 폭발력이
   * 통째로 뒤집힙니다:
   *
   *     잡몹 반격창(0.7초) 쌍단검 피해 24 → **124** · `weapons` 17/18 → 14/17
   *
   * 같은 값을 이유만 바꿔 다시 넣는 것은 배운 것이 아닙니다. 그쪽의 진짜
   * 질문은 그 노트가 이미 적어 두었습니다 — *"왜 두 대뿐인가"*. 그건 잡몹
   * 체력의 이야기라 이 축이 혼자 답할 수 없습니다.
   *
   * ── ✅ 그래서 **보스 쪽만** 고칩니다 ───────────────────────────
   * ── 🩸 **몰릴수록 지운 것이 덜 지워집니다** (세키로의 체간) ───────
   *
   * ── ❌ 먼저 **식는 속도**를 늦춰 봤고, 프로브가 아니라고 했습니다 ──
   * 처음엔 *"체력이 낮을수록 천천히 식는다"* 로 넣었습니다(배율 0.3).
   * `npm run bleed` 가 바로 재 줬습니다:
   *
   *     같은 간격 3.2초 × 14대 — 체력 100% 최고 **24** · 체력 15% 최고 **89.7**
   *
   * 방향은 맞는데 **안 터집니다.** 그리고 더 나쁜 것은, 89.7 까지 가는 데
   * 열여섯 대가 필요한데 **보스가 체력 15% 이하로 있는 시간이 8.3초**
   * (실측 3단계 길이)뿐이라는 것입니다. 그 사이에 들어가는 타격은 세 대쯤
   * 입니다. 즉 이 처방은 **실제 보스전에서는 일어나지 않는 조건**을 고치고
   * 있었습니다. 계측기가 없었으면 "고쳤다"고 적고 넘어갔을 자리입니다.
   *
   * ── 실측이 가리키는 진짜 범인은 **긴 공백**입니다 ────────────────
   *
   *     타격 간격 평균 2.45초(유예 2.5초 **안쪽**) · **최대 17.05초**
   *     쌓은 총량 192 · 식어서 날아간 것 162.7 (85%)
   *
   * 평균은 이기고 있는데 85%가 날아갑니다. 17초짜리 공백 한 번이면 20/초로
   * **340** 이 지워집니다 — 그때까지 쌓은 것이 무엇이든 0 이 됩니다. 보스전은
   * 원래 붙었다 떨어지는 싸움이라, 이 축은 구조적으로 성립할 수가 없었습니다.
   *
   * ── 세키로가 푸는 방식: **바닥이 올라옵니다** ───────────────────
   * 세키로의 체간은 그냥 회복되는 것이 아니라, **체력이 깎일수록 회복될 수
   * 있는 하한이 올라갑니다.** 그래서 아무리 물러나도 이미 몰아붙인 만큼은
   * 남고, 긴 싸움의 끝은 반드시 체간으로 끝납니다.
   *
   *     바닥 = 문턱 × decayFloorRatio × (1 - 남은 체력 비율)
   *
   *     체력 100% → 바닥 **0**    (아무것도 안 바뀝니다)
   *     체력  50% → 바닥 **30**
   *     체력  15% → 바닥 **51**
   *     체력   0% → 바닥 60
   *
   * 0.6 의 근거: 바닥 51 에서 문턱 100 까지는 롱소드 **네 대**(12씩)입니다 —
   * 콤보 한 바퀴 반. *"몰아붙이면 터진다"* 가 한 번의 붙음 안에 들어와야
   * 하고, 그렇다고 바닥이 문턱에 닿으면 안 됩니다(닿으면 안 때려도 터집니다).
   *
   * ⚠️ **체력이 가득한 적에게는 정확히 0 입니다.** 이게 이 값을 고른 가장
   *    큰 이유입니다 — `npm run weapons` 의 허수아비는 체력 1,000,000 을
   *    가득 채우고 서 있으므로 그 벤치의 숫자는 **한 점도 안 움직입니다.**
   *    예전에 문턱을 건드렸다가 정확히 그 벤치가 17/18 → 14/17 로 깨졌고,
   *    그래서 되돌렸습니다. 이번 변화는 그 자리를 **지나가지 않습니다.**
   *
   * ⚠️ 이 축의 계약(*"물러나면 사라진다"*)은 **초반에 그대로**이고 끝으로
   *    갈수록 느슨해집니다. 계약이 갑자기 바뀌는 것이 아니라 **연속적으로**
   *    바뀌는 것이 중요합니다 — 어디서부터 규칙이 달라지는지 외울 필요가
   *    없어야 합니다.
   */
  decayFloorRatio: 0.6,
  /**
   * ── ⏸ **유예의 주어는 플레이어입니다 — 그리고 이미 그렇게 되어 있었습니다** ──
   *
   * 바닥(`decayFloorRatio`)을 넣고 나서 자동 플레이가 이렇게 찍었습니다:
   *
   *     보스 — 터짐 **0회** · 최고 **98.5/100** · 쌓은 총량 132 · 식어서 33.5
   *
   * 65.7 에서 98.5 로 올라왔는데 **1.5 가 모자랍니다.** 여기서 숫자를
   * 만지고 싶어지지만 그건 이 저장소가 여러 번 되돌린 길입니다
   * (*"한 칸 차이의 초록은 운이다"*). 대신 물었습니다 — **그 33.5 는
   * 누구 탓인가.**
   *
   * 위 `decayDelay` 주석은 이 축의 계약을 *"이어서 압박하면 유지되고
   * 물러나면 사라진다"* 라고 적어 두었습니다. 계약의 주어는 **플레이어**
   * 여야 합니다. 페이즈 전환 1.25초 동안 보스는 **무적이고 플레이어를
   * 밀어냅니다** — 때려도 안 들어가는 시간을 태만으로 계산하면, 게임이
   * 손을 묶어 놓고 그 값을 플레이어에게 물리는 셈입니다.
   *
   * ── ❌ 그래서 규칙을 새로 넣었다가 **A/B 가 반박했습니다** ──────────
   * `무적이면 유예 시계를 멈춘다`를 식는 블록에 넣고 실험대를 세웠더니
   * 통과했습니다. 그런데 **넣기 전과 넣은 뒤가 소수점까지 같았습니다**
   * (창A −0 · 창B −22.3 · 14/14, 양쪽 동일).
   *
   * 범인은 `enemyAI` 의 전환 블록 끝에 있는 **`continue`** 였습니다.
   * 전환 중에는 식는 블록이 통째로 건너뛰어져서, **이 성질은 이미
   * 참이었습니다.** 제가 넣은 것은 같은 일을 두 번째로 적은 죽은 가지였고,
   * *"규칙은 한 곳에만"* 을 어기는 쪽이었습니다. 되돌렸습니다.
   *
   * ⚠️ **A/B 를 안 돌렸으면 "고쳤다"고 적고 넘어갔을 자리입니다.** 초록은
   *    변화의 증거가 아닙니다 — 초록은 **문장이 참이라는** 증거일 뿐이고,
   *    그 문장이 원래 참이었는지는 **없는 판**을 돌려야만 알 수 있습니다.
   *
   * ── ✅ 남긴 것 ────────────────────────────────────────────────
   * · `npm run bleed` — **같은 보스·같은 체력**으로 등을 맞댄 두 창을 재서
   *   못 박습니다(창A 무적 −0 · 창B −22.3). 부수 효과로 얻은 성질이라
   *   누가 `continue` 를 풀어 쓰면 **아무 오류 없이** 사라집니다.
   * · `playthrough` 의 *"때릴 수 없어서 유예를 안 먹은 시간"* — 안 깎은 것은
   *   장부에 흔적이 안 남아서, 초로 적어 둡니다. 0.0초인 판이 있으면 그
   *   판에서는 이 성질이 한 번도 일하지 않은 것이고 **그것도 사실입니다.**
   *
   * ✅ **「98.5 에서 1.5 가 모자란다」는 질문은 닫혔습니다 — 판이 짧았던
   *    것입니다.** 그때 저는 *"한 판에 쌓는 총량이 132 뿐이니 이 축은 잘
   *    풀려도 판당 한 번, 그것도 맨 끝에 터진다"* 고 적었습니다. 그 132 는
   *    **보스전이 22초에 잘린 판**의 값이었습니다. 봇이 존을 끝까지 돌게
   *    고친 뒤 다시 재니:
   *
   *        롱소드  쌓은 총량 132 → 터짐 **1회** (최고 109.5)
   *        쌍단검  쌓은 총량 **292.8** → 터짐 **2회** (최고 109.9)
   *
   *    단검은 문턱의 **세 배**를 쌓습니다. 손잡이가 없는 것이 아니라,
   *    **손잡이를 안 쥔 무기로 재고 있었습니다.**
   *
   * ⚠️ 그러니 여기서 배울 것은 출혈의 수치가 아니라 이것입니다 —
   *    **재는 도구가 판을 끝까지 못 돌면, 그 판에서 나온 모든 진단은
   *    「짧아서 그렇다」와 구분되지 않습니다.** 이 주석은 그때 그 구분을
   *    안 하고 설계를 탓했습니다.
   */
} as const

export const WEAPON_UPGRADE = {
  maxLevel: 5,
  /** 단계당 피해 증가 비율 */
  damagePerLevel: 0.12,
  /** 0→1, 1→2, ... 단계별 불티 비용 (대장장이 삯) */
  costs: [80, 170, 300, 480, 720] as const,
  /**
   * ── 정련석 — **비용으로는 희소성이 안 만들어집니다** ──────────────
   *
   * 무기 강화를 넣고 자동 플레이로 재보니 이렇게 나왔습니다:
   *
   *     한 판(408초)에 번 불티 1442 = 초당 3.53
   *     무기 하나 만렙 1750 → **210초만 더 파밍하면 끝**
   *
   * 게다가 쉬면 적이 되살아나므로 파밍은 **무제한**입니다(같은 판에서 처치가
   * 21 → 59로 늘었습니다). 비용을 올려 봐야 파밍 시간만 길어지고,
   * 그건 어려운 게 아니라 **지루한 것**입니다.
   *
   * 그래서 소울라이크의 정석을 씁니다: 강화에 **돈과 별개의 재료**를 요구하고,
   * 그 재료는 **파밍으로 얻을 수 없게** 합니다.
   *   · 불티   = 흔한 것. 싸우면 나옵니다 (성수병·삯)
   *   · 정련석 = 귀한 것. **보물과 보스에서만** 나옵니다
   *
   * 이 구분이 우리 기둥 4(탐험)와 정확히 같은 방향입니다.
   * *"많이 싸우면 세진다"* 가 아니라 **"많이 찾으면 세진다"** 가 됩니다.
   *
   * ── ⚠️ **이 자리에 틀린 설명이 오래 서 있었습니다** ────────────────
   *
   * 여기에는 이렇게 적혀 있었습니다:
   *
   *     "이 존에는 보물 5개 + 보스 1이 있어 최대 7개를 얻습니다.
   *      누적 필요량이 1·2·4·6·9 이므로 한 존을 다 털어도 +4까지입니다."
   *
   * 그 문장을 쓴 날에는 맞았습니다. 그 뒤로 **지도가 자랐습니다** —
   * 지금 이 존의 상자는 5개가 아니라 **11개**입니다(놓인 것 8 + 항아리
   * 속 3). 그래서 정련석 공급은 7이 아니라 **13**이고, 만렙 필요량 9를
   * 이미 넘습니다. 설명은 그대로였고 숫자만 움직였습니다.
   *
   * 이게 왜 치명적인가 — 바로 위에 적은 대로 **불티는 무한합니다.**
   * 쉬면 적이 되살아나고 되살아난 적도 불티를 줍니다(`respawnFloor`
   * 아래로는 안 내려갑니다). 그러니 *"파밍으로는 만렙이 안 된다"* 를
   * 지키는 문턱은 **정련석 하나뿐**이었고, 공급이 필요량을 넘은 순간
   * 그 약속이 통째로 거짓이 됐습니다. 같은 자리를 오래 돌면 +5 가
   * 나옵니다 — 이 표 전체를 만든 이유가 사라진 상태였습니다.
   *
   * ── 왜 마지막 한 단계만 크게 올렸는가 ─────────────────────────────
   * 1·1·2·2·3 은 다섯 단계가 **다 비슷한 물건**이라고 말합니다. 그래서
   * 상자가 늘 때마다 다섯 단계가 한꺼번에 헐거워집니다.
   *
   * 엘든 링의 단조석이 그 반대로 생겼습니다 — 앞의 단계는 흔한 돌로
   * 오르고, **마지막 한 칸만 고룡의 단조석**이라 성격이 아예 다릅니다.
   * 그러면 "만렙"이 단계 하나가 아니라 **사건**이 됩니다.
   *
   * 그래서 1·1·2·3·**9** (누적 1·2·4·7·16):
   *   · +1~+4 는 **불티**가 정합니다. 이 존의 공급 13 으로 +4(7)까지
   *     정련석은 남고, 실제로 막는 것은 불티입니다(한 바퀴 608 → +3).
   *     **싸운 만큼** 오르는 구간입니다.
   *   · +5 는 **정련석**이 정합니다. 16 > 13 이라 한 존을 다 털고
   *     무한히 파밍해도 안 됩니다. **찾은 만큼** 오르는 구간입니다.
   * 둘이 서로 다른 구간을 맡으므로 한 칸에 담기지 않습니다.
   *
   * 벽이 아니라 눈금인지도 같이 봤습니다: 두 존이면 26 ≥ 16 이라
   * 다음 존 초반에 닿습니다. *"아무도 못 넘는 문턱은 눈금이 아니라
   * 벽"* 이라는 이 저장소의 규칙에 걸리지 않습니다.
   *
   * ── ⚠️ **이 값은 다시 틀어집니다** ────────────────────────────────
   * 상자를 세 개만 더 놓으면 공급이 16이 되어 또 깨집니다. 그래서 이번엔
   * 숫자를 고치고 끝내지 않고 **`npm run economy` 를 만들었습니다.**
   * 그 검사가 공급(지도)과 필요량(이 표)을 매번 같이 읽고 비교합니다 —
   * 이 주석이 다시 거짓이 되는 날 **빨간불로 알려 줍니다.**
   * 설명을 고치는 것으로는 같은 일이 또 일어납니다.
   */
  stoneCosts: [1, 1, 2, 3, 9] as const,
  /** 보물 하나가 주는 정련석 */
  stonePerTreasure: 1,
  /** 보스 처치가 주는 정련석 — 존의 마지막 보상 */
  stonePerBoss: 2,
} as const

/**
 * ── 화톳불 (체크포인트) ──────────────────────────────────────────
 *
 * 성수병만 있으면 "한 번 쓰고 끝"인 소모품이라 아무 의미가 없습니다.
 * 보충 지점이 있어야 **"돌아갈까 밀고 갈까"** 라는 판단이 생깁니다.
 *
 * ── 쉬면 적이 되살아납니다 ───────────────────────────────────────
 * 이 한 줄이 없으면 "화톳불 왕복 = 공짜 회복"이 되어, 위에서 애써 만든
 * 판단이 통째로 사라집니다. 적이 되살아나면 되돌아가는 것 자체에 비용이
 * 생기고, 그제서야 "밀고 갈까"가 진짜 선택이 됩니다.
 *
 * ── 왜 별도 키가 아니라 접근인가 ────────────────────────────────
 * 남는 키가 마땅치 않기도 하지만, 더 큰 이유는 **적이 근처에 없어야만**
 * 쉴 수 있게 하고 싶었기 때문입니다. 조건을 "가만히 서 있기"로 두면
 * 전투 중 회복이 자연히 막히고, 키 안내를 배울 필요도 없습니다.
 */
export const BONFIRE = {
  /** 이 거리 안에 들어와야 쉴 수 있습니다. */
  radius: 2.4,
  /** 이만큼 가만히 있어야 휴식이 됩니다. 지나가다 실수로 켜지지 않게. */
  restTime: 0.8,
  /** 이 거리 안에 쫓아오는 적이 하나라도 있으면 쉴 수 없습니다. */
  safeRadius: 14,
} as const

/**
 * 사다리에 손이 닿는 거리(m).
 *
 * 화톳불(2.4m)보다 넓게 잡은 이유: 사다리는 **절벽 경계선**에 있어서,
 * 좁게 잡으면 낭떠러지 끝에 정확히 서야만 안내가 뜹니다. 쿼터뷰에서
 * 자기 발끝이 어느 칸에 있는지는 생각보다 안 보입니다.
 * 대신 칸 판정(위 칸/아래 칸)을 함께 걸어서, 넓혀도 엉뚱한 쪽에서 열리지 않습니다.
 */
/**
 * 🟢 달려드는 자 — **반격을 가르치는 적.**
 *
 * 얽는 자·끄는 자와 같은 원칙: 패턴 하나, 색 하나. 다만 이 적은 다른 셋과
 * 요구하는 것이 정반대입니다 — **물러나지 말고 앞으로.**
 *
 * ── 수치의 근거 ────────────────────────────────────────────────
 * · 키 2.60m: 여섯 종류의 실루엣을 **0.30m 씩** 벌린 자리입니다.
 *   색이 안 보여도 구분되려면 이 간격이 필요합니다(끄는 자를 1.40으로
 *   낮추면서 배운 규칙).
 *
 *   ⚠️ 처음엔 2.25 라고 적고 *"1.40 · 1.70 · 2.05 · 2.90 에서 유일하게
 *   비어 있던 자리"* 라고 썼는데, **그 자리는 비어 있지 않았습니다** —
 *   2.05(얽는 자)와 0.20m 밖에 안 떨어져 있습니다. 규칙을 적어 놓고
 *   그 규칙을 어긴 값을 같은 문단에서 정당화했습니다. 쏘는 자도
 *   1.80 으로 들어와 잡몹(1.70)과 **0.10m** 였습니다.
 *
 *   왜 아무도 못 잡았나: `enemyRoster()` 가 `[잡몹·얽는·끄는·보스]` 라고
 *   **손으로 적혀** 있어서, 나중에 들어온 이 둘이 실루엣 검사에 아예
 *   들어가지 않았습니다. 목록을 데이터에서 돌리도록 고치자마자
 *   `npm run enemies` 가 바로 잡아냈습니다.
 *   → 1.40 끄는 · 1.70 잡몹 · 2.00 쏘는 · 2.30 얽는 · 2.60 달려드는 · 2.90 보스
 *
 *   쏘는 자가 잡몹보다 큰 것이 이상해 보일 수 있는데, 키는 **세기가 아니라
 *   실루엣 칸**입니다. 굵기가 함께 말해 줍니다 — 쏘는 자는 반지름 0.42로
 *   가장 가늘고(길쭉), 달려드는 자는 0.50으로 굵습니다(육중).
 * · 이동 3.4m/s — 잡몹(3.0)보다 빠릅니다. "달려든다"가 이름이자 성격입니다.
 * · 쿨다운 3.2초 — 예고가 1.4초로 길어서, 짧게 두면 반격 예고만 계속 뜹니다.
 * · 체력 46 — 잡몹(58)보다 약합니다. 반격에 성공하면 2.4초가 열리는데,
 *   그 창에서 확실히 죽어야 **"반격이 이득이다"** 가 몸으로 남습니다.
 */
/**
 * 🔴 쏘는 자 — 높은 곳에서 화살을 놓는 적.
 *
 * ── 왜 이제야 넣는가 ────────────────────────────────────────────────
 * DESIGN.md 의 *"아직 안 한 것"* 에 이렇게 적어 뒀습니다: *"우리 적 AI가
 * 절벽을 낀 추격을 어떻게 처리하는지 확인하지 않은 채 넣으면 **영영 못
 * 다가오면서 계속 쏘는 적**이 생깁니다. 먼저 재고 넣습니다."*
 *
 * 그래서 재는 것을 먼저 만들었습니다(`npm run map` — *쏘는 적의 사거리
 * 안에서, 그 적까지 걸어야 하는 거리가 사거리의 3배를 넘지 않는다*).
 * 그 검사를 통과하는 자리에만 세웁니다.
 *
 * ── 수치의 근거 ────────────────────────────────────────────────────
 * · 체력 34 — 가장 약합니다. 붙기만 하면 금방 끝나야 "붙어라"가 답이 됩니다.
 * · 예고 1.25초 — 12m 밖에서 날아오므로 **알아채는 시간**까지 포함합니다
 *   (끄는 자의 1.15초와 같은 근거).
 * · 쿨다운 2.6초 — 계속 맞으면 대응이 아니라 소모가 됩니다. 한 발 피하고
 *   두세 걸음 붙을 수 있는 간격입니다.
 *
 *   ── ⚠️ **2.6 → 1.4** (한 바퀴를 안 세고 정한 값이었습니다) ─────────
 *   `npm run map` 이 오래 빨갛게 서 있었습니다:
 *
 *       🔴 지나가는 동안 2발 이상 쏠 수 있다 — 1.7발
 *          ⛰️ 이 판의 천장 **1.87발** — 어느 칸으로 옮겨도 2발이 안 됩니다
 *
 *   즉 **자리 문제가 아니었습니다.** 계측기가 이미 *"사거리/한 바퀴를
 *   보세요"* 라고 가리키고 있었는데, 여러 회차 동안 자리만 만졌습니다.
 *
 *   ── 문서 안에 모순이 있었습니다 ────────────────────────────────
 *   위 「두세 걸음」 근거는 **붙기로 한 사람**을 봅니다. 그런데 map 의
 *   검사는 **그냥 지나가는 사람**을 봅니다. 그리고 이 적의 정답이
 *   「붙어라」이려면 **무시하는 데 대가가 있어야** 합니다 — 한 발은
 *   사고고, 두 발부터 *"저걸 처리해야겠다"* 가 됩니다. 대가가 없으면
 *   붙는 것은 선택이 아니라 취향입니다.
 *
 *   ── 왜 쿨다운인가 ─────────────────────────────────────────────
 *   발수를 정하는 것은 **한 바퀴**입니다:
 *
 *       한 바퀴 = 쿨다운 + 평균(저격 2.29 · 큰 한 발 3.21)
 *               = 2.6 + 2.75 = **5.35초**   (필요: 8.9 ÷ 2 = 4.45 이하)
 *
 *   사거리(12m)는 **발수를 안 정합니다** — 발수를 정하는 것은 깨는
 *   거리(19m) 안의 동선 길이이고, 그건 종류별 규칙이라 이 적만
 *   고칠 수 없습니다. 그러니 남는 손잡이는 한 바퀴뿐입니다.
 *
 *   ── 그리고 원래 근거는 **쿨다운이 아니라 화살 사이 간격**이 지킵니다 ──
 *   플레이어가 느끼는 간격은 쿨다운이 아니라 `후딜 + 쿨다운 + 예고` 입니다:
 *
 *       2.6 → 4.75초 (그동안 붙을 수 있는 거리 25.6m)
 *       1.4 → **3.55초** (19.2m)
 *
 *   19m 면 *"두세 걸음"* 을 한참 넘깁니다. 원래 근거는 그대로 삽니다.
 *
 *   ⚠️ **1.7 로 안 잡습니다.** 1.7이면 한 바퀴가 4.45로 **문턱과 정확히
 *      같아집니다** — 「한 칸 차이의 초록은 운이다」. 1.4 는 4.15초라
 *      2.14발이고, 동선이 조금 흔들려도 2발이 유지됩니다.
 * · 피해 13 — 잡몹(14)보다 낮습니다. 무서운 것은 한 대가 아니라 **누적**이고,
 *   붙는 동안 여러 발을 맞기 때문입니다.
 * · keepDistance 7 — 붙으면 물러납니다. 다만 이동 2.6m/s 로 플레이어보다
 *   훨씬 느려서, 쫓으면 반드시 잡힙니다(얽는 자와 같은 규칙).
 */
export const ARCHER = {
  maxHp: 34,
  radius: 0.42,
  height: 2.0,
  moveSpeed: 2.6,
  turnSpeedDeg: 130,
  backReactionDelay: 0.5,
  aggroRange: 55,
  attackRange: 12,
  attackCooldown: 1.4,
  windup: 1.25,
  active: 0.14,
  recovery: 0.9,
  damage: 13,
  attackArcDeg: 22,
  attackReach: 12,
  knockback: 2.2,
  hurtStagger: 0.26,
  separationForce: 7.5,
  keepDistance: 7,
} as const

export const CHARGER = {
  maxHp: 46,
  radius: 0.5,
  height: 2.6,
  moveSpeed: 3.4,
  /**
   * ── 접근 속도를 줬다가 **되돌렸습니다** (3판 벤치가 답했습니다) ──────
   *
   * `npm run bypass` 로 "싸우지 않고 종주"를 재보니 달리는 플레이어는 한 대도
   * 안 맞았고, 명단을 보니 가장 빠른 적이 3.4 m/s — 달리기(8.4)의 41% 였습니다.
   * 「달려드는 자」가 조깅하는 사람도 못 따라잡았습니다. 그래서 사거리 밖에서만
   * 걸리는 배율 1.75(→5.95 m/s)를 줬습니다. 전투 속도는 안 건드렸으니
   * **전투 지표는 정의상 안 변한다**고 적었습니다.
   *
   * 틀렸습니다. 3판씩 벤치를 돌려 보니:
   *
   *              깨어 있던 시간   예고 → 판정        경직 비중
   *   전(3.4)      16.7초        5회 → 3회 (끊김 40%)   3%
   *   후(5.95)      5.8초        4회 → **0회** (끊김 100%)  23%
   *
   * **판정이 한 번도 안 떴습니다.** 3판 내내 0회입니다.
   *
   * 이유는 속도가 아니라 **어디서 예고를 시작하느냐**였습니다. 느리게 걸어올
   * 때는 사거리(5.5m) 언저리에서 예고를 시작해서, 1.4초 동안 플레이어가
   * 다가오는 사이에 휘두름이 완성됐습니다. 빨리 붙으면 **이미 코앞에서**
   * 예고를 시작하고, 그 1.4초 동안 플레이어의 콤보가 3~4대 들어가
   * 강인도(45)가 매번 무너집니다.
   *
   * 즉 접근 속도는 "전투와 무관한 값"이 아니었습니다. **교전이 시작되는
   * 거리**를 바꾸고, 그 거리가 예고의 생사를 정합니다. 전투 속도를 안
   * 건드렸으니 전투가 안 변한다는 추론이 통째로 틀렸습니다.
   *
   * 돌진(`lungeSpeed`)은 남겨 둡니다 — 그건 다른 일을 합니다. 지나쳐 가는
   * 플레이어를 잡는 것은 접근 속도가 아니라 예고 중의 돌진이고, 그쪽은
   * `npm run charge` 가 실제로 닿는 것을 확인했습니다.
   * (기능 자체는 `EnemyDef.approachSpeedScale` 에 남아 있습니다. 언젠가
   *  예고가 짧은 적에게는 맞을 수 있습니다 — 이 적에게 안 맞았을 뿐입니다.)
   */
  turnSpeedDeg: 140,
  backReactionDelay: 0.5,
  aggroRange: 55,
  attackRange: 5.5,
  attackCooldown: 3.2,
  windup: 1.4,
  active: 0.16,
  recovery: 1.15,
  damage: 16,
  attackArcDeg: 55,
  attackReach: 5.5,
  knockback: 6,
  hurtStagger: 0.24,
  separationForce: 7.5,
} as const

/**
 * ── 🥋 집중(集中) — 검은 신화: 오공 ──────────────────────────────
 *
 * ── 무엇이 비어 있었나 ─────────────────────────────────────────
 * 지금 우리 기본 공격(좌클릭)에는 **아무 판단이 없습니다.** 스킬 쿨이 도는
 * 동안 채워 넣는 것일 뿐입니다. 그리고 구르기는 **순수한 방어**입니다 —
 * 잘 굴러도 얻는 것이 "안 맞았다" 뿐입니다.
 *
 * 오공의 집중이 이 둘을 한 번에 고칩니다. 가벼운 공격과 **완벽 회피**로
 * 자원이 쌓이고, 그것을 강타 한 방에 태웁니다. 그 순간 두 가지가 바뀝니다:
 *   · 기본 공격이 "지금 태울까, 3점까지 모을까"라는 **결정**을 갖게 됩니다
 *   · 구르기가 방어가 아니라 **공격 준비**가 됩니다
 *
 * ── 수치의 근거 ────────────────────────────────────────────────
 * · perLightHit 0.34 — 3타 콤보를 다 넣으면 딱 1점입니다. "콤보 하나 = 1점"이
 *   손에 남는 단위가 되어야 세는 것이 부담이 아닙니다.
 * · perPerfectDodge **1.0 → 0.35.** 처음엔 *"완벽 회피 한 번이 콤보 하나와
 *   맞먹는다"* 로 세게 잡았습니다. 이 값이 낮으면 아무도 위험을 감수하지
 *   않는다는 것이 이유였습니다.
 *
 *   ── 그런데 장부가 정반대를 보여 줬습니다 ─────────────────────────
 *   자동 플레이 한 판의 집중 흐름입니다:
 *
 *       번 것 21.0점 — **평타 5.3(25%) · 완벽회피 15.7(75%)**
 *       태운 것 21.0점 · **가득 차서 흘린 것 12.0점(번 것의 57%)**
 *
 *   집중의 4분의 3을 구르기가 벌고, 번 것의 **절반 넘게가 넘쳐서 버려집니다.**
 *   자원이 남아도니 강타를 아무 때나 쓸 수 있고, 그래서 **평타를 칠 이유가
 *   없습니다.** 같은 판의 잡몹 장부가 그 결과입니다 — 강타 40% · 스킬 26% ·
 *   **평타 4%.** 그리고 평타를 안 치니 타수로 쌓이는 출혈은 영영 안 찹니다.
 *   한 줄로 이어지는 이야기였습니다.
 *
 *   ── 이 저장소가 이미 적어 둔 규칙이 답을 갖고 있었습니다 ──────────
 *   저스트 가드를 만들 때 이렇게 적었습니다 — *"보상은 **강인도**입니다.
 *   완벽 회피(다음 한 대 확정 치명타)와 **다른 것을 벌어야** 두 답이 각자
 *   값을 갖습니다."* 그런데 완벽 회피는 **둘 다** 받고 있었습니다:
 *   확정 치명타 창 **그리고** 집중 1점.
 *
 *   그래서 완벽 회피의 몫을 **즉시 보상(확정 치명타)** 쪽에 두고, 집중은
 *   **평타가 버는 자원**으로 되돌립니다 — 오공의 경파(輕棍)→중격 그대로입니다.
 *   0 으로 만들지 않은 이유: 위험을 감수한 값이 **아예 0** 이 되면 예전 문제로
 *   되돌아갑니다. 평타 한 대(0.34)와 같은 값으로 두면 *"잘 굴러도 한 대
 *   친 만큼"* 이라, 자원의 주된 통로는 평타가 되고 회피는 덤이 됩니다.
 * · max 3 — 오공과 같습니다. 상한이 없으면 "다 모을 때까지 안 쓴다"가
 *   최적이 되어 매 순간의 판단이 사라집니다.
 * · damagePerPoint 0.85 — 1점 강타는 콤보 마무리의 1.85배, 3점이면 3.55배.
 *   선형으로 둔 이유: 제곱으로 두면 **기다리기가 정답**이 되어 매 순간의
 *   판단이 사라집니다.
 *
 *   ❌ **그런데 이 자리에 오래 틀린 문장이 붙어 있었습니다** — *"모아 쓰는
 *   것이 살짝 이득이되"*. 식을 보면 정반대입니다(arsenal.ts:
 *   `last.damage * (1 + focusSpent * damagePerPoint)`):
 *
 *       1점씩 세 번 = 3 × 1.85 = **5.55배**
 *       3점 한 번   = 1 × 3.55 = **3.55배**    → 나눠 쓰는 쪽이 **56% 이득**
 *
 *   기반값 1이 매 발동마다 붙기 때문입니다. 즉 이 수치는 *"자주 쓰는 것이
 *   정답"* 으로 **의도대로 잘 설계돼 있었고**, 틀린 것은 그 옆의 설명뿐
 *   이었습니다. 모아 쓰는 것이 사는 것은 피해가 아니라 **타이밍**입니다 —
 *   지금 이 한 번에 큰 것을 꽂을 수 있다는 것.
 *
 *   ⚠️ 그런데 **자동 플레이 봇이 그 틀린 문장을 그대로 따르고 있었습니다** —
 *   *"가득 찼을 때만 태운다"*. 그래서 집중의 절반 넘게가 넘쳐 버려졌고
 *   (번 것의 57~68%), 저는 그 숫자를 하마터면 **게임의 결함**으로 읽을
 *   뻔했습니다. 계측기가 주석을 베껴 쓰면, 주석의 오류가 측정값이 됩니다.
 *   이제 `npm run focus` 가 이 부등식을 **게임의 수치로 직접** 검사합니다.
 * · windup 0.42 — 콤보보다 확실히 깁니다. 오공의 강타가 재밌는 이유의 절반은
 *   **모아 놓고도 맞을 수 있다**는 데 있습니다. 커밋 없는 큰 한 방은 공짜입니다.
 */
export const FOCUS = {
  max: 3,
  /** 기본 공격 한 대가 채우는 양. 3타 콤보 = 1점. */
  perLightHit: 0.34,
  /** 완벽 회피 한 번 = 평타 한 대와 같은 값(위 설계 노트 참고). */
  perPerfectDodge: 0.35,
  /**
   * ── 완벽 회피 뒤에 열리는 **확정 치명타 창**(초) ──────────────────
   *
   * 집중 +1 만으로는 그 순간에 손에 잡히는 것이 없었습니다. 자원은
   * **나중에** 쓰는 것이라, 잘 굴렀다는 사실이 화면에서 곧바로 보상되지
   * 않습니다. 완벽 회피가 재미있는 게임들은 전부 **즉시** 무언가를 줍니다 —
   * 오공은 후속타(섬), 베요네타는 시간 감속, NRFTW 는 반격 창.
   *
   * 우리는 이미 있는 것으로 줍니다: **다음 한 대가 확정 치명타**(×1.8).
   * 새 상태도 새 키도 만들지 않고, 화면에는 이미 있는 치명타 숫자가
   * 뜨므로 **배울 것이 늘지 않습니다.**
   *
   * ── 길이는 **재서** 정했습니다 (처음 1.2초는 틀렸습니다) ───────────
   *
   * 처음엔 "구르기 0.42 + 돌아서 한 대(약 0.5)" 로 1.2초를 잡았습니다.
   * 그 계산에는 빠진 항이 있었습니다: **구르면 멀어집니다.** 이동 입력
   * 없이 구르면 등 뒤로 4.2m 를 가는데 무기 사거리는 그보다 짧습니다.
   *
   * `npm run focus` 가 실제로 잰 값(더미 상대, 뒤로 구른 최악의 경우):
   *
   *   구르기 0.42 → 4.2m 밖 → 다시 1.76m 까지 **0.88초** → 선행동작 0.12
   *   ─────────────────────────────────────────────────
   *   합계 약 1.42초
   *
   * 1.2초로는 0.02초를 남기고 선행동작 도중에 창이 닫혔습니다 —
   * 치명타 0회. 즉 **설계상 받을 수 없는 보상**이었습니다.
   *
   * 그래서 1.8초입니다. 최악의 경우(뒤로 구르기)에 0.4초 정도의 여유가
   * 남는 값입니다. 앞으로 굴러 파고든 사람은 0.2초면 넣으므로 훨씬
   * 넉넉하고, 그게 맞습니다 — 잘 굴러 붙은 쪽이 손해 볼 이유는 없습니다.
   *
   * 위로는 열어두지 않았습니다. 굴러 놓고 딴 데 갔다가 돌아와서도 남아
   * 있으면 "굴렀으니 바로 친다"가 아니라 그냥 상시 버프가 됩니다.
   */
  perfectDodgeCritWindow: 1.8,
  /** 강타 피해 = 콤보 마무리 × (1 + 태운 점수 × 이 값) */
  damagePerPoint: 0.85,
  /** 강타 강인도 피해 배수 — 큰 한 방은 자세를 무너뜨려야 합니다. */
  poiseMult: 2.2,
  heavy: {
    windup: 0.42,
    active: 0.14,
    recovery: 0.62,
    staminaCost: 22,
    /** 콤보 마무리 대비 사거리·각도 배수 */
    rangeMult: 1.18,
    arcAdd: 18,
    knockback: 7.5,
    hitstop: 0.11,
    trauma: 0.62,
    lunge: 1.4,
  },
} as const

/**
 * ── 방 단위 어그로 (레벨 모드 전용) ──────────────────────────────
 *
 * ── 측정이 먼저였습니다 ─────────────────────────────────────
 * 존이 쉬운 이유를 재보니 **동시 교전이 평균 1.22마리**였습니다.
 * 사실상 연속된 1:1입니다. 그런데 적의 어그로 범위는 **55m** 이라
 * 앞뒤가 안 맞습니다 — 다 깨어나 있어야 하는데요.
 *
 * 둘 다 사실이었습니다. 55m면 존의 거의 모든 적이 동시에 깨어나
 * 플레이어를 향해 걷기 시작하는데, 저마다 거리와 속도가 달라서
 * **한 줄로 늘어서 도착합니다.** 조합이 아니라 줄서기가 되는 것입니다.
 * 그러면 아무리 여럿을 배치해도 매번 1:1이고, 소울라이크 위험의
 * 대부분인 *"한 놈에게 커밋한 사이 두 번째가 닿는 것"* 이 안 생깁니다.
 *
 * balance.ts 의 GRUNT.aggroRange 주석에 제가 이미 답을 적어 뒀습니다 —
 * *"나중에 던전 콘텐츠를 만들 때는 방 단위로 작은 값을 쓰게 됩니다."*
 *
 * ── 왜 아레나는 그대로 55m 인가 ─────────────────────────────
 * 아레나는 전투 시험장이라 적이 반경 21m 지점에 소환됩니다. 좁히면
 * 어그로 범위 밖이라 **영원히 제자리에 서 있게** 됩니다(예전에 실제로
 * 잡았던 버그). 그래서 레벨 모드에서만 덮어씁니다.
 *
 * ── 14m 인 근거 ─────────────────────────────────────────────
 * · 카메라가 세로로 담는 높이가 22m 입니다. 그보다 좁아야 **화면에 들어온
 *   뒤에** 깨어납니다 — 안 보이는 곳에서 걸어오는 적은 읽을 수가 없습니다.
 * · 우리 조합은 3~6m 간격으로 묶습니다. 14m면 무리 하나가 통째로 깨어나되
 *   옆 무리는 안 건드립니다.
 * · 화톳불 안전 반경(14m)과 같은 값입니다 — "쉴 수 있다 = 아무도 안 깨어난다"가
 *   저절로 성립합니다.
 */
export const LEVEL_AGGRO_RANGE = 14

/**
 * ── 원거리 적에게 주는 **여유 거리** ──────────────────────────────
 *
 * 위 14m 의 근거를 다시 읽어 보면 **전부 근접 적 이야기**입니다 —
 * 무리 간격 3~6m, 화톳불 반경, 카메라. 사거리가 긴 적은 계산에 없었습니다.
 *
 * 그래서 이런 일이 벌어져 있었습니다:
 *   잡몹   사거리 2.5m · 어그로 14m → 깨고 나서 **11.5m 를 걸어옵니다**
 *   쏘는 자 사거리  12m · 어그로 14m → 여유 **2m**
 *
 * 근접 적에게는 "걸어오는 시간"이 저절로 여유가 됩니다. 원거리 적은
 * 걸어올 필요가 없으니 그 여유가 **어그로 거리 안에 들어 있어야** 합니다.
 * 지금은 플레이어가 이미 코앞에 온 뒤에야 깨어나서, 한 발 쏘고 죽습니다
 * (자동 플레이 3판: 판당 예고 **1회** · 깨어 있던 시간 **5.8초**).
 *
 * ── 어느 "사거리"인가 — **두 번 틀린 자리** ──────────────────────
 * 이 값을 넣으면서 근거를 *"근접 최대 사거리가 끄는 자 6.5m 라 6.5+7=13.5
 * < 14, 그러니 근접은 안 바뀐다"* 라고 적었습니다. **문장은 맞는데 코드가
 * 딴 걸 읽고 있었습니다.**
 *
 *   · `cfg.attackRange` — 이 적이 **달려들기 시작하는** 거리
 *   · 패턴의 `reach`   — 이 적이 **실제로 때리는** 거리
 *
 * 끄는 자는 attackRange **12**, reach **6.5** 입니다. 저는 6.5 를 생각하며
 * 12 를 읽는 코드를 썼고, 그래서 끄는 자 어그로가 조용히 넓어졌습니다.
 * `npm run encounter` 의 *"한 무리를 깨워도 존 전체가 따라오지 않는다"* 가
 * 잡았습니다 — 깨어난 적이 5마리에서 **7마리**로 늘었습니다.
 * (그 전에 `keepDistance` 유무로 거르는 판도 있었는데, 얽는 자·끄는 자도
 *  물러나는 적이라 똑같이 걸렸습니다. 두 번 다 **필드를 잘못 골랐습니다.**)
 *
 * 재야 할 것은 *"안 다가와도 나를 때릴 수 있는 거리"* 이므로 **reach** 입니다.
 *
 * ── 7m 인 근거 ─────────────────────────────────────────────────
 * reach + 7 로 걸면 지금 값들이 이렇게 갈립니다:
 *   잡몹 4.6→11.6 · 달려드는 자 5.5→12.5 · 얽는 자 6.0→13.0 ·
 *   끄는 자 6.5→13.5   … 전부 14 미만이라 **그대로**입니다.
 *   쏘는 자 12→**19m** — 이 값이 실제로 드는 유일한 적입니다.
 *
 * ── ⚠️ **위 표는 낡았습니다** (남겨 두는 이유는 아래에) ───────────
 * 게임에게 직접 물어본 지금 값입니다(`enemyAI.wakeRangeOf`):
 *
 *     잡졸 14m · 달려드는 자 14m · **얽는 자 15m · 끄는 자 19m** · 쏘는 자 19m
 *
 * 끄는 자는 쏘는 자와 **같은 19m** 입니다. 위 표가 *"끄는 자 reach 6.5"*
 * 라고 적힌 뒤에 `dragger_hook` 의 사거리가 **12m** 로 늘었고, 아무도 이
 * 표를 안 고쳤습니다. 얽는 자도 마찬가지로 8m 짜리 패턴이 생겼습니다.
 *
 * 그래서 *"원거리 적은 쏘는 자 하나뿐"* 이라는 이 절의 결론도 이제
 * 사실이 아닙니다 — **끄는 자가 두 번째 원거리 적**입니다.
 *
 * 표를 지우지 않고 남기는 이유: 이 값을 **7** 로 정한 근거가 저 계산이고,
 * 근거를 지우면 다음 사람이 값을 바꿀 때 무엇을 다시 확인해야 하는지
 * 모릅니다. 다만 **지금 값으로 읽으면 안 된다**는 것을 여기 적어 둡니다.
 *
 * ⚠️ 프로브가 이 표를 보고 *"근접은 전부 14m"* 라고 반지름 하나로 세고
 *    있었습니다(`npm run map`). 그쪽은 종류별로 갈랐습니다 — 숫자를
 *    베끼지 말고 `enemyRoster().wakeRange` 를 읽으십시오.
 * 즉 7m 는 "근접을 안 건드리는 최대치"(끄는 자 기준 7.5)에서 한 뼘 물러난
 * 값입니다. 여유를 더 주고 싶으면 **수치가 아니라 카메라와 배치**를
 * 다시 봐야 합니다.
 * · 7m 는 플레이어 걸음(5.4m/s)으로 1.3초입니다. 예고 1.25초짜리를 한 번
 *   더 돌릴 여유가 생깁니다 — "붙는 동안 여러 발"이라던 설계대로요.
 */
export const LEVEL_AGGRO_LEAD = 7
/** 어그로가 넘을 수 없는 천장 — 카메라가 세로로 담는 높이(위 주석). */
export const LEVEL_AGGRO_MAX = 22

/**
 * ── 🚪 **깨울 때만 걸어서 재고, 깬 뒤로는 안 물었습니다** ────────────────
 *
 * `enemyAI` 는 적을 깨울 때 **걸어야 하는 거리**를 씁니다(직선으로 재면
 * 벽 건너 적이 깨어나 영원히 벽을 향해 걷기 때문 — 그 자리 주석에 실측이
 * 있습니다). 그런데 그 질문을 **한 번밖에** 안 합니다. 한 번 깬 적은
 * 그 뒤로 영원히 직선거리로만 삽니다.
 *
 * 자동 플레이가 그 결과를 찍었습니다:
 *
 *     막힘 @무너진 회랑 (24,3) — 가까운적 **6.0m** · **경로 78m**
 *     그 적의 상태 {"aggro":true,"attacking":true,"attackId":"charger_rush"}
 *
 * 벽 건너 6m 에서 **돌진 예고를 띄우고 있었습니다.** 사람에게는 화면에
 * 빨간 예고가 떠 있는데 그 공격이 **영원히 안 오는** 것으로 보입니다.
 * 기둥 2 는 *"예고를 읽어 대응한다"* 인데, 대응할 수 없는 예고는 난이도가
 * 아니라 **노이즈**입니다. 읽기를 가르치는 게임이 못 읽을 것을 띄우면
 * 플레이어는 예고를 **덜** 믿게 됩니다.
 *
 * ── 다른 게임은 어떻게 하는가 ────────────────────────────────────
 * 소울류·엘든 링·로스트아크 전부 **길이 끊기면 적이 포기하고 돌아갑니다.**
 * 이 저장소에도 리쉬가 있지만 **보스 아레나 전용**이고, 잡몹은 한 번
 * 깨면 영원히 쫓습니다.
 *
 * ── 왜 「멀면 푼다」가 아니라 「돌아가야 하면 푼다」인가 ─────────────
 * 그냥 거리로 풀면 **도망이 공짜**가 됩니다(달려서 28m 벌리면 전투 종료).
 * 그건 이 존의 설계를 통째로 바꾸는 변경이라 여기서 할 일이 아닙니다.
 * 고칠 것은 거리가 아니라 **지형이 사이에 낀 경우**뿐입니다. 그래서
 * 직선과 경로의 **비(比)** 로 묻습니다 — 실측된 병만 정확히 겨냥합니다:
 *
 *     실측 병      직선  6m · 경로 78m → **13.0배**   ← 풀어야 합니다
 *     모퉁이 하나  직선  8m · 경로 20m →   2.5배      ← 그대로 쫓아야 합니다
 *     열린 곳 카이팅 직선 20m · 경로 22m →   1.1배      ← 그대로
 *
 * 4배는 그 사이에서 **모퉁이 쪽에 붙여** 잡은 값입니다. 지형이 한 번
 * 끼는 정도로는 안 풀리고, 아예 반대편으로 돌아가야 할 때만 풀립니다.
 */
export const LEVEL_DEAGGRO_RATIO = 4
/**
 * 비(比)만으로는 **가까운 곳에서 헛돕니다.** 직선 1m · 경로 5m 는 5배지만
 * 그냥 기둥을 끼고 도는 것이고, 적은 반 바퀴만 돌면 닿습니다. 그래서
 * **절대 거리**도 함께 넘어야 풉니다.
 *
 * 20m 근거: 깨우는 거리의 천장이 `LEVEL_AGGRO_MAX`(22m)입니다. 그보다
 * 조금 안쪽으로 잡아, *"어차피 지금 깨울 수도 없을 만큼 걸어야 하는 자리"*
 * 를 뜻하게 했습니다. 두 문턱을 같은 값으로 두지 않은 이유는 **깜빡임**
 * 입니다 — 푸는 문턱이 깨우는 문턱과 같으면 경계에서 매 프레임 켜졌다
 * 꺼집니다(소울류가 어그로 해제 거리를 따로 두는 이유).
 */
export const LEVEL_DEAGGRO_MIN = 20

export const LADDER_REACH = 3.2

/**
 * ── 🟢 반격 (로스트아크의 카운터) ──────────────────────────────────
 *
 * 네 색의 정답을 나란히 놓고 보니 전부 **"피하라"** 였습니다. 구르기,
 * 걸어서 이탈, 무적 프레임, 사거리 밖 — 색은 넷인데 **동사는 하나**였습니다.
 * 그래서 전투의 리듬이 늘 "참고 → 피하고 → 반격" 한 방향으로만 흐릅니다.
 *
 * 로스트아크의 카운터가 이 문제를 정확히 풉니다. 한 색만 **반대 방향**을
 * 요구하기 때문에, 물러나던 몸을 갑자기 앞으로 밀어야 합니다.
 *
 * ── 수치를 이렇게 고른 근거 ────────────────────────────────────
 * · brokenTime 2.4초: 일반 무너짐(1.0초 / 보스 1.9초)보다 **확실히 깁니다.**
 *   같으면 "반격했다"가 특별한 일이 되지 못합니다. 대신 성공 조건이
 *   훨씬 까다롭습니다 — 위험 구역 안에 서서, 정면에서, 예고 중에.
 * · damageMultiplier 1.6: 백어택(1.35)보다 높습니다. 백어택은 안전한 자리를
 *   잡는 기술이고 반격은 **위험한 자리에 남는** 기술이라, 더 줘야 합니다.
 * · 실패해도 벌은 따로 없습니다. 초록을 그냥 구르면 아무 일도 안 일어납니다.
 *   실패에 벌까지 붙이면 초보는 초록이 뜰 때마다 굳습니다 — 새 동사를
 *   가르치는 단계에서는 **시도가 손해가 아니어야** 합니다.
 */
export const COUNTER = {
  /** 반격 성공 시 무방비 시간(초) */
  brokenTime: 2.4,
  /** 반격 타격의 피해 배율 */
  damageMultiplier: 1.6,
  hitstop: 0.18,
  trauma: 0.75,
  /**
   * ── 성공했을 때만 돌려주는 쿨다운 비율 ────────────────────────────
   *
   * NRFTW 를 조사하다 찾은 구멍입니다(`BENCHMARK-NRFTW.md` 2절).
   * 그 게임에서 패링이 특별한 이유는 경직 때문만이 아닙니다 —
   * **성공하면 자원이 거의 안 들어서** 긴 보스전에서 회피보다 우월해집니다.
   *
   * 우리 반격이 쓰는 자원은 스태미나가 아니라 **쿨다운**입니다. 그리고
   * 지금까지는 성공하든 빗나가든 **똑같이 전부 돌았습니다.** 그러면 이런
   * 구조가 됩니다:
   *
   *   구르기 — 빗나가도 기력 25. 다음 초록에 또 대응할 수 있습니다.
   *   반격   — 빗나가면 슬롯 하나가 통째로 죽습니다. 다음 초록엔 답이 없습니다.
   *
   * **위험한 쪽에 보상이 없고 벌만 있는** 모양입니다. 그러면 잘하는
   * 플레이어일수록 반격을 안 쓰게 됩니다 — 가르치려던 새 동사가
   * 숙련될수록 사라지는 셈입니다.
   *
   * 0.6 인 이유: 절반보다 조금 더 돌려줍니다.
   *   · 1.0(전액)이면 반격이 **공짜**가 됩니다. 초록마다 같은 슬롯으로
   *     답하면 되니 슬롯 다섯 개를 쓸 이유가 다시 사라집니다.
   *   · 0.5 이하면 "돌려받았다"가 손에 안 잡힙니다. 8초 쿨이 4초가 돼도
   *     다음 초록(예고 간격이 그보다 짧습니다)에는 여전히 못 씁니다.
   *   0.6 이면 8초 쿨이 3.2초로 남습니다 — **연달아 오는 초록 두 번 중
   *   두 번째에는 닿는** 길이입니다. 즉 "잘하면 계속 반격할 수 있다"가
   *   되고, 못 맞히면 원래대로 슬롯이 죽습니다.
   *
   * ⚠️ 빗나간 스킬은 **그대로 전부** 돕니다. 그 구분이 없으면 이건
   *    반격 보상이 아니라 그냥 쿨다운 단축입니다.
   */
  cooldownRefund: 0.6,
} as const

/**
 * 낙하 — **높이를 전투에 개입시키는 장치.**
 *
 * ── 왜 필요한가 ─────────────────────────────────────────────────
 * 지도를 수직으로 만들어 놓고도 지금까지 절벽은 **순전히 길**이었습니다.
 * 떨어져도 안 아프고, 적을 밀어 떨어뜨려도 아무 일이 없었습니다. 그러면
 * 높이는 지형의 무늬일 뿐 전투의 재료가 되지 못합니다.
 * NRFTW를 조사하면서 계속 나온 것이 "떨어져 죽는다", "밀려서 떨어진다"였고,
 * 실제로 플레이어들이 **불평까지 하는** 항목이었습니다 — 불평한다는 것은
 * 그만큼 존재감이 있다는 뜻입니다.
 *
 * ── 숫자를 이렇게 고른 근거 ─────────────────────────────────────
 * 지금 존에서 플레이어가 실제로 밟을 수 있는 낙차는 세 종류뿐입니다
 * (지형 데이터를 훑어 세었습니다):
 *   · 1단 — 어디에나 있습니다. 걸어서 오르내리는 계단이라 **반드시 공짜**여야 합니다.
 *   · 2단 — 중앙 폐허 → 남쪽 함몰지. **주 동선이 강제하는 낙하**라 공짜여야 합니다.
 *           여기서 아프게 하면 설계된 길이 벌처럼 느껴집니다.
 *   · 3단 — 성벽마루 → 폐허/계단. 여기가 **선택**입니다.
 * 그래서 공짜 한계를 2단으로 두면, 아픈 낙하는 정확히 "안 떨어져도 되는데
 * 떨어지기로 한 곳" 하나만 남습니다.
 *
 * 피해량은 최대 체력 비율입니다. 고정값으로 두면 성수병을 강화한 뒤에도
 * 똑같이 아파서 성장이 낙하에는 반영되지 않습니다.
 */
export const FALL = {
  /** 이 단계까지는 공짜. (지형 한 단계 = HEIGHT_STEP = 0.9m) */
  freeSteps: 2,
  /** 공짜를 넘은 한 단계마다 최대 체력의 이 비율. 3단 = 12%. */
  damagePerStep: 0.12,
  /**
   * 착지하면 **무너집니다**(강인도 즉시 0).
   *
   * 이게 이 시스템의 진짜 보상입니다. 밀어 떨어뜨린 적이 12% 깎이는 것보다,
   * **무방비로 착지해 공짜 딜 창이 열리는 것**이 훨씬 큽니다.
   * 피해를 크게 올려 처형기로 만들지 않은 이유이기도 합니다 — 2.7m 낙하로
   * 적이 즉사하면 전투가 "절벽으로 유인하기" 한 가지로 수렴합니다.
   */
  breaksPoise: true,
  /** 착지 연출 */
  hitstop: 0.05,
  trauma: 0.32,
} as const

/**
 * 🔵 얽는 자 — **원거리 속박만** 겁니다.
 *
 * ── 왜 이 적이 필요한가 ─────────────────────────────────────────────
 * 4색 예고는 이 게임의 어휘인데, 지금까지 🔵와 🟣는 **보스만** 썼습니다.
 * 즉 플레이어는 파랑·보라를 **실수 대가가 가장 큰 싸움에서 처음** 만납니다.
 * 학습 순서가 거꾸로입니다. 튜토리얼은 잡몹이 맡아야 합니다.
 *
 * ── 설계 원칙: 한 적은 한 색만 ──────────────────────────────────────
 * 패턴을 여러 개 주면 "얽는 자는 파랑을 쓴다"가 흐려집니다. 색과 적을
 * 1:1로 묶어야 **적의 실루엣을 보는 순간 대응이 정해집니다.**
 *
 * ── 아프지 않아야 합니다 ────────────────────────────────────────────
 * 피해 6은 오타가 아닙니다. 파랑의 위협은 피해가 아니라 **그 다음**입니다.
 * 속박 자체가 아프면 플레이어는 "아프니까 피한다"를 배우고, 정작
 * "묶이면 다음을 못 피한다"는 진짜 교훈은 못 배웁니다.
 * 잡몹과 **함께** 나올 때 비로소 위협이 됩니다 — 보스의 연계를
 * 스크립트가 아니라 **배치로** 재현하는 셈입니다.
 */
export const BINDER = {
  maxHp: 40,
  radius: 0.38,
  height: 2.3,
  moveSpeed: 2.4,
  turnSpeedDeg: 130,
  backReactionDelay: 0.55,
  aggroRange: 55,
  attackRange: 6.0,
  /**
   * 이 거리보다 가까워지면 **물러납니다.**
   * 없으면 원거리 적이 그냥 걸어 들어와 약한 잡몹이 되어 버립니다.
   */
  keepDistance: 4.5,
  /** 속박은 자주 걸리면 게임이 아니라 형벌이 됩니다. 잡몹의 2배 넘게 쉽니다. */
  attackCooldown: 2.8,
  windup: 0.85,
  active: 0.14,
  recovery: 0.9,
  damage: 6,
  attackArcDeg: 45,
  attackReach: 6.0,
  knockback: 0,
  hurtStagger: 0.3,
  separationForce: 7.5,
} as const

/**
 * 🟣 끄는 자 — **아주 먼 거리에서 끌어당기기만** 합니다.
 *
 * 이 적의 존재 이유는 하나입니다: **거리가 안전지대가 아니게 만들기.**
 * 지금 플레이어는 곤란하면 뒤로 빠지면 그만입니다. 12m까지 닿는 갈고리가
 * 있으면 "물러난다"가 공짜가 아니게 되고, 화면 구석의 적을 무시할 수
 * 없게 됩니다.
 *
 * 예고 1.15초는 게임에서 가장 깁니다. 가장 멀리서 오는 공격이라
 * **알아채는 데 드는 시간**까지 예고에 포함해야 공정합니다.
 * (가까운 적은 화면 중앙에 있어 바로 보이지만, 12m 밖은 눈이 가야 보입니다.)
 */
export const DRAGGER = {
  maxHp: 34,
  /**
   * 넓고 낮은 실루엣.
   *
   * **측정으로 고친 값입니다.** 처음엔 1.55m 였는데 잡몹(1.70m)과 0.15m 차이라
   * 화면에서 4px 남짓이었습니다 — 색이 안 보이는 사람에게는 사실상 같은 적입니다.
   * 1.40m 로 낮추고 대신 굵게 해서, 네 종류의 키 간격이 최소 0.30m 가 됐습니다.
   * (1.40 · 1.70 · 2.05 · 2.90)
   */
  radius: 0.55,
  height: 1.4,
  moveSpeed: 2.0,
  turnSpeedDeg: 120,
  backReactionDelay: 0.6,
  aggroRange: 55,
  attackRange: 12,
  keepDistance: 8,
  /** 4초. 끌기가 잦으면 플레이어가 조작권을 잃습니다. */
  attackCooldown: 4.0,
  windup: 1.15,
  active: 0.16,
  recovery: 1.1,
  damage: 4,
  attackArcDeg: 40,
  attackReach: 12,
  knockback: 0,
  hurtStagger: 0.32,
  separationForce: 7.5,
} as const

/**
 * 포지셔닝 — 백어택과 치명타 (DESIGN.md 기둥 3).
 *
 * 이 기둥의 목적은 하나입니다: **이동을 "도망"이 아니라 "공격의 일부"로 만들기.**
 * 등 뒤를 치면 크게 아프기 때문에, 회피 구르기와 그림자 도약이
 * "안 맞으려고 쓰는 것"에서 "때리려고 쓰는 것"으로 바뀝니다.
 * 소울라이크의 '회피 후 반격'과 로스트아크의 '백어택'이 여기서 하나로 맞물립니다.
 *
 * 적 -> 플레이어 방향으로는 적용하지 않습니다.
 * 적이 여럿이면 플레이어는 한쪽만 볼 수 있어서, 뒤에서 오는 추가 피해가
 * "실력으로 막을 수 있는 것"이 아니라 **피할 수 없는 소모**가 되기 때문입니다.
 * (막기 방향 같은 대응 수단이 생기면 그때 대칭으로 되돌립니다.)
 */
export const COMBAT = {
  /**
   * 등 뒤 판정 부채꼴(도). 적의 후방 이 각도 안에서 때리면 백어택.
   * 120°는 좁아서 조금만 비껴도 놓쳤습니다. 140°면 "대충 뒤쪽"이면 들어갑니다.
   */
  backArcDeg: 140,
  /** 백어택 피해 배율 */
  backDamageMult: 1.55,
  /** 기본 치명타 확률 */
  baseCritChance: 0.08,
  /** 백어택일 때 더해지는 치명타 확률 */
  backCritBonus: 0.45,
  /** 치명타 피해 배율 */
  critMult: 1.8,
  /**
   * ── 읽기의 **등급**을 손끝으로 나눕니다 ────────────────────────
   *
   * 이 게임의 재미가 무엇인지 정하고 나서 생긴 값입니다. 답은
   * **컨트롤**이었고(DESIGN.md 「이 게임의 재미는 컨트롤이다」), 그러면
   * 손맛이 할 일은 하나로 정해집니다:
   *
   *   > 손맛은 목표가 아니라 **컨트롤의 영수증**입니다.
   *   > "네가 제대로 읽고 눌렀다"를 몸으로 알려 주는 것.
   *
   * 그런데 재 보니 **백어택만** 그 대접을 받고 있었습니다. 치명타(피해
   * 1.8배)도, 완벽 회피 뒤의 확정타도 **평타와 똑같은 정지·흔들림**으로
   * 왔습니다. 숫자만 다르고 **몸으로는 구분이 안 됐습니다.** 세키로의
   * 완벽 튕겨내기와 오공의 완벽 회피가 다른 감각으로 오는 이유가 정확히
   * 이것입니다 — 읽어 낸 것과 얻어걸린 것이 같은 느낌이면, 읽을 이유가
   * 손에 남지 않습니다.
   *
   * ── 단 크기를 지어내지 않았습니다 ──────────────────────────────
   * 한 단 = **0.035초**. 이건 이 저장소가 이미 "느껴진다"고 판단해 백어택에
   * 쓰고 있던 값입니다. 새 숫자를 만들면 "왜 0.05인가"에 답할 수 없지만,
   * 이미 쓰던 값을 단위로 삼으면 답할 수 있습니다.
   *
   * ── ⚠️ 처음엔 반격·처형에도 단을 얹으려다 **재 보고 접었습니다** ──
   * 히트스톱은 `requestHitstop` 이 **가장 큰 값 하나만** 채택합니다
   * (core/time.ts). 그래서 이미 전용 연출값을 가진 두 동작은 단을 얹어도
   * 소용이 없거나, 오히려 화면이 너무 오래 멎습니다:
   *
   *   반격  COUNTER.hitstop 0.18 이 어차피 이깁니다 → 얹어도 **아무 일도 안 남**
   *   처형  FINISHER.hitstop 0.16 + 2단 = 0.23초 → time.ts 가 권하는
   *         대역(0.05~0.14)을 한참 넘습니다. **영수증이 게임을 멈추면
   *         그건 보상이 아니라 방해입니다.**
   *
   * 즉 둘은 **이미 자기 몫을 따로 받고 있었고**, 비어 있던 것은 가운데
   * 칸이었습니다. 등급이 채우는 것은 그 가운데뿐입니다:
   *
   *   0단 평타            0.055초 — 그냥 휘둘렀습니다
   *   1단 백어택·치명타    0.090초 — **자리**를 잡았습니다
   *   2단 완벽 회피 확정타  0.125초 — **예고를 읽고** 정확한 답을 냈습니다
   *   (처형 0.160 · 반격 0.180 은 각자의 상수가 이어서 올려 줍니다)
   *
   * 사다리 전체가 오르막인지는 `npm run feel` 이 **실제 화면 정지 시간을
   * 재서** 확인합니다. 위 숫자를 프로브에 베껴 넣지 않았습니다.
   */
  feelStep: 0.035,
  feelTraumaStep: 0.12,
  /**
   * 이 거리 안으로 들어가면 적의 등 뒤 구역이 지면에 표시됩니다.
   * 항상 켜두면 화면이 지저분해지고, 없으면 백어택이 운처럼 느껴집니다.
   */
  backIndicatorRange: 5.5,
} as const

/**
 * ── 🤕 **맞는 쪽의 반응** ─────────────────────────────────────────────
 *
 * ── 여기 무엇이 비어 있었나 ──────────────────────────────────────────
 * 이 저장소는 타격의 무게를 **때리는 쪽에만** 실어 왔습니다:
 *
 *   손끝  히트스톱 0.035 → 0.15 (무기·콤보 단계마다 다름)
 *   화면  트라우마 0.14 → 0.7
 *   궤적  `swingPower` 0 → 1 (초승달이 굵어지고 색이 바뀜)
 *
 * 그런데 **맞은 적은 전부 똑같이 반응했습니다.** 단검 1타(7 피해)도,
 * 대검 마무리(46 피해)도 `flashT = 0.12` 한 줄이었습니다. 게다가 그
 * `0.12` 는 **세 파일에 각자 적혀** 있었습니다 — combat.ts 가 넣고,
 * main.ts(낙하)가 또 넣고, visuals.ts 가 그 값으로 나눠서 밝기를
 * 만들었습니다. 한쪽만 고치면 나머지 둘이 조용히 어긋납니다.
 *
 * ── 왜 이게 「타격감」의 핵심인가 ─────────────────────────────────────
 * 참고한 게임들의 공통점은 *"세게 때렸다"를 **맞은 쪽이** 말해 준다*는
 * 것입니다:
 *
 *   · 오공 — 봉세를 채운 한 방에 적이 **꺾이며** 뒤로 접힙니다
 *   · 위키드 — 무거운 무기는 적을 **눈에 띄게 뒤로 보냅니다**
 *   · 로스트아크 — 무력화가 터질 때 적의 자세가 **무너집니다**
 *
 * 때리는 쪽만 화려하면 "이펙트가 크다"가 되고, 맞는 쪽이 응답해야
 * "잘 맞았다"가 됩니다. 지금 이 게임은 앞쪽만 있었습니다.
 *
 * ── 무엇을 무게로 삼는가 ─────────────────────────────────────────────
 * **히트스톱**입니다. 새 축을 만들지 않는 이유: 이미 손끝·화면·궤적이
 * 전부 히트스톱에서 갈라져 나옵니다. 여기에만 다른 기준(피해량 등)을
 * 쓰면 *"손끝은 무거운데 눈은 가볍다"* 는 어긋남이 생길 수 있고,
 * **어긋날 수 있는 규칙은 언젠가 어긋납니다.**
 *
 * 양 끝은 게임 안에 실제로 있는 값에서 가져왔습니다 — 지어내지
 * 않았습니다. 쌍단검 1타가 0.035 로 가장 가볍고, 대검 마무리가 0.15 로
 * 가장 무겁습니다(arsenal.ts). 그 바깥(반격 0.18·처형 0.16)은 어차피
 * 자기 연출을 따로 받으므로 위쪽 끝에 붙여 둡니다.
 *
 * ── 두 채널로 나눠 싣습니다 ──────────────────────────────────────────
 *   1. **번쩍임의 길이** — 가벼우면 톡, 무거우면 잠깐 머뭅니다.
 *      ⚠️ **밝기는 등급을 안 매깁니다.** 처음엔 가벼운 타격을 어둡게
 *      만들려다 접었습니다 — 맞았는데 흐릿하면 그건 "가볍다"가 아니라
 *      **"안 맞았나?"** 로 읽힙니다. 한 대는 한 대로 또렷해야 하고,
 *      무게는 **얼마나 머무는가**로만 말합니다.
 *   2. **몸이 기우는 각도** — 밀려나는 방향으로 젖혀집니다.
 *      넉백 속도(`Velocity.k*`)에서 직접 뽑습니다. 새 상태를 안 만드는
 *      이유: 그러면 기울기와 밀림이 **서로 다른 곡선**으로 움직여서,
 *      몸은 아직 젖혀져 있는데 발은 이미 멈춘 그림이 나옵니다.
 */
export const HURT = {
  /** 가장 가벼운 타격(쌍단검 1타 0.035)이 만드는 번쩍임 길이(초) */
  flashLight: 0.09,
  /** 가장 무거운 타격(대검 마무리 0.15)이 만드는 번쩍임 길이(초) */
  flashHeavy: 0.24,
  /** 이 히트스톱 이하는 전부 「가장 가벼움」 — arsenal.ts 의 최솟값 */
  lightHitstop: 0.035,
  /** 이 히트스톱 이상은 전부 「가장 무거움」 — arsenal.ts 의 최댓값 */
  heavyHitstop: 0.15,
  /**
   * 몸이 젖혀지는 최대 각(라디안). 0.4 = 약 23°.
   * 더 키우면 넘어진 것처럼 보이는데, 넘어짐은 **무너짐(강인도)** 이
   * 이미 쓰고 있는 표현이라 두 상태가 헷갈립니다.
   */
  leanMax: 0.4,
  /**
   * 이 넉백 속도면 최대로 젖혀집니다. 대검 마무리(7.5)를 기준으로
   * 잡았습니다 — 가장 무거운 한 방이 최대치를 **실제로 찍어야** 위쪽
   * 눈금이 장식이 아니게 됩니다.
   */
  leanFullKnock: 7.5,
} as const

/**
 * 🤕 이 무게의 타격이 맞는 쪽에 남기는 번쩍임 길이(초).
 *
 * **이 함수가 유일한 출처입니다.** 넣는 쪽(combat.ts·main.ts)과 그리는
 * 쪽(visuals.ts)이 전부 여기를 지나갑니다. 예전처럼 각자 `0.12` 를 적어
 * 두면 한쪽만 고쳐도 아무도 모르므로, `npm run guard` 가 `flashT` 에
 * 직접 숫자를 넣는 코드를 잡아냅니다.
 */
export function hurtFlash(hitstop: number): number {
  const span = HURT.heavyHitstop - HURT.lightHitstop
  const w = span > 0 ? (hitstop - HURT.lightHitstop) / span : 0
  const k = Math.max(0, Math.min(1, w))
  return HURT.flashLight + (HURT.flashHeavy - HURT.flashLight) * k
}

/**
 * ── 🛡️ **정예 — 「아는 적인데, 이놈은 다르다」** ────────────────────
 *
 * ── 왜 넣는가: 이 존에는 **중간이 없습니다** ──────────────────────
 * 적의 체력이 이렇습니다 — 잡몹 58 · 얽는 자 34 · 끄는 자 46 · 궁수 40 ·
 * 돌격병 34 · **보스 620**. 58 과 620 사이가 **10.7배**로 비어 있고,
 * 그 빈칸이 세 곳에서 동시에 문제가 됩니다:
 *
 *   ① **가르칠 자리가 없다.** `npm run bleed` 가 적어 둔 대로, 출혈은
 *      채우는 데 6대가 필요한데 잡몹은 **2.8대**에 죽습니다. 그래서
 *      플레이어는 이 축을 **보스전에서 처음** 만납니다.
 *   ② **집중이 넘쳐서 버려집니다.** 자동 플레이 실측 — 번 것 30.0 ·
 *      태운 것 30.0 · **가득 차서 흘린 것 35.7**. 절반 이상이 쓸 곳이
 *      없어 사라집니다. 강타(집중 소모)로 무너뜨릴 만한 상대가 없어서입니다.
 *   ③ **한 구역이 99초 동안 같은 일입니다.** 무너진 회랑에서 21마리를
 *      잡는 데 99.2초 — 한 판(268초)의 37%가 같은 잡몹입니다.
 *
 * ── 참고: **아는 적을 세게** 만드는 것이 정석입니다 ────────────────
 * · **소울류의 흑기사** — 새 적이 아니라 *"같은 검사인데 이놈은 다르다"*.
 * · **디아블로의 챔피언/희귀 몹** — 같은 몬스터에 표식과 능력치만 붙습니다.
 * · **로스트아크의 네임드** — 잡몹 구간 사이에 놓여 준비물을 점검합니다.
 *
 * 공통점: **새 색(외울 것)을 안 늘립니다.** 그래서 정예는 잡몹과 **같은
 * 공격 목록**(🔴 찌르기 · 🟡 쓸기)을 씁니다. 새로 배우는 것이 아니라
 * **배운 것을 시험하는** 자리입니다.
 *
 * ── 숫자의 근거 ───────────────────────────────────────────────────
 * · `maxHp: 190` — 쌍단검 한 대 19.2 로 **10대**를 버팁니다. 출혈 문턱
 *   (6대)이 처음으로 **적이 죽기 전에** 닿습니다. 보스(620)보다는 훨씬
 *   짧아서 "작은 보스"지 두 번째 보스가 아닙니다.
 * · `poiseMax: 55` — 잡몹 30 의 배에 가깝습니다. **평타 연타로는 못
 *   무너뜨립니다.** 강타(집중 소모)를 써야 무너지고, 그게 ②의 답입니다.
 *   보스 105 보다는 낮아서 한 번의 붙음 안에 끝납니다.
 * · `damage: 22` — 잡몹 14 의 1.6배. 성수병 한 모금(회복 34)보다 작게
 *   두어, 한 대 맞았다고 곧바로 되돌릴 수 없게는 하지 않습니다.
 * · `moveSpeed: 2.6` — 잡몹 3.0 보다 **느립니다.** 크고 느린 것이
 *   *"붙었다 빠지는"* 답을 성립하게 합니다. 빠르고 센 적은 답이 없습니다.
 * · `attackRange`·`attackReach` 를 키운 이유: 몸이 커졌는데 사거리가
 *   그대로면 **그린 것과 닿는 것이 어긋납니다.** 몸 굵기(radius)가
 *   0.45 → 0.62 로 커진 만큼 그대로 더합니다.
 *
 * ⚠️ **나머지는 전부 잡몹에서 그대로 가져옵니다**(`...GRUNT`). 손으로
 *    다시 적으면 잡몹의 회전 속도나 등 뒤 반응 지연을 손보는 날 정예만
 *    옛 값으로 남습니다 — 이 저장소가 표를 손으로 나열했다가 여러 번
 *    당한 모양입니다.
 */
/**
 * 🛡️ 정예의 **몸 굵기** — 이 적의 유일한 실루엣 신호입니다(아래 `height` 주석).
 * 사거리 보정이 이 값에서 유도되므로 상수로 빼 둡니다.
 */
const ELITE_RADIUS = 0.7

export const ELITE = {
  ...GRUNT,
  maxHp: 190,
  radius: ELITE_RADIUS,
  /**
   * ── 📏 **키는 잡몹과 똑같습니다 — 사다리에 자리가 없습니다** ────────
   *
   * ── ❌ 2.15m 로 넣었다가 `npm run enemies` 가 되돌렸습니다 ─────────
   * 이 게임의 적 키는 **1.4 · 1.7 · 2.0 · 2.3 · 2.6 · 2.9** 로 0.3 사다리가
   * 꽉 차 있습니다. 색약 대비 규칙이 *"대응이 다른 적끼리는 키가 0.3m 이상
   * 갈려야 한다"* 라서, 2.15 는 얽는 자(2.3)와 0.15 밖에 안 벌어집니다.
   * 빈 칸은 **보스보다 큰 3.2m** 뿐인데, 정예가 보스보다 클 수는 없습니다.
   *
   * ── ✅ 그래서 **잡몹과 같은 키**로 둡니다 ───────────────────────
   * 규칙이 지키려는 것은 *"실루엣만 보고 **대응**이 정해져야 한다"* 입니다.
   * 정예는 잡몹과 **공격 목록이 글자 그대로 같아서**(같은 배열) 둘을
   * 헷갈려도 내야 할 답이 하나도 안 바뀝니다. 사다리에서 면제되는 유일한
   * 쌍이고, 검사도 그렇게 고쳤습니다.
   *
   * 그리고 이쪽이 **더 나은 그림**이기도 합니다. 「키가 조금 큰 잡몹」보다
   * 「같은 키인데 **1.6배 두꺼운** 잡몹」이 *"같은 놈인데 이놈은 다르다"* 를
   * 훨씬 분명히 말합니다 — 디아블로의 챔피언 몹이 쓰는 그림입니다.
   * 그리고 존에서 **우뚝한 실루엣은 보스 하나**로 남습니다.
   */
  height: GRUNT.height,
  moveSpeed: 2.6,
  /**
   * 몸이 굵어진 만큼 **그대로** 더합니다. 안 더하면 그린 것(예고 부채꼴)과
   * 닿는 것이 어긋납니다 — 숫자를 손으로 적지 않고 굵기 차이에서 유도합니다.
   */
  attackRange: GRUNT.attackRange + (ELITE_RADIUS - GRUNT.radius),
  attackReach: GRUNT.attackReach + (ELITE_RADIUS - GRUNT.radius),
  damage: 22,
  knockback: 3.4,
} as const

/** 보물 */
export const TREASURE = {
  /** 이 거리 안에 들어오면 획득 */
  pickupRadius: 1.5,
  bobHeight: 0.25,
  bobSpeed: 2.2,
  spinSpeed: 1.6,
} as const

/** 월드 */
export const WORLD = {
  /** 아레나 반지름(m) */
  arenaRadius: 26,
  gridSize: 2,
  /** 초기 적 스폰 수 */
  initialEnemies: 6,
  /** 적이 다 죽으면 다음 웨이브를 이 시간 뒤에 소환 */
  waveDelay: 2.5,
  /** 웨이브마다 늘어나는 적 수 */
  enemiesPerWaveGrowth: 2,
  maxEnemiesPerWave: 24,
} as const

/** 처치 시 연출 */
export const KILL_FEEDBACK = {
  hitstop: 0.13,
  trauma: 0.45,
} as const

/**
 * ── 실험용 덮어쓰기를 **여기서** 적용합니다 ─────────────────────────
 *
 * 파일 맨 아래인 이유: 이 파일을 가져다 쓰는 쪽이 모듈 초기화 때 값을
 * 붙잡아 두는 자리가 있습니다(예: playerControl.ts 의
 * `const TEMPO = PLAYER.tempo.attackScale`). main.ts 같은 데서 나중에
 * 덮어쓰면 그런 값들은 **옛날 값을 쥔 채로** 남고, A/B 는 조용히
 * 아무것도 안 바꾼 두 판을 비교하게 됩니다.
 *
 * `as const` 는 컴파일 타임 표시라 실행 중 대입은 됩니다. 타입 검사만
 * 여기서 한 번 벗겨 냅니다 — 이 한 줄 밖으로는 안 나갑니다.
 */
applyTweaks({
  CAMERA, PLAYER, GRUNT, BOSS, VIAL, BOSS_ARENA, POISE, FINISHER, EMBER,
  WEAPON_UPGRADE, BONFIRE, ARCHER, CHARGER, FOCUS, COUNTER, FALL, BINDER,
  DRAGGER, COMBAT, TREASURE, WORLD, KILL_FEEDBACK,
} as unknown as Record<string, unknown>)
