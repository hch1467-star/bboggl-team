/**
 * 봇의 **정책 상수** — 여러 도구가 같이 봐야 하는 값들.
 *
 * ── 왜 파일을 따로 두는가 ──────────────────────────────────────
 * 이 값은 봇에만 있는 것이 아니라 **레벨 배치의 제약**이기도 합니다.
 * 봇이 곁길을 40m 에서 자르면, 41m 짜리 보물은 아무리 잘 보여도 **규칙상
 * 영영 안 갑니다.** 그러니 배치를 검사하는 프로브도 같은 값을 봐야 합니다.
 *
 * ⚠️ `playthrough.mjs` 에서 가져오면 안 됩니다 — 그 파일은 불러오는 순간
 *    **판을 통째로 돌리는 스크립트**입니다. 프로브가 그걸 import 하면
 *    검사 대신 자동 플레이가 시작됩니다. (그렇게 짰다가 바로 걸렸습니다.)
 */

/**
 * 곁길 예산 — **걸어야 하는 거리**로 자릅니다(직선 아님).
 *
 * 40m 로 잡은 근거: 이 존의 곁길 셋(북쪽 단상 · 숨은 벽감 · 남쪽 함몰지)은
 * 전부 주 동선에서 **보이는** 거리에 있습니다(기둥 4 설계). 40m 면
 * "보이니까 들른다"는 되고, "존을 되돌아간다"는 안 됩니다.
 */
export const DETOUR_BUDGET = 40
